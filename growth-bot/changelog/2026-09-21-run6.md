# Run 6 — 2026-09-21 — MEASURE

## Classification

**MEASURE.** Nothing shipped. No verdict was due (`EXPERIMENTS.md` is still empty; the nearest seo-bot row is 09-22). The run's yield is two things: a **confirmed null** on the primary metric, read on two independent instruments, and a **correction to a recorded number about the primary metric's own instrument** — the "42% of bookings are dropped" figure that has been leading Daniel's blocker list since run 5.

**Window: 2026-08-24 → 2026-09-20**, against 2026-07-27 → 2026-08-23. This shares **27 of 28 days** with run 5's window, so under the standing rule it is not a new measurement of traffic. **GA4 was deliberately not re-read.** Run 4's traffic figures stand.

## The primary metric: no change, confirmed null on two instruments

**12 bookings, ZERO qualified, website-sourced still 3.** The window moved forward one day and nothing entered or left it.

- **Gmail, sender-scoped** (`from:noreply@wescalestartups.com OR from:notifications@calendly.com`, 35d): 16 threads, and the most recent booking-related message is Revathy Sivasankaran's reschedule on **2026-09-17**. The last *new* booking notice is Josefina on **09-16**. Nothing on 09-20.
- **Gmail, subject-scoped** (the run 5 addendum's query 1, 7d): 18 threads. No Cal.com Growth Audit booking after 09-16. What it does surface is covered under negative findings below.
- **Clarity, last 3 days** (31 sessions, 37 bots excluded): **no `/book/thanks` pageview anywhere in Top pages with URL parameters on**, and the Smart events card reads **"No data available"** — the `Order success` event did not fire once. Two instruments, same answer.

**Fifteen consecutive unqualified bookings** across every window this programme has measured.

## The correction: "the email feed drops 42% of bookings" overstates the gap

Run 5 recorded that the booking feed missed **5 of 12** bookings (Tom McColllum, Thabi Pulikkotil, Scott McKim, Lei Zhao, Koa Browne) and that figure — 42% — has since led both the blocker list and the case for the work-calendar connector. Run 6 re-checked all five against the wider query set. **Four of the five are recoverable from Gmail:**

| Invitee | New-event notice | Recoverable how |
| --- | --- | --- |
| Tom McColllum | absent | **Two `has been rescheduled` notices from `noreply@wescalestartups.com` on 09-15 — in the very feed recorded as missing him** — plus a Google Calendar `Accepted:` |
| Thabi Pulikkotil | absent | `Accepted: Growth Audit \| Thabi × We Scale Startups`, `calendar-notification@google.com`, 09-16 |
| Scott McKim | absent | Daniel's own outbound thread to `scott.mckim@ferrock.co.uk` (08-31), booking link sent by hand on 09-09 |
| Koa Browne | absent | Daniel's own outbound thread to `partner@onfound.com` (09-10) |
| **Lei Zhao** | absent | **No Gmail trace under any query tried** |

**The honest figures: 5 of 12 (42%) missing from the new-event notice · 3 of 12 (25%) with no booking notification of any kind · 1 of 12 (8%) invisible to Gmail entirely.**

The 42% is a property of **one query shape**, not of the feed. Tom McColllum is the sharpest case: he was recorded as missing from a feed that contains two emails about his booking, because only `A new event has been scheduled` subjects were read. **This is the programme's recurring failure mode exactly — a number taken from one instrument and never re-checked against it — and it is the second one this bot has written about its own primary metric.** Sixteenth false fact. The wrong line has been deleted from `PLAYBOOK.md`, not annotated.

**This does not weaken the ask for the work calendar; it sharpens it.** The metric is currently assembled by hand from three Gmail query shapes plus a Clarity ledger, each incomplete. One instrument that sees every booking is still the fix. But Daniel should be told 3 of 12 and 1 of 12, not 42%.

## Diagnosis

Unchanged, and not re-derived: **the site converts non-ICP visitors competently and says nothing about who it is for.** Nothing in this window's evidence moves it. The website denominator has now survived five attempts to move it and sits at 3 per 28 days.

## Why nothing shipped

Gate 1 fails before the others are reached: no new constraint was identified this window, and the one identified constraint (no positive ICP statement where the decision is made) is **blocked on backlog 1** — Daniel's answer on whether non-ICP booked calls are a problem — and **held until 2026-09-29** by the money-page observation window. Shipping into `/pricing` or `/book` now would spend the one clean pre/post read the programme has, on a change whose direction is not yet authorised. Gates 2 and 6 both fail.

## Negative findings — do not re-derive

- **No booking landed 09-18 → 09-21.** Confirmed on the Cal.com/Calendly feed, the subject-scoped query and Clarity's `/book/thanks` + Smart events. Do not re-check this range.
- **No friction, fourth reading.** Clarity last 3 days: rage 0%, dead clicks 0%, excessive scrolling 0%, quick backs 6.45% (2 sessions of 31 — two sessions on a tiny base, not a signal). Zero JavaScript errors. Pages/session 1.48, scroll 26.47%, active 21s.
- **All eight money pages are live and fast.** `/`, `/book`, `/pricing`, `/contact` and all four `/services/*` returned **200 in 0.33–0.86s**.
- **Performance, last 3 days: 87/100 from 4 pageviews — LCP 1.2s, INP 130ms, CLS 0.072, all three good.** A four-pageview sample decides nothing; recorded only so the next run does not read it as a change against run 5's 28-day 80/100.
- **Non-Cal.com calendar meetings are not bookings.** The subject-scoped query surfaces Google Calendar acceptances titled `Daniel Johnson + <name>` / `Daniel Johnson × <name>`: James Sedgemore (TML Partners, a marketing recruitment firm, 09-21), Phil Benson (Benson Finance, 09-25), Sagnik Halder (a Boardy.ai intro, cancelled 09-17). **The tell is the title — a Cal.com Growth Audit booking is always `Growth Audit | <name> × We Scale Startups`.** These are Daniel's own networking and partner meetings, out of ICP and outside the funnel. Recorded so no future run counts them as a booking surge.
- **The built-in browser still has no Google session.** `clarity.microsoft.com` shows the signed-out card. Chrome was connected and worked on the first attempt. Re-confirmed, not re-investigated.

## Handoff to seo-bot

**None this run.** No SEO-shaped constraint surfaced. Noted only in passing, and not acted on: three of the last three days' pageviews carry `utm_source=chatgpt.com` (`/case-studies/equoo` ×2, `/insights/growth-signal-loop`, `/`). AI-referred traffic is arriving on case-study and insight pages. That is seo-bot's series to read, on its own instruments, over a window longer than three days — it is not a finding and should not be treated as one.

## Assumptions

- The window's booking set is taken as unchanged from run 5's corrected count of 12 rather than rebuilt from scratch, on the grounds that both endpoints were checked: nothing new after 09-16, and no booking on 08-23 (the day that dropped out) in any feed.
- Revathy Sivasankaran has two separate `A new event has been scheduled` notices (08-26 and 09-02) for different times. Run 5 counted her once. **Not relitigated this run** — it cannot change the qualified count, which is zero either way — but flagged here so a future run that rebuilds the ledger resolves it deliberately rather than by accident.

## State files

- **`PLAYBOOK.md` compacted on disk with scripted edits before analysis**, per the 30KB rule: 31,455 → 30,226 bytes, then the run 6 entries added. Merged the two booking-under-count bullets into one corrected bullet, the four friction readings into one, the two website-conversion method notes into one; compressed the build-dirties-tree note (now fixed in `.gitignore`), the stale Rankrat connector note, and closed backlog items 2–5 into a single line; deleted two struck-through CLOSED items. **No operative instruction was dropped.**
- **`FUNNEL.md` gets no row.** The last row was added at run 3 on 09-17, four days ago; the rule is one per week.
- **`EXPERIMENTS.md` unchanged.** Nothing shipped, so no row and no new lock.

## Locks observed

`/insights/what-is-a-fractional-cmo`, `/insights/how-to-run-a-90-day-growth-sprint`, `/services/90-day-growth-sprint` (entangled with the 09-17 row), `/facts/we-scale-startups` (09-22), the three pillars + `/insights` hub + orphans (09-23), `/insights/what-226-founder-reviews-reveal` (09-24), `/services/fractional-cmo` (10-03); and no title or meta-description change on any money page before 2026-09-29. `git log` for the last 24 hours shows one commit, `abc0134`, touching `growth-bot/PLAYBOOK.md` and `.gitignore` — no page. **Nothing was touched.**

## Blocked on Daniel

1. **Are non-ICP booked calls a problem to fix, or part of the model?** Unchanged since run 1, now twelve for twelve with 75% of volume from channels Daniel built by hand. Nothing about money-page filtering can ship until this is answered, and **2026-09-29 is the moment it becomes actionable** — an answer before then is worth materially more than one after. Eight days.
2. **Authorise the `daniel@wescalestartups.com` work calendar on the Google Calendar connector.** Still the highest-value access fix, on corrected figures: 3 of 12 bookings leave no notification of any kind and 1 of 12 leaves no Gmail trace at all.
3. **Confirm the 2026-09-29 money-page interpretation** (body copy and proof placement available, no title/meta change). Unconfirmed since run 1 and load-bearing.
4. **Keep Chrome running with the Claude extension when the daily run fires.** It was connected this run and Clarity answered first time.
5. Configure `clarity` and `cloudflare` accounts on the search-analytics connector if that is possible.

## Tomorrow's single highest-value action

**Nothing, unless a booking lands.** The window moves one day; traffic shares 27 of 28 days with this read; the behavioural baseline, the performance question and now the booking-instrument question are all closed. The next run with real work is **2026-09-29**, judging the money-page window with seo-bot — or the first run after Daniel answers backlog 1, whichever comes first. If a booking arrives, reconcile it across both ledgers and qualify it from the `/book/thanks` payload.

---

# Addendum — SHIP (interactive, same day)

**The unattended run above classified MEASURE and shipped nothing. Daniel then picked the session up in chat, pushed back on booking-count-only measurement, and the run became a SHIP.** Recorded as an addendum rather than a rewrite, because the unattended reasoning stands on its own evidence and should not be retro-fitted.

## What changed the run

Daniel's instruction: be data-driven beyond the booking feed, and ask questions when there is nothing. Acting on it surfaced that the **WSS Search Analytics connector was present again** after three consecutive absences — the playbook had written it off as gone. GA4 and GSC both answered on the first call. That is what made the rest possible.

## The three decisions

1. **Backlog 1, open since run 1, is ANSWERED: "More calls, don't filter."** Non-ICP booked calls are part of the model. The programme optimises for booking-CTA volume and does not ship ICP-filtering copy. Backlog 6 retired.
2. **The 09-29 money-page hold: delegated to me, settled as assumed.** Body copy, CTA placement and proof placement available now; no title or meta-description change on a money page before 2026-09-29. Reasoning of record: that window measures money-page clicks *from search*, which titles, meta and rank drive — on-page body and CTA changes cannot move it. **If that reasoning is wrong, the run 6 verdict is confounded.**
3. **`book_call` is now the primary metric for judging shipped changes**, with qualified bookings kept as the commercial scoreboard and reported every run. Delegated to me. At 3 website bookings per 28 days nothing is ever judgeable; at 357 CTA impressions it is.

## A diagnosis I got wrong and withdrew inside the run

I first claimed money pages under-serve the CTA. **False.** Impressions per session: `/case-studies` 100%, `/about/daniel` 79%, `/pricing` 71%, `/services/growth-diagnosis` 50%, and **`/` 40% — the worst on the site**. Money pages get few impressions because they get few visitors. Withdrawn before it reached the change brief, which is the only reason it did not become the seventeenth false fact.

## What shipped

**`src/components/StickyBookCta.astro` — CSS only, no markup change.** The bar renders three CTAs; the component's own comment said the newsletter and WhatsApp links get *"identical space and identical weight — neither is the junior partner."* Measured consequence: on a 375x812 phone the bar stood **264px tall, a third of the viewport**, and covered the price tiers on `/pricing`. Both secondary links keep their place and their labels; only their weight changes — card chrome and the two-line descriptions go, the badge shrinks, and the booking button grows (2.4rem → 2.75rem, 0.85rem → 0.95rem).

**Measured before/after, headless Chromium against the built `dist`, both viewports:**

| | before | after |
| --- | --- | --- |
| Bar height, mobile 375x812 | 264px | **189px (−28%)** |
| Bar height, desktop 1440x900 | 169px | **153px** |

Screenshots compared on `/pricing` and `/services/growth-diagnosis`. First mobile attempt put the two links side by side and truncated both labels; reverted to stacked single-line, which costs 38px and keeps them readable. **`npm run build` and `astro check` clean, 121 pages, 110 sitemap entries, `check:lastmod` current — all unchanged.**

## The lock judgement, stated openly

`StickyBookCta` is a **shared component and renders on every page, including pages inside open seo-bot verdict windows.** FIXED RULE 6 forbids touching those indirectly *"where that would materially change the rendered page"*. **I judged it does not, because every open verdict is judged on search clicks or AI citation, and a bottom-fixed CTA bar cannot influence impressions, rank or citation.** The page content, headings and schema are untouched. **This is a judgement call, not a given. It is the one thing in this run most likely to be wrong, and it is flagged to Daniel rather than buried.**

## State

- **`PLAYBOOK.md` split at 34,456 bytes** — settled evidence moved to the new `growth-bot/FINDINGS.md`, mirroring seo-bot's run 26 split. Playbook now 21,406 bytes with headroom; nothing dropped.
- **`EXPERIMENTS.md` has its first row in six runs**, and therefore the programme's first lock: `StickyBookCta.astro`, review **2026-10-19**.

## Next run

Do not touch the sticky CTA again before 2026-10-19. Report `book_call` and `sticky_book_cta_shown` every run from here so the trend is visible rather than reconstructed at the verdict. Nearest verdict is seo-bot's `/facts/we-scale-startups`, 09-22.
