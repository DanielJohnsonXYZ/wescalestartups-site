# Run 27 — 2026-09-14

**Classification: MEASURE. Nothing shipped to the site, and no verdict was due.** The run exists because run 26's browser was unreachable and the mandatory workstream went untaken; Daniel restored Chrome and asked for a retry.

**The most useful finding is not the panel. It is that the Generative AI report has never been blocked by a connector fault — Chrome is signed in as `admindjohnson@gmail.com`, which does not have access to the Search Console property.** Two runs of "browser unavailable" resolve into one account mismatch with a one-line fix.

Instruments first, per the standing rule.

| Check | Result | Consequence |
| --- | --- | --- |
| `accounts_manage list` | **Both phantoms present** — removed. | Trap 11 as written. Not diagnosed. |
| `analytics_query` `["page"]` row one | **`https://wescalestartups.com/`, 39 clicks** — no `www.` | **Run 26's repair HOLDS. Second consecutive clean run.** |
| Browser | **Connected**, but see §3 — signed into the wrong Google account, and it dropped twice mid-panel | Panel taken; **Search Console UI refused** |
| **GSC terminal date** | **2026-09-11** — advanced three days in three calendar days | **Lag settled at 3 days. 25 of 28 days shared with run 26. Still not independent.** |

---

## 1. AI PANEL — READING #11. The share held at 2/10 and that is the misleading part.

**Google AI Mode, full ten prompts, all completion-verified via the `AI Mode response is ready` tell, scrolled to the sources card, none re-issued after scoring.**

**Mention 2/10 · own-domain citation 2/10 · cited own URLs 4 · accuracy 2/2 · winnable surface 9/10.**

**Series: 2, 2, ~~2~~, ~~1~~, 3, 2, 2, 3, 3, 2, 2.** The 2–3 range holds; **two consecutive 2s is not a level either, and this file will not make run 24's mistake in the other direction.**

**The movement is underneath the headline. Prompt 6 has now lost BOTH halves** — reading #10 recorded it losing the body naming while keeping the `/` citation, and #11 completes the exit: no naming, no citation, on a grounded answer with ten source hosts. **Own-domain citation 3/10 → 2/10; cited URLs 6 → 4.** Nothing was gained elsewhere.

**This is the reading that justifies run 24's rule about reporting breadth and URL count separately: the headline number concealed a decline in both.**

**The two survivors are unchanged and both accurate.** Prompt 3 (WSS named, `/services/90-day-growth-sprint` cited, correct 12-week / 6–8 ICE-scored method detail for a third reading). Prompt 5 (WSS leads the list, **three own-domain URLs** — `/facts/we-scale-startups`, `/insights`, `/about` — stage and price band correct for the eighth time).

**One flagged item CHECKED AND CLOSED rather than left open.** Prompt 5 rendered *"1-week Growth Diagnostics (£2k–£4k)"*. The reading file flagged the duration as unverified. **`src/site.ts:957` — `pricingTiers` Growth Diagnosis carries `duration: "1 week"`.** Accurate, source-derived, **not** an engine-invented specific. **Accuracy 2/2 is fully verified, not assumed.**

Absences worth naming: **prompt 2 wholly ungrounded for a second consecutive reading** (zero source domains — the whole of the 9/10 surface); prompts 4, 7, 8, 9 all at a **seventh consecutive absence**; prompt 8 still shows `lennysnewsletter.com`; prompt 9 still reaches WSS only through Growth Division's source card, misnamed, dated 7 May 2026; prompt 10's replacement wording is clean of M&A content for a third reading.

Full tables: `seo-bot/ai-visibility/2026-09-14-reading11.md`.

## 2. PERPLEXITY — one valid absence that states the authority constraint better than any number in this file, and a fifth mutation of the contamination tell

**Prompt 9 — VALID ABSENCE, 24 sources, no personalisation register.** Perplexity named **roughly twenty-five** UK growth advisors and firms — Yaniv Goldenberg, Maria Botta, Oren Greenberg, Growth Division, K3C, Winnow, Scaled Consulting, Gravitonic, Partner in Growth, Kurve, VCMO, GrowPad, Powered by Search and more. **WSS is not one of them. `growth-division` is cited six times and supplies the pricing paragraph.**

**Quote this when the backlog is next challenged.** A buyer asks the single most commercially relevant question in the panel; the engine returns twenty-five vendors from twenty-four sources; WSS appears nowhere — while Google, on the same question, reaches WSS only via a competitor's page. **There is no content defect visible in that answer. It is 29 referring domains, and it is the clearest picture of the constraint the panel has produced in eleven readings.**

**Prompt 5 — PERSONALISED, UNSCOREABLE, NEW TELL.** Perplexity named WSS, then wrote *"Suggested homepage copy / Headline: Turn post-PMF traction into a repeatable growth system…"* and *"Stronger differentiation: Avoid positioning the consultancy as a general 'AI growth agency.'"*

**It is consulting *for* WSS, not recommending WSS to a buyer.** No "Given you run", no "based on your" — **the assumption is never stated, only acted on. This is the fifth mutation and the first that is purely structural rather than lexical; no regex would catch it, and the playbook's invariant does.**

**This also settles something and should stop being retested:** run 25 cleared the memory store, turned off search history and confirmed personalisation survived. **Five days later, with no further intervention, it survives. No account setting fixes it. The only open question is signed-out readings, and that is Daniel's.**

## 3. THE GENERATIVE AI REPORT WAS NEVER A CONNECTOR PROBLEM

Run 26 reported *"no AI panel and no Generative AI report"* as one blocker with one cause. **They had two different causes, and only one of them was the extension.**

With Chrome restored, `search.google.com/search-console/performance/search-analytics/ai?resource_id=sc-domain:wescalestartups.com` redirected to `not-verified` and rendered:

> **"Oops, you don't have access to this property. Signed in as: `admindjohnson@gmail.com`. Property: wescalestartups.com."**

**The API connector authenticates as `daniel@wescalestartups.com` and works fine. The browser is signed into a different Google account that is not a user on the property.** So the Generative AI report — **the only instrument for AI impressions, and UI-only because `searchAppearance` returns `[]`** — has been unreadable for a reason no amount of extension debugging would have found.

**Two fixes, either sufficient:** sign Chrome into `daniel@wescalestartups.com`, or add `admindjohnson@gmail.com` as a user in Search Console → Settings → Users and permissions.

**Consequence for the record: Generative AI impressions were NOT read for a second consecutive run.** Run 25's **258 → 337** is still a single reading. **Do not treat that rise as established.** Two open verdicts — run 15 (09-17) and run 19 (09-22) — are explicitly to be judged on that channel, and **both fall due before this is fixed unless Daniel acts.** That makes this the run's highest-value ask, ahead of anything about the panel.

**Method note, and it is Trap 12 gaining a fourth case.** The taxonomy was fault / refusal / stale-dataset. **This is a fourth: a working connector, correctly authenticated, pointed at a surface the *browser's separate identity* cannot see.** Tell: the tool succeeds, the page loads, and the page itself says no. **Read what the page says before writing down a cause.**

## 4. MEASUREMENT — money pages moved for the first time in four readings, and it went down

**28d to 2026-09-11: 55 clicks / position ~17.5 on the homepage row.** Window 2026-08-15 → 2026-09-11, **sharing 25 of 28 days with run 26's.**

| Money page | Clicks | Was |
| --- | --- | --- |
| `/pricing` | **2** | 3 |
| `/contact` | 1 | 1 |
| `/services/90-day-growth-sprint` | 1 | 1 |
| `/book` | 0 | 0 |
| **Total** | **4** | **5** |

**Sequence 2 → 3 → 3 → 5 → 5 → 5 → 5 → 4.**

**The honest reading, and it cuts against the optimistic one this file was drifting toward.** Four consecutive readings at 5 were reported across windows sharing 24–27 of 28 days — **one observation repeated, as runs 25 and 26 both said.** The first change in that number is **downward**, and it is a single `/pricing` click on a window that still shares 25 of 28 days. **It is turnover, not a trend, in exactly the same way the four 5s were.**

**What it does do is puncture the framing.** "Held at 5" was starting to read as stability. **It was never measured as stability, and the moment the window turned over three days it moved.** Nothing before a genuinely independent window says anything, and this run does not have one.

Non-homepage clicks 17 → 16. Named clicks still from the same four brand/collision queries. **Twenty-seven runs, still zero clicks from any named query that is neither WSS's brand nor another company's.**

## 5. What was deliberately not done

- **Nothing shipped.** No change was available whose expected effect on booked calls is real and measurable, and no build-verifiable defect surfaced — run 26's arithmetic check (`121 built − 11 noindexed = 110`) was restored and no new page has landed since.
- **No verdict taken. None was due.** Run 9 **09-16**, run 15 **09-17**, run 19 **09-22**, runs 10–13 and backlog 10's lead **09-23**, original research **09-24**, run 3 **10-03**.
- **No page inside an open verdict window touched.**
- **Backlog 15 still declined** — unblocks 09-17. Run 26 already did its shape check; **judge it on 13 impressions at position 27, not 94 at 41.4.**
- **The Chrome Google account NOT changed.** Signing a browser into a different account is the user's to do. Reported as an ask.
- **Perplexity memory not touched** — settled as unfixable by setting; nothing to try.
- **The API repair not diagnosed** — standing instruction. It held; that is all that was checked.
- **Bing not pulled.** Tenth run running; deliberate.
- **Nothing sent, posted, or created off-site.** Prohibition 17.

## 6. Blocked on Daniel

1. **Search Console access for the browser — the highest-value item, and it is one setting.** Chrome is signed in as `admindjohnson@gmail.com`, which cannot see the property. Either sign Chrome into `daniel@wescalestartups.com`, or add that Gmail as a user in Search Console → Settings → Users and permissions. **Until then there is no Generative AI report, and the run 15 (09-17) and run 19 (09-22) verdicts both fall due on a channel that cannot be read.**
2. **Sign-off on signed-out panel readings.** Perplexity contamination is confirmed unfixable by any account setting and has now mutated a fifth time. Moving the panel to a signed-out browser would fix it outright and **arguably measures a buyer better than a signed-in account does** — but it breaks the eleven-reading series and needs a deliberate decision. **Related and new: reading #11 was taken signed in as `admindjohnson@gmail.com`, and no reading in the series has ever recorded which account it was on.**
3. **Original research from the 479+ founder sessions.** Unchanged, still the only open Workstream C route, and §2 is the argument for it: prompt 8 is held by `lennysnewsletter.com` for a seventh reading, on a question WSS has two corpora about. **Publish from the PUBLIC corpus only.**
4. **Facebook** — contact email still `hello@`, category "Consulting agency", 2022 cover image.
5. **Instagram** points at a Linktree rather than the site.
6. **The extension config that recreates the phantom accounts** (Trap 11). Both present again. Per-run chore until the manifest is fixed.

## Carry forward

1. **The Generative AI report is blocked by a BROWSER ACCOUNT MISMATCH, not a connector fault (§3).** Do not re-diagnose the extension. Check `admindjohnson@gmail.com` vs the property before concluding anything about browser tooling.
2. **Record the signed-in browser account with every panel reading.** Eleven readings, none of them recorded it.
3. **Panel #11: headline 2/10 held while citation breadth 3→2 and URLs 6→4 fell. Prompt 6 is fully out.** Report all three numbers.
4. **Money pages 5 → 4 on a 25-of-28-day overlapping window (§4).** Neither the four 5s nor this 4 is independent evidence. **Stop describing the sequence as stable.**
5. **Perplexity prompt 9 — twenty-five vendors, twenty-four sources, WSS absent.** The cleanest available statement of the authority constraint.
6. **Trap 12 has a fourth case: a working, correctly-authenticated connector pointed at a surface the browser's separate identity cannot see.** Read what the page says.
