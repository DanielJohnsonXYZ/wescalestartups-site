# Run 17 — 2026-08-27

**Classification: REPORT. Nothing shipped. The deciding fact is that no new search data exists.**

Run 16 closed a few hours earlier on the same calendar day. Search Console's window has not advanced since, so this run had one genuinely new instrument reading and no new evidence on which to change the site.

## The panel — reading #2, and a first

Both engines, ten prompts, all ten successfully run on each. No prompt marked unavailable.

| Engine | Mention share | Citation share | vs reading #1 |
| --- | --- | --- | --- |
| Perplexity | **1/10** | **1/10** | 0/10 → 1/10 |
| Google AI Mode | 2/10 | 2/10 | unchanged |

**Perplexity cited `/services/90-day-growth-sprint` on prompt 3 — the first Perplexity citation ever recorded for this site.** WSS appeared second in the list, behind Brendan Hufford's Growth Sprints. Reading #1 found Perplexity's zero to be uniform across all ten prompts and both the answer body and the source panel, so this is a change in kind rather than degree.

**Two readings hours apart cannot establish a trend and this one does not.** The honest statement is that a first citation appeared, not that a rate moved. Reading #3 on a different day is the first that can say anything about direction.

Google AI Mode was stable: same two prompts (3 and 5), same URLs, `/services/90-day-growth-sprint` and `/facts/we-scale-startups` + `/about` + `/press`. On prompt 5 WSS was listed first, ahead of Growth Division, but a single observation cannot support a positioning claim.

**Accuracy 3/3.** No "Ltd", no incorporation claim, no pricing, stage or geography error.

Two findings worth more than the scores:

- **Prompt 9's listicle reproduced identically.** Google again surfaced a third-party page reading "Growth Division, GrowthCurve, Kurve, Rise Marketing, and WeScaleStartups" in its sources but not its answer. Scored not-named both times. The reproducibility is the point: this is a stable retrieval path, not a one-off, and it is the evidence behind Workstream C's directory thesis.
- **Google returned zero external citations on prompts 2, 6 and 10.** Fully ungrounded answers. Those three prompts are structurally unwinnable in their current form — no brand gets a surface at all. Worth tracking as a property of the panel rather than a failure of the site.

**One soft defect, logged not acted on.** Google filed WSS under the heading "Paid Ads & Conversion Rate Optimisation (CRO) Sprints" and described the sprints as "often spanning paid media and creative". The sprint description itself is accurate. The categorisation is not: it frames a diagnosis-first consultancy as a paid-media shop. Not a factual error, so not a defect to fix on the site today, but it is the second time an engine has drifted the positioning and the run-16 rule applies — check the off-site profiles before touching a page.

`/insights/what-226-founder-reviews-reveal` appeared on no engine, no prompt, in body or sources. Expected hours after publication.

## Why nothing shipped

**Backlog 11, `/gtm-strategy`, was the queued candidate and it fails the gate.** The SERP check is what made that clear rather than marginal.

- **Gate 8 — the live SERP.** `go to market strategy for b2b saas startup` returns Reddit, Amplitude, Cognism, DevSquad, ZoomInfo, Arise GTM, Medium and YouTube. High-authority guide content and UGC. That is authority-bound at 29 referring domains, and no lede rewrite reaches it.
- **Collision risk, and it is the strongest argument.** The page that should own that SERP is the pillar `/insights/b2b-saas-gtm-strategy`, which is **locked inside run 13's verdict window until 2026-09-23**. Pointing `/gtm-strategy` at the same intent would build exactly the self-competing cluster run 3 spent a whole run undoing, and the pillar could not be adjusted to compensate.
- **Gate 4.** It would be the fourth direct-answer-lede intervention shipped with the first verdict still due (run 4, **2026-09-01**). Shipping a fourth instance before the first result lands means learning less from all four.
- **Gate 5 and 7.** 75 classic impressions at position 39.8, 25 AI impressions over three months, zero clicks. Marginal even if it worked.

**Run 16 held this page on the same evidence a few hours ago.** Nothing has changed since except a panel reading that is explicitly noise. Re-deciding an unchanged question because a run was invoked is manufacturing work.

**Locks.** Nineteen files under `src/` and `public/` were committed in the previous 24 hours, including Daniel's own Category Reframe work (`e7da767`). `/gtm-strategy` was verified **not** locked: its rendered text is byte-identical to the 24-hour-ago build, checked by building both trees and diffing visible text rather than by reading the file list.

**No verdicts due.** Nearest is run 4 on 2026-09-01.

## Instrument failure — blocked on Daniel

**The GSC connector's OAuth token has expired or been revoked** (`invalid_grant`). It needs re-authorising.

The Search Console UI still works through Chrome and confirmed the window has not moved: 3-month total now **148 clicks / 16.3k impressions / 0.9% / position 25.9**, against 144 clicks this morning. That +4 is the trailing edge settling, and the daily chart still ends 24 August. **The 28-day comparison window is the same one run 16 analysed.**

The UI is a workable fallback for totals and for the Generative AI report. It is not a practical substitute for `page,query` analysis, which is what confirming `/gtm-strategy`'s real query set would have required. So the connector outage genuinely narrowed this run.

## Carry forward

- **Re-authorise the GSC connector.** Until then, `page,query` work is unavailable and any run is measurement-limited.
- **`/gtm-strategy` should now be reconsidered rather than simply queued.** Run 16 recorded it as "diagnosed, unblocked, executable in minutes". The SERP check says otherwise. It is a candidate only if the target is a query cluster that does not collide with the locked pillar, and that cannot be established without `page,query`.
- **Reading #3 of the panel must fall on a different day.** Same-day readings cost a full panel run and produce noise.
- Watch whether Perplexity's prompt-3 citation persists. A single citation that disappears is worth as much as one that stays.
