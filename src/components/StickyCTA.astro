---
import { siteConfig } from "../site";
---

<div class="sticky-cta" id="sticky-cta" data-hidden="true" aria-hidden="true">
  <div class="sticky-cta-inner">
    <p class="sticky-cta-label">Fix messy growth →</p>
    <a class="button button-brand sticky-cta-button" href={siteConfig.bookingUrl} data-cta="sticky-book">
      {siteConfig.bookingLabel}
    </a>
  </div>
</div>

<script is:inline>
  (function () {
    const el = document.getElementById("sticky-cta");
    if (!el) return;
    const show = () => {
      const show = window.scrollY > 600;
      el.dataset.hidden = show ? "false" : "true";
      el.setAttribute("aria-hidden", show ? "false" : "true");
    };
    window.addEventListener("scroll", show, { passive: true });
    show();
  })();
</script>
