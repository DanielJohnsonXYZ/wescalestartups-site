# Growth-bot findings — the settled record

**Split out of `PLAYBOOK.md` at run 6**, which had reached 34,456 bytes. The playbook is the working document and must stay small enough to re-emit safely every run; this is the evidence it kept growing on. **Read `PLAYBOOK.md` first, then this. Detail beyond what is here lives in `changelog/`.**

Everything below is settled unless a line says otherwise. **Do not re-derive it. When you correct one, DELETE the wrong line — do not annotate it.**

## Settled findings

- **SEVENTEENTH FALSE FACT, AND THE THIRD THIS BOT HAS WRITTEN ABOUT ITS OWN INSTRUMENTS (run 7, corrected same day).** Run 7 recorded the Rankrat connector **absent**, called it "the fourth absence in seven runs", reported GA4 and `book_call` as unreadable, and led Daniel's blocker list with it. **All of that was false.** The server was **still connecting** when the first-minute check ran; a bare `ToolSearch` for its tool names missed because the schemas had not loaded. It was healthy the whole time: 90+ tools, `accounts_list` answering first call, `diagnostics` `ready: true`, `provider_readiness` for `google` `available: true`. **The shape is identical to the previous sixteen: a claim taken from one instrument reading and never re-checked against the thing it is about.** `RefreshMcpTools` names a server as still connecting, and `ToolSearch` waits for connecting servers — **either one would have caught it, and one of them is now mandatory before recording any connector as unavailable (Trap 13).** Runs 3, 4 and 5 recorded the same absence and cannot be verified retroactively; treat them as suspect.
- **`book_call` AND `sticky_book_cta_shown`, THE PRE-CHANGE BASELINE, CONFIRMED ON AN INDEPENDENT WINDOW (run 7, GA4 `259840282`).** 2026-08-25 → 2026-09-21 against 2026-07-28 → 2026-08-24:

| Event | Prior 28d | This 28d | Change |
| --- | --- | --- | --- |
| `session_start` | 1,303 | 1,014 | **−22.2%** |
| `sticky_book_cta_shown` | 335 | 352 | **+5.1%** |
| `book_call` | 32 | 30 | −6.3% |
| `ai_referral` | 14 | 17 | +21% |
| `form_start` | 11 | 8 | −27% |
| `scroll` | 206 | 168 | −18% |
| `page_view` | 1,682 | 1,244 | −26% |

  **`book_call` per CTA impression 9.55% → 8.52%; per session 2.46% → 2.96%.** This is a **second, non-overlapping-endpoint confirmation of run 6's finding**: sessions falling hard while CTA impressions rise, and click-per-impression declining. **The constraint run 6 shipped against is real and was not an artefact of one window.** The sticky-CTA change landed 2026-09-21 19:08, roughly five hours before this window closed, so **none of this is a read on the change** — it is the baseline the 2026-10-19 verdict is judged against.
- **`form_submit` HAS FIRED ZERO TIMES IN 56 DAYS, AND THIS IS NOT A DEFECT (run 7).** It appears in neither 28-day window. The contact form carries **no `action` attribute** — it submits by JS fetch — so GA4 enhanced measurement never sees a form navigation to fire on. **Customer.io proves the submissions land.** Recorded explicitly because "Turnstile is CSP-blocked" plus "zero `form_submit`" is an obvious and wrong conclusion waiting to be drawn; **the forms work. Do not chase this.**

- **CLOUDFLARE TURNSTILE HAS NEVER EXECUTED ON THIS SITE. THE CSP BLOCKS IT, AND HAS SINCE 2026-08-06 (run 7, read on the live page).** `4c0f32a` (2026-08-06) added Turnstile to public forms and hard-coded `https://challenges.cloudflare.com/turnstile/v0/api.js` into `BaseLayout.astro`. `public/_headers` was last touched 2026-08-09 and **`challenges.cloudflare.com` is in neither `script-src` nor `frame-src`**; the live response header matches the file. On live `/contact`, all four `.cf-turnstile` containers (sitekey `0x4AAAAAAEIhzoHuWLsynQnO`) are **empty — zero children, zero height**, `window.turnstile` is `undefined`, and **no `cf-turnstile-response` input exists in the DOM**. Five surfaces carry it: `src/pages/contact/index.astro`, `src/components/EmailSignup.astro` (the sitewide footer newsletter), `src/pages/growth-course.astro`, `src/pages/podcast-guest-application.astro`, `src/pages/refer.astro`.
  - **IT IS NOT A CONVERSION DEFECT, AND THAT WAS SETTLED ON DATA RATHER THAN HEDGED.** `_form-guard.js` fails **closed** on a missing token, but only when `TURNSTILE_ENFORCE === "1"` — a Cloudflare env var no available instrument can read. **Customer.io closed the branch:** submissions dated well after the break (`contact-form` 09-12, `newsletter` 09-14, `podcast-guest-form` 09-15, `/book#book-cal` 09-08 and 09-11) could not have carried a token, so **the flag is unset and forms work.**
  - **THE HAZARD IS LATENT AND SITEWIDE. Setting `TURNSTILE_ENFORCE=1` while the CSP blocks the script takes every form down instantly and silently** — `verifyTurnstile` returns `turnstile_missing` and the submission is rejected before the honeypot and origin checks run. **And the lead-capture monitor would stay green:** `isMonitorProbe` lets the cron probe skip Turnstile whenever `FORM_MONITOR_SECRET` matches and the body email is the synthetic `monitor+run@wescalestartups.com`. An armed trap plus a monitor blind to it is the finding; the inert widget is only how it got there.
  - **DO NOT CLAIM THE LIST IS BEING SPAMMED BECAUSE TURNSTILE IS INERT. The evidence does not support it.** The junk-looking newsletter batch in Customer.io — unrelated consumer and industrial domains arriving in pairs at identical timestamps — is dated **2026-08-05 → 2026-08-06 and stops there**, i.e. it stopped when Turnstile was *added* despite Turnstile never running. Either it was a one-off list-bombing event or the guard's **honeypot, Origin, Referer and `source_page` host checks** are what hold the line; those all still execute.
  - **No-touch under FIXED RULES 1 (form handling) and 5 (`public/_headers` is Cloudflare Pages configuration — its own first line says so). Reported to Daniel, never routed around.**
- **A SECOND SCRIPT IS CSP-BLOCKED: `https://widget.intercom.io/widget/rkz1so24` (run 7).** Intercom has **no reference anywhere in the repo**, so it is GTM-injected, and it has therefore never loaded. **There is no chat widget on this site.** Tracking tags are no-touch; reported only.
- **RUN 6'S SHIPPED CHANGE IS VERIFIED IN LIVE PRODUCTION AND PASSES (run 7).** Pushed tree `42169ca`: `astro check` 0 errors / 0 warnings / 9 hints (125 files), `npm run build` **121 pages**, `check:lastmod` current, sitemap **110 entries** — page and sitemap counts unchanged. Live production, headless Chromium: sticky bar **189px at 375x812** and **153px at 1440x900** on `/` and `/pricing`, exactly the intended post-change figures; `position: fixed`, visible, **no horizontal overflow at 375px**, neither secondary label truncated. **Zero JavaScript exceptions.** Do not re-verify.
  - **Accuracy flag for the 2026-10-19 verdict.** `EXPERIMENTS.md` calls the lead element "the booking button"; **its rendered label is "Get in touch →"** (`siteConfig.bookingLabel`). Its `href` is `/book`, and `BaseLayout` fires `book_call` on any `/book` or Cal.com href (`if (isCalendly || isBookPath)`), so **the metric is correctly wired and the verdict is sound.** The bar's primary CTA simply does not say "book".
- **LEI ZHAO IS NO LONGER INVISIBLE TO GMAIL (run 7).** `Accepted: Growth Audit | Lei Zhao × We Scale Startups` arrived from `shaun.zhao@viiraa.com` on **2026-09-21 18:20Z** — between run 6's morning read and its evening addendum. **Corrected figures: 5 of 12 (42%) missing from the new-event notice · 3 of 12 (25%) with no notification of any kind · 0 of 12 invisible to Gmail.** Qualification unchanged: Viiraa is personalised glucose monitoring and coaching for individuals, the B2C consumer-health app already on the record.
- **SIXTEEN CONSECUTIVE UNQUALIFIED BOOKINGS (run 7).** Window 2026-08-25 → 2026-09-21: **12 bookings, ZERO qualified, website-sourced still 3.** No new Cal.com booking since Josefina on 09-16, confirmed on both query shapes. **Both window edges checked rather than assumed** — nothing booked on 08-24 (dropped out) or 09-21 (came in); the only Cal.com traffic in 08-24 → 08-26 is Revathy's 08-26 booking and the excluded `Cal Audit Test`. Two reschedules landed 09-21 and neither is a new booking: Revathy moved to 29 Sept (her third, reason given "We are going through a pivot"), and Shaun Zhao accepted Lei Zhao's 24 Sept slot.
- **`/contact` FORM VOLUME IS TINY AND LOW-QUALITY (run 7, Customer.io).** Two `contact-form` captures in the visible span: `sabya@newsicmail.org` (09-12) and `business@intelliglobalsummits.com` (09-05). Neither is ICP. Consistent with the settled reading that `/contact` is not where this funnel is won.

- **`/contact`'s "CTR gap" IS BRAND SITELINK STACKING. THERE IS NOTHING TO FIX (run 6, second look).** The page shows 139 impressions at position 6.8 with 1 click (0.7%) over 28 days, which reads as a broken title or meta description. **It is not.** Filtered to the page, GSC names its queries: **`we scale startups` 60 impressions at position 1.0 with ZERO clicks**, `wescalestartups` 6 at 1.0 with 1 click, `we scale startups company` 1 at 1.0. These are brand searches, and `/contact` is stacked as a secondary result beside the homepage, which takes `we scale startups` at **204 impressions and 12 clicks**. The searcher wants the homepage and clicks it. **`/contact`'s CTR is the arithmetic of ranking second for your own name — the click is going to the right page.** Its live title and meta are both present, specific and under the limits (`Contact Our Growth Team | We Scale Startups`; 113-char description). **Do not rewrite them for CTR. Do not hand this to seo-bot as an opportunity.**
  - **This was generated, flagged to Daniel as "the largest single gap I saw today", and withdrawn within the same session.** It is the **sixth** instance of the recorded pattern — brand sitelink stacking was already seo-bot's run 4 closed verdict, and "five false leads, five different tells" is a standing lesson. **A money page with high impressions, a good average position and near-zero CTR is a BRAND-STACKING SUSPECT FIRST. Pull its queries before calling it a defect.** The average position of 6.8 was the tell that should have stopped it: it is an average over anonymised long-tail rows, not the position on the queries that carry the impressions.


- **FIFTEEN CONSECUTIVE UNQUALIFIED BOOKINGS across every window measured, runs 1–6. The booking flow is not broken; it converts people who are not the ICP.** Run 1 (7), run 2 (8), run 3 (10 corrected), run 5 (12 corrected), run 6 (12, unchanged) — zero qualified in all of them. Invitees were out of ICP on their own stated facts: two `pre-pmf`, a consultancy, a consumer drinks brand, a VR franchisor who wrote **"We aren't SaaS"**, a B2C glucose-coaching app, a freemium founder-networking app, an incoming employee wanting personal guidance, an IT services firm via the Google mentor programme, and two referral partners Daniel approached himself. **Website-sourced has been 3 per 28 days through every correction** — Revathy Sivasankaran, Anthony Pollock, Bea Brampton. **As of run 6 this is no longer a defect to fix (see backlog 1): Daniel's answer is that these calls are part of the model.** Keep counting qualified and unqualified separately; stop treating the zero as an open problem.
- **THREE INVITEES LOOKED WEBSITE-SOURCED AND WERE NOT: Josefina (MentorCruise), Scott McKim (Daniel's 08-31 approach to Ferrock), Koa Browne (Daniel's 09-10 approach to Onfound).** Koa's Clarity session read `/` → `/pricing` → `/book/thanks` at 84.67% scroll and 1.5 min active — perfect ICP behaviour, and still outbound. **Site behaviour is not how you find out how someone got the link.**
- **THE DEVICE SPLIT, first measured (run 3, Clarity 28d, bot-excluded, 618 sessions): PC 87.22% (539) · Mobile 10.19% (63) · Tablet 2.59% (16).** **This is a desktop site.** The method rule "check the device split before assuming the desktop render is the one buyers see" has been checked: the desktop render **is** the one buyers see. Mobile is not where this funnel is won. Do not re-derive.
- **Zero JavaScript errors sitewide** (run 3, 28d: 0%, 0 total) and zero in the mobile `/book` segment. Settled; do not re-audit.
- **Core Web Vitals (run 3, Clarity field data).** Sitewide 80/100 from 33 pageviews — **LCP 3.1s needs improvement**, INP 230ms needs improvement, CLS 0.067 good. Mobile `/book*` **94/100 — LCP 0.9s, INP 120ms, CLS 0, all good**. **The money page is fast on mobile.**
- **THE SITEWIDE LCP IS THE HOMEPAGE, AND NOTHING PERMITTED FIXES IT (run 5, Clarity `URL performance`).** Per page, 28d: `/book/thanks` 94 / 0.9s · `/resources/growth-dependency` 94 / 0.7s · `/case-studies` 92 / 0.68s · `/proof` 92 / 1.6s · **`/book` 86 / 2.3s** · `/ai-growth-audit` 81 / 3.5s · **`/` 78 / LCP 3.1s / INP 350ms / CLS 0.1** · `/podcast-guest-application` 64 / 20s · `/fractional-cmo-vs-agency` 39 / 45s. **No money page is slow.** `/` is the top entry page, so it is the slow page that matters — but **every image on `/` already has `loading="lazy" decoding="async"` and explicit width/height, and there is no hero or eager image**, so the permitted intervention is already in place. What is left is font/CSS-driven and lives in `BaseLayout`, a shared component touching locked pages (FIXED RULE 6). **Closed. Do not re-open.**
- **`/pricing` has NO CWV row at all** — 46 pageviews, not one sample. Do not read its absence as a good score.
- **The 45s and 20s LCP rows are thin-sample artefacts, not defects** (run 5). `/fractional-cmo-vs-agency` has **no images on the page** and answers in 0.36s; `/podcast-guest-application` answers in 0.44s. The whole sitewide CWV sample is ~30 pageviews, so one stalled or backgrounded session owns a page's number. **Do not chase these.**
- **No friction constraint on this site — read FOUR times, thirty-five days apart end to end, and materially identical.** Run 5 (593 sessions): rage 0%, excessive scrolling 0%, dead clicks 3.88% (23), quick backs 3.20% (19); pages/session 1.25, scroll 31.59%, active 45s. Run 3 (618): rage 0%, excessive scrolling 0%, dead clicks 4.85% (30), quick backs 3.56% (22); 1.24, 31.98%, 47s. Run 1 (609): rage 0%, excessive scrolling 0%, dead clicks 5.25% (32), quick backs 3.78% (23); 1.25, 31.94%, 47s. Run 6 (31 sessions, last 3 days only): rage 0%, dead clicks 0%, excessive scrolling 0%, quick backs 6.45% (2 of 31 — two sessions on a tiny base, not a signal); 1.48, 26.47%, 21s. Zero JavaScript errors throughout. **Do not re-derive this, and do not treat "find the friction" as an open thread.** A single re-read of the top four numbers confirms nothing has changed.
- **No mobile render defect on `/pricing` or `/book`** (run 2, 375x812). Both render correctly. `/book` states the next step — **"Pick a time →"** — in the first screen without scrolling, with the Scorecard fallback beneath it. **The Cal.com embed lazy-loads and renders on mobile; an empty container before scroll is expected, not a fault.**
- **`/book` ALREADY carries qualification copy** — `BEFORE YOU BOOK — This call isn't for everyone`, four disqualifiers, honest free-vs-paid labelling, Scorecard fallback. What it lacks is the **positive** ICP (B2B SaaS and AI, Seed–Series B, £1m–£10m ARR). Run 1's opening hypothesis, "the booking page has no qualification", was **false**. **Read the rendered page before diagnosing it.**
- **The money pages filter on budget and symptom, never on company type or stage** (run 2). `/pricing` filters by budget in the first screen and by symptom in the self-routing block, and never asks whether the visitor is the kind of company WSS serves; the four proof cases (eQuoo, Nevly, LessonsUp, Ned) are not labelled B2B SaaS or AI. `/book` already carries *negative* qualification (`BEFORE YOU BOOK — This call isn't for everyone`, four disqualifiers, honest free-vs-paid labelling) but no positive ICP. **Recorded as an observation only. Backlog 1's answer means this is NOT to be fixed.** The durable lesson is the method one: run 1's opening hypothesis — "the booking page has no qualification" — was false. **Read the rendered page before diagnosing it.**
- **The sharpest lever in the funnel is no-touch.** The Cal.com `Stage` field offers `pre-pmf` and books it, against a page that says to hold off. The embed, its container and the Cal.com instance are off-limits. Report it; never route around it.
- **GA4 session totals are not a conversion denominator.** Direct was **792 of 996 sessions (79.5%) at 8s and 16.67% engaged**, against Organic Search 116 at 47s / 56.03%. Clarity excluded **305 bot sessions from 914** over the same days. Any "conversion rate ÷ sessions" built on the raw GA4 number is fiction.
- **`/book/thanks` is landed on directly** (15 landing sessions / 12 users vs 7 booking emails). Not a booking proxy.

- **Run 6 (2026-08-24 → 2026-09-20): no change. 12 bookings, ZERO qualified, website-sourced still 3.** The window moved one day and nothing entered or left it. **Confirmed null on two independent instruments:** the Cal.com/Calendly feed has no new-event notice after Josefina (09-16), and Clarity for the last 3 days shows **no `/book/thanks` pageview and no `Order success` smart event at all** ("No data available"). **Fifteen consecutive unqualified bookings.** All eight money pages returned 200 in 0.33–0.86s.
- **Non-Cal.com calendar meetings are NOT bookings and must not enter the count (run 6).** The subject-scoped query surfaces Google Calendar acceptances for meetings titled `Daniel Johnson + <name>` or `Daniel Johnson × <name>` — James Sedgemore (TML Partners, a marketing recruitment firm, 09-21), Phil Benson (Benson Finance, 09-25), Sagnik Halder (a Boardy.ai intro, cancelled). **The tell is the title: a Cal.com Growth Audit booking is always `Growth Audit | <name> × We Scale Startups`.** These are Daniel's own networking and partner meetings, out of ICP and outside the funnel.

- **THE PROGRAMME NOW HAS A MEASURABLE METRIC, AND IT IS NOT BOOKINGS (run 6).** GA4 event counts, 28d vs prior 28d: `session_start` 1,304 → **1,020 (−22%)**; `sticky_book_cta_shown` 326 → **357 (+9.5%)**; `book_call` 32 → **30**; `ai_referral` 14 → 17; `form_start` 11 → 8; `scroll` 211 → 169; `user_engagement` 589 → 379. **Sessions fell 22% while CTA impressions ROSE 9.5% — the traffic that left was traffic that never scrolled.** `book_call` per session went 2.45% → 2.94%; per CTA impression **9.8% → 8.4%**. At n=357 this is readable in one 28-day window; at 3 website bookings per 28 days nothing ever is. **Daniel's decision, run 6: `book_call` is the primary metric for judging shipped changes; qualified bookings stay the commercial scoreboard and are reported every run.**
- **THE CTA IS NOT UNDER-SERVED ON MONEY PAGES — THAT DIAGNOSIS WAS WRONG AND WAS WITHDRAWN INSIDE RUN 6.** Impressions per session_start: `/case-studies` 11/11 (100%) · `/about/daniel` 11/14 (79%) · `/pricing` 20/28 (71%) · `/services/acquisition-system-build` 6/9 (67%) · `/services/growth-diagnosis` 8/16 (50%) · `/proof` 6/13 (46%) · **`/` 177/443 (40%) — the WORST rate on the site**, because `/` absorbs the direct/bot traffic that never scrolls past 640px. Money pages get few impressions because they get few visitors, not because the bar is missing. **Do not re-derive this as a constraint.**
- **`book_call` BY PAGE (28d): `/` 11 · `/book` 10 · all other pages ≤5 each, 9 in total.** The bar is disabled on `/book` (`enabled={false}`), so its 10 events are in-page anchor CTAs, not the bar. **Sticky-bar click rate is therefore ~20/357 = 5.6%, not 8.4%. Quote the right denominator.**
- **THE `Order success` 24-vs-10 PUZZLE IS SOLVED (run 6).** GA4 shows `/book/thanks` with **26 page_views and 22 `session_start` events**. Cal.com's success redirect starts a NEW GA4 session, so a single booking generates a page_view *and* a session_start there, and reschedules re-fire both. **`/book/thanks` and `Order success` overcount bookings roughly 2:1. Neither is a booking count.** Closes the open item from run 3.
- **TRAFFIC QUALITY BY SOURCE (28d, GA4 engagedSessions ÷ sessions).** `LinkedIn / bio_link` 6/7 (86%) · **`chatgpt.com / ai-assistant` 12/17 (71%) — the best of any source with volume** · `clutch.co / referral_profile` 7/12 (58%) · `google / organic` 58/105 (55%) · `bing / organic` 1/11 (9%) · **`(direct) / (none)` 125/828 (15%)**. Direct is **828 of 1,020 sessions (81%)** at 15% engagement. **The real human denominator on this site is ~190 sessions per 28 days, not 1,020. Never build a conversion rate on the raw session total.**
- **GSC money-page position vs clicks (28d).** `/contact` **139 impressions at position 6.8 and 1 click (0.7% CTR)** — position 7 should return 5–8%. `/pricing` 41 impressions at 12.0, 2 clicks. `/book` 19 impressions at 4.9, **zero clicks**. `/services/acquisition-system-build` 71 impressions at **position 51**. Organic is overwhelmingly brand: `we scale startups` 12 clicks at position 1.2 of ~51 total site clicks. **`/contact`'s CTR gap is a title/meta question, which is seo-bot's and is held until 09-29 — handed off, not acted on.**

## Open, not established

- **`Order success` (Clarity smart event) fired 24 times sitewide against 10 bookings**; 8 of 16 sessions in the mobile `/book*` segment. Either it counts reschedules and revisits, or the booking count is still short. The `/book/thanks` `email=` list says 10 distinct invitees, so the smart event is the looser of the two — but this has not been pinned down. **Do not use `Order success` as a booking count.**
- ~~`/book` on mobile puts the calendar in a nested scroll box.~~ **RETIRED at run 3, on behaviour.** Clarity, 28d, Device = Mobile + Visited URL contains `/book`: 16 sessions, 13 unique users — **rage clicks 0 sessions, dead clicks 0 sessions, excessive scrolling 0 sessions**, quick backs 1; scroll depth 38.52% (*above* the 31.98% sitewide); **`Order success` 8 of 16 sessions**. Only 7 `/book` pageviews in the whole segment. The 698px box measured at run 2 is real and is **not stopping anyone**. It was no-touch regardless (FIXED RULE 1). **Do not re-derive; do not re-inspect the container.**
