# Growth-bot playbook

State for the scheduled Growth/CRO operator (daily, 07:00 London). **Files: this one is the working document — access notes, funnel model, locks, backlog, method. `FINDINGS.md` is the settled evidence. `EXPERIMENTS.md` is the lock register. `FUNNEL.md` is the weekly numbers. `changelog/` is per run.** Read this, then `FINDINGS.md`, then the last three changelogs. The SEO/GEO operator's state is in `seo-bot/`; locks are shared both ways. Prompt of record is the scheduled task "WSS Growth Operator (CRO, daily)".

## Access notes

- **AT RUN 8 THE WSS Search Analytics (Rankrat) CONNECTOR WAS NOT IN THE SESSION AT ALL — checked the way Trap 13 requires, not the way run 7 got it wrong.** `RefreshMcpTools` enumerated **30 servers and none of them was it** (a configured-but-unreachable server is named `not_connected`, and five were), and a `select:` `ToolSearch` on its exact tool names (`google_analytics_report`, `accounts_list`, `diagnostics`, `provider_readiness`, `clarity_insights`, `pagespeed_core_web_vitals`, `analytics_query`) returned **no matching deferred tools**. **This is a per-session provisioning difference, not evidence the service is broken, and it is not a blocker: GA4 answered through Chrome the same run.** Do not lead a report with it. Everything below remains the record of the connector when it IS present.
- **THE WSS Search Analytics (Rankrat) CONNECTOR IS HEALTHY, AND "IT IS ABSENT" HAS BEEN A FALSE READING AT LEAST ONCE (run 7, corrected same day).** Run 7 recorded it absent for the fourth time in seven runs. **That was wrong.** The server was **still connecting** when the run's first-minute check ran, and a bare `ToolSearch` for its tool names returned no match because the schemas had not loaded yet. Twenty minutes later it was present with 90+ tools, `accounts_list` answered on the first call, `diagnostics` returned `ready: true` / `configured_account_count: 2`, and `provider_readiness` for `google` returned `available: true`. **THE CHECK THAT IS VALID: call `RefreshMcpTools` (it names any server as `not_connected` or still connecting) and/or `ToolSearch`, which WAITS for connecting servers. A single early tool-name lookup that misses is NOT evidence of absence.** Runs 3, 4 and 5 recorded the same absence and cannot now be verified; **assume they may have been the same error** and never conclude absence again without the check above. Accounts: `google` (3 GSC sites, 3 GA4 properties, 1 PageSpeed site) and `bing` (1 site). GA4 property `259840282` re-verified at run 7 against run 6's table. **No `clarity` and no `cloudflare` account is configured** — `clarity_insights` returns `TOOL_REQUEST_REJECTED`, re-confirmed run 7 — so those two stay Chrome-only.
  - **`google_analytics_report` REJECTS TWO `date_ranges` IN ONE CALL** — `INVALID_RESPONSE: provider operation failed` (run 7). A single range answers fine. **Make two calls and compare them yourself; do not read the error as the property or the connector being broken.**
  - **Clarity and GA4 do not agree in absolute terms and should never be mixed in one rate.** Clarity 618 bot-excluded sessions (28d, run 3) against GA4's 996 raw at run 1, same period. Clarity excludes bots; GA4 as read does not. Say which instrument a number came from.
  - **Clarity is still Chrome-only.** The connector exposes `clarity_insights` but **no `clarity` account is configured** — the call is rejected. It is also capped at 1–3 days, so even once configured it will not replace the 28-day dashboard read.
  - **A Clarity "Your session has expired" card is often TRANSIENT (run 5).** Run 4 hit it three times and recorded Clarity as dark; **at run 5 the same Google round-trip to `daniel@wescalestartups.com` worked on the first attempt.** Sign in via **Sign in to Google → daniel@wescalestartups.com**, one attempt, before reporting Clarity unreachable. Never enter a password.
  - **THE `/book/thanks` URL CARRIES THE ENTIRE CAL.COM BOOKING FORM (run 5).** Turn **URL parameters ON** on the Top pages card and filter **Path → URL → contains → `isSuccessBookingPage=true`**. Each success URL then returns in full: `email`, `attendeeName`, `title`, **`description` (the invitee's free-text challenge)**, `startTime`/`endTime`, `uid`, `rescheduleUid`/`rescheduledBy`/`formerTime`, `isEmbed`. **This is the whole qualification payload, and it is the better route to qualification than email-by-email — but it is not complete either (it misses Jordy Hartendorp and Thabi Pulikkotil), so reconcile it against Gmail rather than trusting it alone.** Reusable segment: `clarity.microsoft.com/projects/view/wkannkoxst/dashboard?date=Last%2028%20days&URL=2%3B2%3BisSuccessBookingPage%3Dtrue`. Narrow to one invitee with a domain fragment in the same filter (`&URL=2%3B2%3Bonfound`) to get that session's full path and referrer.
  - **TRAP: the Path → URL autocomplete is a STATIC recent-URL list capped at 10, not a search.** Typing into it does not filter it; different query strings just reshuffle the same ten. Run 3 read that list, got 10, and recorded 10 as the count — it was the cap. **Apply the filter and read the Top pages card instead.**
  - **Clarity attributes Core Web Vitals per page: Performance overview → `URL performance` tab** (run 5). Sorted by score, one row per page with Score/LCP/INP/CLS. This is what closed the unattributed-LCP question.
  - **Clarity via Chrome answered everything at run 3.** Set the window with the URL parameter `?date=Last%2028%20days` — no need to drive the numeric control. Filters live under **Path → URL** (`contains` / `matches regex`), device under **User info → Device**; applying them writes them into the URL (`&deviceType=Mobile&URL=2%3B2%3B%2Fbook`), so a segment can be re-opened directly next run.
  - **Cloudflare is still unavailable.** No `cloudflare` account configured in the connector either.
  - **BOTH MCP BROWSERS CAN BE DARK AT ONCE, AND THERE IS A ROUTE ROUND IT FOR THE SITE (run 7).** At run 7 Claude in Chrome returned "Browser extension is not connected", the built-in browser returned "The device this session is bound to is not connected to the bridge", and the whole `mcp__remote-devices__*` server dropped mid-run. **Playwright and Chromium are pre-installed in the cloud container** (`/opt/pw-browsers`; `playwright` resolves only from `/home/claude/.npm-global/lib/node_modules/playwright/index.js`, is global rather than in the repo, and is CommonJS, so an ESM script needs `import pw from '<that path>'; const { chromium } = pw;`). It renders **live production** at any viewport, reads the console and measures the DOM, which covers the post-deploy verification the FIXED RULES require and is what found the 47-day CSP defect. **It cannot reach Google-authenticated dashboards**, so it replaces Chrome for the site and never for Clarity. **GA4 and GSC do not need a browser at all when the Rankrat connector is up.**
- **The built-in browser is not a substitute for Google-authenticated sources.** It keeps its own profile with **no Google session**: both `clarity.microsoft.com` and `analytics.google.com` bounce to `accounts.google.com/v3/signin/identifier`. Signing in needs a password, which is prohibited. **Do not spend a run trying to route around this** — record the gap and move on.
  - **Chrome is still required for Clarity and the GSC Generative AI report.** At run 2 the `claude-in-chrome` extension was not connected for the whole run and both were unreachable; it reconnected afterwards.
- **Mobile viewport: use the built-in browser, not Chrome.** `resize_window` with `preset: "mobile"` gives a real 375x812 viewport and a mobile user agent (verified via `window.innerWidth`). Chrome's `resize_window` did **not** change the screenshot frame at run 1. The built-in browser needs the desktop app online; it can drop mid-run.
- **GA4 property `259840282`, account `161039443`** — "We Scale Startups / wescalestartups.com". **Prefer the connector (above).** Chrome fallback URL shape: `https://analytics.google.com/analytics/web/?authuser=2#/a161039443p259840282/reports/explorer?params=_u..nav%3Dmaui%26_u.date00%3DYYYYMMDD%26_u.date01%3DYYYYMMDD&r=<report>`; `_r.explorerCard..rowsPerPage%3D100` for rows.
  - **THE ACCOUNT INDEX IS `authuser=2`, CHANGED AT OR BEFORE RUN 8 (verified run 8, property name asserted in the header).** `authuser=1` now lands on the GA provisioning page — "Welcome to Google Analytics", no properties — and `authuser=0` returns "Missing permissions" for this property. Both look like an outage and are not one. **Try `authuser=2` first; if it moves again, walk the index rather than recording GA4 as unreachable.**
  - **THE COMPARISON PARAMETERS `_u.date10`/`_u.date11` DO NOT APPLY, AND THEY FAIL SILENTLY AND CATASTROPHICALLY (run 8).** Passing them rendered the comparison period as **`22 Sept - 22 Sept 2026` — one day, 41 sessions** — against a 28-day primary range, producing `+2,346%` on every row. **Read the comparison period label before reading any % change, and prefer two single-range reads.** Two single-range reads of the same window returned byte-identical totals, so the single-range route is sound. Working report ids: `all-pages-and-screens`, `landing-page`, `top-events`, `lifecycle-traffic-acquisition-v2`; **`lifecycle-events` and `tech-details` bounce silently to Home.** **`?authuser=1` alone lands on the WRONG property** (it resolved to *Baboodle Universal - GA4*) — Daniel has access to dozens of GA4 accounts, so assert the property name in the header before reading a number.
- **Key event is `book_call`** — a **CTA click, not a booking**. It fires on `/` and `/book`. Related events: `sticky_book_cta_shown`, `ai_referral`. Never report `book_call` as bookings.
- **THE BOOKING FEED UNDER-COUNTS, AND THE RECORDED "42%" IS A PROPERTY OF ONE QUERY SHAPE (run 5, CORRECTED run 6).** The `A new event has been scheduled` notice missed **5 of 12** bookings in the run 5 window. Run 6 re-checked all five: **4 of 5 are recoverable from Gmail.** Tom McColllum has two `has been rescheduled` notices from `noreply@wescalestartups.com` (09-15) **in the very feed recorded as missing him**, plus a Google Calendar `Accepted:`; Thabi has `Accepted: Growth Audit | Thabi × We Scale Startups` from `calendar-notification@google.com` (09-16); Scott McKim and Koa Browne have full outbound threads from Daniel. (Lei Zhao had none until 2026-09-21; he does now.) Honest figures: **5 of 12 (42%) missing from the new-event notice · 3 of 12 (25%) with no notification of any kind · 1 of 12 (8%) invisible to Gmail.** **Never say "the feed drops 42% of bookings".** Sixteenth false fact, and the second this bot wrote about its own primary metric. Read reschedule and `Accepted:` subjects, not just new-event ones. **And the Clarity `/book/thanks` list misses others the feed catches** (Jordy Hartendorp, Thabi Pulikkotil). **Neither ledger alone is the count.**
- **Clarity's `/book/thanks` URL list is the second booking ledger.** Cal.com's success redirect is `…/book/thanks?…&isSuccessBookingPage=true&email=<invitee>&eventTypeSlug=20min`. Clarity's **Path → URL** autocomplete lists every distinct one it recorded, so the set of `email=` values is a per-booking ledger — the only instrument that caught Scott McKim. **Pageviews of `/book/thanks` over-count (28 views against 10 bookings); the distinct `email=` values do not.**
- **Reaching `/book/thanks` is NOT evidence of a website-sourced booking.** Run 1 recorded it as "landed on directly — not a booking proxy"; both halves were wrong. It is the post-booking redirect, and **9 of 16 mobile `/book*` sessions arrived from `cal.wescalestartups.com`** — people who book the direct Cal.com link never touch the site's `/book` page but do land on `/book/thanks` after.
- **THE BOOKING QUERY WAS SENDER-SCOPED AND THEREFORE STRUCTURALLY BLIND. USE A SUBJECT-SCOPED QUERY (run 5 addendum).** Google Calendar acceptance notices arrive **From: the invitee's own address**, not from `calendar-notification@google.com` (Revathy, Bea, Anthony Pollock, Dominic Monn and, at run 8, **Mahmod Mahamed** all sent one). A sender list cannot enumerate those. **Run BOTH of these every run and reconcile:**
  1. `subject:("Growth Audit" OR "has been scheduled" OR "has been rescheduled" OR Accepted OR Declined OR Canceled OR Cancelled) newer_than:35d`
  2. `(from:noreply@wescalestartups.com OR from:notifications@calendly.com OR from:calendar-notification@google.com) newer_than:35d`
  Query 1 finds what query 2 misses. **This reduces — but does not remove — the need for the work-calendar connector: Koa Browne has no email trace under EITHER query, confirmed against a wide net. Lei Zhao did not either until his acceptance landed on 2026-09-21.**
  - **TWO THINGS THE WIDER QUERY SURFACES THAT MUST NOT BE COUNTED.** **`Growth Audit | Cal Audit Test × We Scale Startups`** (2026-08-26, from `admindjohnson@gmail.com`) is **Daniel's own test booking** — it sits inside the window and would inflate the count; exclude every `Cal Audit Test` event. And **calendar invitations Daniel raises by hand are not funnel bookings**: the Dominic Monn podcast interview (declined 09-01 for Tue 1 Sept, re-invited, accepted for Fri 4 Sept) was arranged in the `Re: Podcast?` email thread, not through the booking page. **Count only bookings made through the Cal.com booking flow.** Neither changes the run 5 count of 12.
- Booking notification feed: Gmail, `from:noreply@wescalestartups.com` (self-hosted Cal.com at cal.wescalestartups.com, subjects `Growth Audit | <name> × We Scale Startups`). Legacy Calendly from `notifications@calendly.com` for old event types. Count by invitee + final event time. **Gmail always answers — and what it answers with is incomplete. Reachability is not completeness; see the under-count note below.**
  - **The Cal.com booking form captures `Company`, `Stage`, constraint and free-text challenge.** This is primary-source qualification evidence and beats a CRM lookup. Legacy Calendly notifications carry none of it — qualification from those is low confidence.
- Clarity: project `wkannkoxst` at `clarity.microsoft.com/projects/view/wkannkoxst/dashboard`. Sign in via **Sign in to Google → daniel@wescalestartups.com** (already authorised in Chrome, no consent screen). Date control is a numeric "Last _N_ days" field, not a preset list.
- **PageSpeed is exhausted by both routes, and no longer matters.** Keyless curl returns 429 (shared Google project `583797351490`); the connector's `pagespeed_core_web_vitals` returns `RATE_LIMITED`. Re-tested at run 2. **Do not re-test either route.** **Clarity's Performance overview card carries field Core Web Vitals per segment and needs no key** (run 3) — LCP, INP, CLS and a 0–100 score, filterable by page and device. A PageSpeed key would add lab data only; the request to Daniel is withdrawn.
- **Cloudflare connector is unauthenticated and unavailable.** Bot share comes from Clarity's own exclusion count only.
- **The Google Calendar connector is on the WRONG ACCOUNT.** Its primary calendar is `admindjohnson@gmail.com`; `list_calendars` returns only personal calendars (Family, TripIt, Facebook events, UK holidays). The `daniel@wescalestartups.com` work calendar, where Cal.com writes the bookings, is **not reachable**. That calendar would be a better source of truth than the email feed — see the booking under-count below. **Do not retry it; it is an authorisation matter for Daniel.**

## Funnel model

Organic / AI / referral / direct → landing page → money page (`/pricing`, `/services/*`, `/contact`) → `/book` (Cal.com embed) → booking → qualified?

**Revised at run 1, corrected at run 2: a majority of bookings never enter at the top.** Referrals, MentorCruise, the podcast and Daniel's own outbound all land straight on the Cal.com link — **five of eight** in the run 2 window. **Separate website-sourced bookings from the rest before computing any conversion rate. The website denominator is 3 per 28 days — not 7, 8 or 10.** Corrected at run 3: the window holds **10 bookings, 7 of them non-website** (two MentorCruise, one Google mentor programme, one podcast, one referral, **two of Daniel's own outbound**). Both bookings recovered at run 3 were Daniel's own outbound, so the website denominator did not move.

- **Mentoring platforms are a recurring structural source of non-ICP bookings, and there are at least three.** MentorCruise, the Google "Mentors Development Program EMEA 2026" (Thabi Pulikkotil, via `smateusz@xwf.google.com`) and `growthmentor.com`. All send people seeking personal guidance rather than a company engagement. **No site change affects any of them. Count them separately every run.**

## Settled findings

**Moved to `growth-bot/FINDINGS.md` at run 6.** Read it every run, after this file. It holds the booking history, the behavioural and performance baselines, the traffic-quality and event-funnel numbers, and the closed questions. **Nothing in it should be re-derived.** The headline it all reduces to: **the booking flow is not broken; the site converts people who are not the ICP, and as of run 6 Daniel's answer is that this is part of the model, not a defect.**

## Locks inherited from seo-bot (read `seo-bot/VERDICTS.md` every run)

Open verdict windows: `/insights/what-is-a-fractional-cmo` (09-16), `/insights/how-to-run-a-90-day-growth-sprint` (09-17), `/facts/we-scale-startups` (09-22), the three pillars + `/insights` hub + orphans (09-23), `/insights/what-226-founder-reviews-reveal` (09-24), `/services/fractional-cmo` consolidation (10-03).

**The 2026-09-29 row names no page.** It marks the first genuinely independent money-page *click* window, not an experiment on a page. Working interpretation, **flagged to Daniel and not yet confirmed**: it does not lock money pages for conversion work, but **no title or meta-description change on a money page before 09-29**, because money-page clicks is the series under observation. Body copy, CTA placement and proof placement remain available.

Run 2 note: the 09-17 row judges whether the new insight URL takes queries **off `/services/90-day-growth-sprint`**, so that service page is entangled in the window. Treat it as locked until the row is judged.

## Ranked backlog

1. ~~Blocked on Daniel — are non-ICP booked calls a problem or part of the model?~~ **ANSWERED BY DANIEL IN CHAT, 2026-09-21: "More calls, don't filter."** Mentoring, podcast and referral bookings are part of the model, not a leak to be plugged. **Consequence: the programme optimises for booking-CTA volume, NOT for ICP filtering.** Do not ship positive-ICP qualification copy to `/pricing` or `/book`; do not treat a rise in unqualified bookings as a warning. The zero-qualified run of fifteen is context for Daniel's commercial judgement, not a defect this bot fixes. **Open since run 1; closed at run 6. Do not re-open it without Daniel.**
2–5. **CLOSED.** Sitewide 3.1s LCP attributed to `/` and no permitted fix remains (run 5, backlog 2). Behavioural baseline closed — device split 87/10/3, sitewide read, mobile `/book` segment, zero JS errors, CWV (run 3, backlog 3). Money-page mobile CWV unblocked via Clarity, `/book*` 94/100 (run 3, backlog 4). Mobile viewport route solved — built-in browser, `resize_window` `preset: "mobile"` (run 2, backlog 5); deprioritised by the device split. **There is no shippable work that is not either locked until 09-29 or blocked on backlog 1.**
6. ~~State the positive ICP where the decision is made.~~ **RETIRED at run 6.** It was conditional on backlog 1 returning "yes, filter". It returned "no". **Deleted rather than parked — do not resurrect it from an old changelog.**
7. **BLOCKED ON DANIEL, opened run 7 — `challenges.cloudflare.com` is missing from the CSP, so Cloudflare Turnstile has never executed since it was added on 2026-08-06.** Five surfaces carry it, including the sitewide footer newsletter. **Not a conversion defect today** (`TURNSTILE_ENFORCE` is unset, proven by post-08-06 submissions landing in Customer.io) but **an armed trap**: setting that flag while the CSP blocks the script takes every form down silently, and `isMonitorProbe` lets the lead-capture cron skip Turnstile so the monitor would stay green. No-touch under FIXED RULES 1 and 5. See `FINDINGS.md`.
8. **After 2026-10-19 only — the sticky bar's primary CTA reads "Get in touch →", not a booking phrase** (`siteConfig.bookingLabel`). Its `href` is `/book` and `book_call` fires correctly, so the open verdict is sound. Whether a bar whose job is booking should say "book" is a separate, testable question. **Locked until the verdict; do not touch the component.**
9. **Nothing is shippable before 2026-09-29, and little before 2026-10-19 (re-confirmed run 8).** The open sticky-CTA row is judged on **sitewide** `book_call` ÷ `sticky_book_cta_shown`, so any change that adds, moves or relabels a booking CTA anywhere fails gate 6; money-page titles and meta are held to 09-29. **Plan these runs as measurement, not shipping, and do not manufacture a change to fill them.**
10. **Judge the run 6 sticky-CTA change on 2026-10-19** (`EXPERIMENTS.md`). Baseline `book_call` 30 clicks / 357 impressions. Then, if it passes, the next candidate is the same hierarchy question on the in-page CTAs — `/book` fires 10 `book_call` from anchors and is the only page where the bar is disabled.

## Proposed to Daniel

- **Add `https://challenges.cloudflare.com` to `script-src` and `frame-src` in `public/_headers`, or retire Turnstile properly.** Until then **do not set `TURNSTILE_ENFORCE=1`** — it would take every form on the site down silently while the monitor stayed green. Opened run 7; no-touch for this bot.
- **Is Intercom meant to be live?** `widget.intercom.io` is CSP-blocked and absent from the repo, so it is a GTM-injected widget that has never rendered (run 7).
- **Authorise the `daniel@wescalestartups.com` work calendar on the Google Calendar connector.** It is currently on `admindjohnson@gmail.com` and sees only personal calendars. **Still the highest-value access fix, on figures re-measured at run 8:** the `A new event has been scheduled` notice misses **6 of 14** bookings. Mahmod Mahamed (2026-09-22) is the newest miss — he left **no Cal.com notice of any kind**, only his own Google Calendar acceptance, so a sender-scoped read of the Cal.com feed cannot see him at all. Today the primary metric is assembled by hand from three Gmail query shapes plus a Clarity ledger, each incomplete. The work calendar would make every booking visible on one instrument.
- **Give the scheduled session a durable repo clone route.** It worked at run 5 and did not at run 4. Without one, `PLAYBOOK.md` and the `seo-bot` handoff cannot be maintained safely and the programme's state drifts from its changelogs.
- **Keep Chrome running with the Claude extension when the daily run fires, and configure `clarity` + `cloudflare` on the Rankrat connector.** Chrome is the only route to **Clarity** and the **GSC Generative-AI report**; it was dark at run 7 and those two were lost. **GA4 and classic GSC were never lost at run 7 — that was the false-absence error, not a browser problem.** A `clarity` account on the connector would take the behavioural read off Chrome's critical path entirely.
- Authorise the Cloudflare connector (bot share is currently inferred from Clarity alone).
- ~~Confirm the 09-29 money-page interpretation.~~ **SETTLED at run 6, operating reading: body copy, CTA placement and proof placement are available now; no title or meta-description change on a money page before 2026-09-29.** The seo-bot window measures money-page clicks *from search*; on-page body and CTA changes cannot move that series. **This reasoning is what licenses shipping.**

## Method notes

- **CLARITY'S "ZERO JAVASCRIPT ERRORS" DOES NOT COVER CSP SCRIPT REFUSALS (run 7).** That metric read 0% across four separate readings while two third-party scripts were refused on every page for 47 days. A blocked script is a console error, not a thrown exception. **"Zero JS errors" is not "nothing is broken in the browser" — read the console directly.**
- **A NO-TOUCH AREA STILL HAS TO BE LOOKED AT (run 7).** Forms, CSP and tracking tags being unshippable is why a live defect went 47 days unseen: the programme stopped inspecting what it had ruled out changing. **No-touch means do not edit it. It does not mean do not read it.**
- **CLOSE A TWO-BRANCH UNKNOWN ON DATA BEFORE REPORTING BOTH BRANCHES (run 7).** Whether the blocked Turnstile killed form submissions turned on a Cloudflare env var this session cannot read. Rather than hand Daniel "either your forms are fine or they are all dead", **Customer.io settled it**: submissions dated after the break prove the enforcement flag is off. One read replaced a hedge.
- **A shared-component experiment locks a whole class of work, not just its own file.** The open sticky-CTA row is judged on **sitewide** `book_call` ÷ `sticky_book_cta_shown`. Any change that adds, moves or relabels a booking CTA anywhere moves that numerator, so "add a CTA to a money page" fails gate 6 until 2026-10-19 regardless of which page it is.

- **Check which GA4 property you are reading. Twice.** Run 1 spent its first reads on another company's site and the numbers looked healthy.
- **Qualify from the invitee's own form answers, not from the domain.** "We aren't SaaS" in a notes field settles a case a domain lookup would have got wrong.
- **A booking is not a website conversion until you know how the invitee got the link — and SITE BEHAVIOUR IS NOT HOW YOU FIND OUT.** Koa Browne's session read `/` → `/pricing` → `/book/thanks` at 84.67% scroll depth and was still Daniel's own outbound. Josefina looked website-sourced until the MentorCruise thread turned up; Scott McKim until Daniel's own 08-31 approach to Ferrock turned up. **Check Gmail and the Notion Sales CRM before counting one, however good the session looks.**
- **RECONCILE TWO BOOKING LEDGERS, NEVER ONE.** The Cal.com email feed and the Clarity `/book/thanks` `email=` list disagree, and the email feed is the one that loses bookings. Run 3 found three of ten missing from it. **The moment a source is described in this playbook as reliable, that is the source to check against a second one.** Fifteenth false fact, and the first this bot wrote about its own primary metric.
- **A same-day second run does not get a new window.** Run 3 fired twelve hours after run 2, so "the last complete 28 days" was byte-for-byte the same window. **Check the previous changelog's date before re-measuring, and if the window has not moved, say so and spend the run on instruments, corrections and unanswered questions instead of re-reading the same numbers.**
- **A window that moves forward by a day is not a new measurement.** Run 2's 7 → 8 is one addition, not growth. Say so, or the next run reads a trend into it.
- **An empty container is not a broken embed.** The Cal.com widget renders nothing until scrolled near. Inspect the DOM before calling a blank screen a defect.
- **Diagnose on desktop on this site.** The general rule is mobile-first; the device split says PC 87.22%. Check a mobile render when a mobile defect is suspected, not as the default lens.
- **Programmatic scroll does not move this site.** `window.scrollTo` and `Element.scrollIntoView` leave `window.scrollY` at 0 — a smooth-scroll library intercepts them. Only real wheel events via the `computer` scroll action move the page, so only they fire a lazy-load. **Scroll with `computer`, never with injected JS, when testing lazy-loaded elements.** Two false "the embed failed to load" readings at run 2 came from this.
- **`npm run build` DIRTIES THE WORKING TREE; never commit what it writes (run 5).** `scripts/optimise-raster-images.mjs` runs inside `build` and writes `.webp` derivatives next to their sources in `public/`. They are build output, not a change — the same script runs on Cloudflare Pages at deploy, so the live site already serves them. **Delete them and leave the tree clean.** **FIXED at Daniel's instruction (run 5 addendum):** the known paths are now in `.gitignore`, with a comment saying to add a line per new large raster source and never to list a hand-authored `.webp`, which is a source and stays tracked.

## Traps (growth-bot's own; the numbered list in the prompt is the prompt's)

12. **Both MCP browsers can be offline in the same run.** Use headless Chromium in the cloud container for anything about the live site (see the access note above); record analytics as unavailable rather than hunting for a route to Google-authenticated dashboards, which there is not one of.
13. **A connector can still be CONNECTING when the run starts, and a tool-name `ToolSearch` that misses looks exactly like absence.** Run 7 wrote a false fact about its own primary instrument this way. `RefreshMcpTools` names a server as `not_connected` or still connecting; `ToolSearch` waits for connecting servers. **Use one of those before recording any connector as unavailable.**
14. **A GA4 URL that silently ignores a parameter produces a confident, enormous, wrong number (run 8).** The comparison-date params were dropped and GA4 compared 28 days against a single day, reporting `+2,346%` on sessions. Nothing in the table said the parameter had failed — the period label did. **Read the period label, not the percentage.**
15. **A Google account index is not stable (run 8).** `authuser=1` had been the GA4 route for seven runs; at run 8 it served the provisioning page and `authuser=0` served "Missing permissions". Neither is an outage. **Walk the index before recording a Google-authenticated source as unreachable.**
