# SEO/GEO/AEO bot — playbook

Updated each run. **Read this, then the changelog, at run start.** Changelog lives in two places: `seo-bot/CHANGELOG.md` holds runs 1–8 and is **frozen**; every run from 9 onward writes `seo-bot/changelog/YYYY-MM-DD-runN.md`. Read the most recent two or three (Trap 4 explains why they are split). **Runs 10–14 were one long interactive session with Daniel** — 10 sitewide audit, 11 entity/schema, 12 off-site profiles, 13 orphans, 14 diagnostic-tool corrections. **Read 14 first**; it carries corrections to run 13's own conclusions.

## Current understanding (2026-08-26)

- Site: **117 pages built, 11 noindexed, 106 in the sitemap** (2026-08-26), Astro static, push to main = deploy (Cloudflare Pages). Deploys land in **under 2 minutes**.
- **SETTLED — the May 2026 cliff and its recovery.** 60% of organic impressions lost on 6 May 2026 (the WordPress-to-Astro rebuild); no run noticed for seven. Nine legacy 404s redirected, the VC and GTM guides restored. **Recovery is complete — do not re-diagnose it.** Detail in `changelog/2026-08-26-run10-audit.md` and `-run13-orphans.md`. **The rebuild date still matters for one thing: third-party pages researched around 7 May 2026 describe the old site (backlog 13).**
- **The site-wide average position "decline" is composition, not decay.** Queries present in both windows *improved* (median −2.25 places); high-position legacy pages left the index and were replaced by new pages at 24–64. **Do not read the site-wide position number as a ranking signal.**
- **Brand queries grew across the rebuild; 100% of the named-query loss was commercial and long-tail informational** — a working long-form content footprint traded for a better-structured brand site.
- **Never analyse a single window in isolation.** Runs 1–7 all did, which is why a 60% collapse stayed invisible while the playbook attributed flat performance to "authority". Always compare against the preceding window, and pull `dimensions: "date"` when anything looks structural.
- **GSC 90d baseline (2026-05-25 → 2026-08-22): 42 clicks / 9,500 impressions / 0.44% CTR.** Use 90d windows. 819 query×page rows total, so the full dataset is small enough to pull whole.
- **THE "ZERO NON-BRAND CLICKS" HEADLINE IS A STATEMENT ABOUT THE NAMED-QUERY TABLE, NOT ABOUT THE SITE. Established run 16.** Across three windows only **three queries site-wide have ever received a click** — `we scale startups`, `we scale`, `wescalestartups` — so most clicks come from queries Google anonymised, and the non-homepage URLs taking them appear nowhere in the named-query data. 1–28 Jul: 36 clicks, 21 anonymised, 12 non-homepage. 29 Jul–25 Aug: 72, 50, 21. 2–29 Aug: 67, 40, 20. **Money-page clicks: 2 → 3 → 3.** Full per-URL breakdown in `changelog/2026-08-27-run16.md`.
  - **Read every run: total clicks minus named clicks, and non-homepage clicks.** Both roughly doubled while the named table went backwards on non-brand. That contradiction is the most important thing in the current data.
  - **Anonymised proves *rare*, not *non-brand*.** Strong evidence, not proof; the honest instrument is conversion data.
  - **Run 18, window 2–29 Aug (the first advance since run 16): 67 clicks / 6.15k impr / 1.1% / pos 24.6**, against 72 / 5.46k / 1.3% / 26.7. Position **+2.1 places**; clicks, non-homepage clicks (20 vs 21) and money-page clicks (3 vs 3) all flat-to-down. Named clicks 27, **anonymised 40 = 60%** (was 69%). **Still zero non-brand named clicks after nineteen runs.** And the impression "growth" is not real — see the `scale startup` collision below.
- **AI PANEL — three readings. Google AI Mode 2/10 named and cited, stable across all three. Perplexity 0/10 organic, and the earlier "1/10" was an artefact.** Google cites `/services/90-day-growth-sprint` (prompt 3) and `/facts/we-scale-startups` (prompt 5), accurately, quoting £8k–£12k, "6–8 ICE-scored experiments", "12 weeks" and "post-PMF" — **both from pages built to be extracted.** Reading #3 added `/insights/how-to-run-a-90-day-growth-sprint` (cited four days after publication) and `/insights`. **THE PERPLEXITY SERIES IS CONTAMINATED: the panel runs on Daniel's signed-in Perplexity account, and reading #3 was explicitly labelled "Given your work with We Scale Startups". Reading #2's "first citation" was almost certainly the same thing unlabelled. Score any personalised mention as non-scoring.** A logged-out control is unavailable — Perplexity refuses logged-out queries, the built-in browser is blocked from google.com, and creating an account is prohibited. Full tables in `AI-VISIBILITY.md`.
- **The site was improving through August, and run 18 is the first flat window.** 14-day windows to 24 Aug: clicks 18 → 36 → 35, CTR 0.75% → 1.24% → 1.37%. Then 28d to 29 Aug: clicks 72 → 67, CTR 1.3% → 1.27% ex-collision, position 26.7 → **24.6**. Position keeps improving; clicks have stopped. **Read clicks and CTR, not impressions.**
- **`page` and `page,query` will disagree, and the gap is the finding.** A page whose named-query rows do not reconcile to its page-level total is being found by long, rare, conversational prompts — the AI-visibility signature. `/insights/b2b-saas-gtm-strategy` was at 100%: 65 impressions under `page`, zero rows under `page,query`. **Pull both and diff them deliberately.**
- **Bing: 29 referring domains / 41 referring pages / 16 anchor texts** (UI figure, 2026-08-25). The "Bing reports zero inbound links" claim in runs 2–5 was a broken endpoint mistaken for a finding — never repeat it. 29 domains is low for these head terms; the authority thesis stands.
- **The entity is contested, and that is evidenced.** Bing's second-largest query is `wescale login` (106 imp) — the wescale.com e-procurement platform; login variants are ~25% of apparent brand traffic. Two further WeScale businesses and **seven Daniel Johnson LinkedIn profiles** compete. Run 18 adds the largest instance: **`scale startup`, 860 impressions in 28 days, is Scale AI.** Off-site entity work is evidenced, not best practice.
- **Agents, not just humans, are doing vendor research here.** Bing shows verbatim operator strings and a truncated system prompt among its queries; ~23% of Bing impressions are conversational or machine-issued. They extract **fit criteria, price band and contact** — keep those stated plainly on `/facts/*` and every service page.
- **Bing is a diagnostic mirror, not a channel.** 3,310 impressions / 30 clicks over 16 months; two genuine non-brand commercial clicks. **Never spend a run optimising for it**; do read it when Google's data is ambiguous — it exposed both findings above.
- **The constraint is authority — but a large part of it was self-inflicted.** Technical foundation is good; commercial head terms sit at position 40–72 on 29 referring domains. Do not repeat "nothing points at this site" as the whole explanation without accounting for the May 2026 cliff.
- **Where this site ranks, it ranks for questions, not keywords** — the most useful idea in this file. `/services/90-day-growth-sprint` holds **1.0–5.6** on provider-selection prompts; `/insights/when-to-hire-fractional-cmo` holds **~8.0** on hiring-decision prompts; `/services/fractional-cmo` sits at **~46–52 across 1,600+ impressions** of head terms. **Head terms are authority-bound; question-shaped queries are not.**
  - **Working rule:** find the queries phrased as questions or decisions, check which page Google already chose, and make that page answer it in one extractable passage. Do not fight for the head term.
  - **Corollary (run 9):** the rule assumes a page exists to carry the answer. Where the question has *no* page behind it, the constraint is **absence**, not authority, and writing the page is the cheapest win available.
- **"Don't optimise titles/meta at position 45+" implies its inverse:** at position 1–10 the snippet and the first extractable passage are the whole lever.
- **Profile-page impressions are largely brand sitelink artefacts. Do not chase them.** `/proof`, `/press`, `/about/daniel`, `/start-here`, `/podcast`, `/speaking` are mostly `we scale startups` at position 1.0 with zero clicks — Google logs one impression per sitelink URL at the parent's position and the homepage absorbs the clicks. **The tell is the same query at the same position across many pages; check the query mix before believing a page-level position.** Not a licence to ignore real clicks: `/about/daniel` took 8 (run 16) and 6 (run 18) from anonymised queries.
- **Brand-name collision is the largest single distortion in this data.** `scale startup` **860 impressions at 10.3, zero clicks — that is Scale AI** (run 18, SERP-verified), plus `we scale ab`, `calconnect login`, `google for startups in shoreditch`. **Discount from every impression and CTR figure.**
- **The site has AI visibility it does not have classic search visibility**, and this is now measured rather than inferred — see the Generative AI report bullet. Optimise for passage extraction: a direct-answer paragraph under a heading, named facts, prices, durations, comparison tables. Not for titles the user may never see.
- Entity graph roots on danieljohnson.xyz for Person, wescalestartups.com for Organization. `/facts/*` carry `lastUpdated` — keep monthly-fresh.
- Six `ai:*` meta tags ship on all pages; no crawler consumes those names. Harmless, inert, **do not expand**. `meta-ai:summary` derives from the page description, so improving a description improves it for free.
- `site.ts` is the single source of truth for pricing/proof/FAQ copy; page copy MUST match `pricingTiers`. The **numeric** ranges for structured data live in `src/lastmod.ts` (`servicePriceRanges`). `/insights/fractional-cmo-cost-uk` is the canonical place for the agency (£6k–£20k/mo) and full-time (£120k–£180k base) comparison bands — reuse those, do not invent new ones.
- **robots.txt says `ai-train=yes` and always has since run 2.** Run 4 invented a "Daniel reverted it" story and runs 5–7 repeated it unchecked; `git log src/pages/robots.txt.ts` shows the last commit is run 2's `423d8ea`. The current state is coherent — GPTBot/ClaudeBot are `Allow`ed and the signal says yes. **Leave it alone because it is correct, not because of a decision that never happened.**
- Daniel works in the repo directly, and so does this bot several times a day. **Check `list_commits` before choosing a file**; never touch a template another run edited hours ago.

## Connector and environment notes

- **The sandbox can clone, install and build the repo.** `git clone` + `npm ci` (~22s) + `npm run build` (~3s after OG generation) all work; the GitHub connector pushes. **Verify every change this way before committing** — build, `astro check`, JSON-LD parsing, rendered HTML and internal-link targets, all before the commit exists.
  - **Traps 1–8 are stated in full in the task file's FIXED RULES. Do not restate them here.** The three the file abbreviates and that cost real time: **Trap 2** — a shallow clone makes `refresh:lastmod` rewrite all 78 routes; `git fetch --unshallow` first, and CI needs `fetch-depth: 0`. **Trap 5** — `check:lastmod` resolves each route to the last *git commit* touching its backing file, so it is structurally incapable of failing before the content commit exists; a green pre-push result means nothing. **Trap 8** — run 15 got three empty `web_fetch` bodies for a live page while `/sitemap.xml` on the same host worked; the Chrome connector returned the full article. Never revert on an empty `web_fetch`.
- **`staticPathLastModified` covers 82 routes including SOME `/insights/*`** — the four pillars (`what-is-a-fractional-cmo`, `b2b-saas-gtm-strategy`, `ai-native-gtm`, `startup-growth-bottlenecks`) plus `/insights` and `/insights/glossary`. Every other insight falls back to `siteConfig.siteLastModified`. **So the sitemap two-step DOES apply to the pillars**, and to static pages and service/case content. Runs 5–8 recorded the opposite and run 9 left CI red trusting it. **Do not guess: `grep` the route in `src/lastmod.ts`.**
- **GSC MCP `row_limit` defaults to 100 and silently truncates.** Rows return clicks-desc, then zero-click rows **alphabetically by (page, query)** — a low limit guillotines everything after roughly `/f…`. **Always pass `row_limit: 25000`**, which returns the whole dataset in one call.
- Large GSC pulls exceed the tool output limit. Run them in a **subagent** with an explicit list of the figures you want back, so the dump stays out of main context.
- 60–89% of a page's impressions — and **68% of the site's clicks** — are **anonymised** by Google (rare, personally-phrased queries). Some pages are at 100%. That gap is itself a signal and it skews heavily conversational.
- **GSC MCP: recrawl has always come via the sitemap, and the old connector had no indexing endpoint. The v2.1.2 extension exposes `indexing_submit`, `indexing_status` and `genai_query_insights` — UNTESTED. Call them and read what comes back rather than assuming either way** (prohibition 18 still applies: do not spam indexing APIs or exhaust quotas). The **UI** still has more than the connector — see the Generative AI bullet.
- **A connector reporting "unavailable" is a symptom, not a diagnosis. Run the server and read its stderr before writing down a cause.** Runs 17 and 18 both carried "Daniel must re-authorise Search Console" as fact without once executing the binary; the real fault was a missing dependency tree and it took four commands to find. **Extension `node_modules` are wiped by any reinstall or update** — the same trap as the Bing extension's vendored date patch. If either connector dies again, check for `node_modules` first.
- **Bing MCP date endpoints were patched 2026-08-25** (`parse_timestamp_from_api` returned a naive datetime; fixed with `tz=timezone.utc` in the vendored lib). **A reinstall wipes the patch.** Backup: `utils.py.bak`.
- **Bing quirks:** query stats are weekly buckets dated to Fridays covering ~36% of impressions — never compare to GSC dailies. Backlinks must be read from the UI (`GetLinkCounts` returns empty regardless). Tools need `self="bing"`. **Quota is 100/day and 100/month** (checked run 18 — the "600/month" recorded earlier was wrong); `submit_url` returns `null` on success. Crawl-issue data is **historical** and lists URLs fixed weeks ago — re-check live before acting. Bing's site URL is `https://wescalestartups.com`; passing `sc-domain:` returns `InvalidUrl`.
- Cloudflare MCP has no Pages-deployment tools; verify deploys by fetching the live URL with a cache-busting query string.
- **curl works fine against the site** for status codes and redirect chains, despite the SKILL note about bot challenges. **Trust curl status codes over `web_fetch` bodies when checking redirects.**

## Backlog

**Numbers are stable IDs, not priority.** They are referenced from the changelog and must never be reassigned. Order within each list is the ranking.

### Open

13. **OFF-SITE, TOP OF THE QUEUE — the Growth Division listicle is wrong about WSS, and it is the retrieval path.** `growth-division.com/blog/best-startup-growth-agencies-in-the-uk` has been in Google AI Mode's sources on panel prompt 9 in **all three readings**, and in run 18 it fed WSS into Perplexity's *answer body* with `wescalestartups.com` absent from the source panel entirely. **Wrong in five ways** — three tiers (four), "pricing not disclosed" (all four bands published), pre-seed to Series A (post-PMF, Seed to Series B), "primary channels Meta Ads, Google Ads, SEO", "London-based agency" — plus a **"+768% growth rate"** and **"£20M in revenue"** attributed to WSS that appear nowhere in the repo, and University of Cambridge listed as a client. **Run 17's unexplained Google mis-categorisation ("Paid Ads & CRO Sprints") traces to this page.** Published 7 May 2026, the day after the rebuild. **The site cannot fix it** — `/facts/we-scale-startups` already says all of it correctly and Google cites that page. Correction drafted as **C-1 in `OUTREACH.md`; blocked on Daniel to send.** Do not write a page about it.

10. **Sweep for commercial pages ranking on informational queries — DEMOTED run 16, swept negative again run 18.** The pattern is real and run 15 took the only instance worth having. Runs 16 and 18 both found no `/services/*`, `/pricing`, `/book` or `/contact` URL serving a `what is` / `how to` / `why` query outside the sprint cluster, which is already handled and inside its verdict window. **One live hit is queued, not dead:** `how does a post-pmf plg saas company scale acquisition without breaking unit economics`, 16 imp @ 36.1, on the bare `/insights` index — blocked until run 13's window closes **2026-09-23**. **Keep the sweep — it costs one pull. Do not rely on it to produce a shippable change.**

**Do not do:** Optimise any page for the site's own framework vocabulary — *layer*, *five-layer*, *bottleneck*, *scorecard*, *constraint*, *framework*. Run 14's 90-day `page,query` pull gives the whole cluster **14 queries, 56 impressions, zero clicks**, with no query using "layer" as a growth term at all. Fix those pages for buyers and passage extraction, never for rank. Re-assess the three 166–173 char meta descriptions (backlog 9 — declined three times). Register at Companies House or describe WSS as an Ltd — **it is not a limited company** and Daniel has declined. Expand `llms.txt` or the `ai:*` tags. Add FAQPage schema *expecting rich results* (deprecated May–Aug 2026 — FAQ content is still worth writing for passage extraction, a different rationale). Build the `/alternatives/*` programme. Ship `Review`/`AggregateRating`. Optimise titles/meta at position 45+. Chase CTR on brand-sitelink impressions. Re-flip robots.txt `ai-train`. Re-anchor the homepage on "agency" (backlog 6). Merge the per-run changelogs (Trap 4). **Target `scale startup` — 860 impressions at position 10.3 with zero clicks is a Scale AI brand collision, not demand (run 18, SERP-verified). Discount it from every impression and CTR figure.** Score a personalised Perplexity mention as a win (run 18).

12. **`agency brief template` — the page does not rank for its own name.** `/resources/agency-brief-template` has zero impressions on its exact title in two windows, and its download is an **83-word markdown skeleton**. The SERP for that term is soft and hits the real pre-hire buyer. If the page is ever worked, thicken the deliverable — do **not** chase `agency collaboration template`, which is a closed decoy (see `QUERY-MAP.md`).

3. **Off-site authority — WORKSTREAM C, lives in `seo-bot/OUTREACH.md`. Read that file, not this entry.** Settled by runs 11–13: Person schema fixed, Organization `sameAs` 7 → 11, founding year unified to **2016**, canonical email `daniel@`, name standardised as **We Scale Startups**, **Ltd** removed from Crunchbase. **Standing decisions from Daniel: no Companies House registration; display name stays "Daniel Johnson".** Open items are in OUTREACH.md and the *What only Daniel can do* list.

6. **Homepage non-brand category cluster — deferred since run 5.** ~630 impressions at positions 7–13, one click. The largest non-brand pool on page 1–2, deferred because the fix means re-anchoring the homepage on "agency" — which the site sells *against*. Revisit only if a non-homepage page picks these up, or Daniel wants a category page.

7. **Finish the abandoned growth-system consolidation** — `/growth-engine`, `/ai-driven-growth`, `/how-we-work` are treated as consolidated but still collect a few impressions. Low priority.

8. **`scalable land acquisition teams` hits `/services/acquisition-system-build`** — real-estate intent, wrong market. Harmless; if the page is ever rewritten, tighten "acquisition" to "customer acquisition" in visible copy.

### Closed — kept for the reasoning, not the task

11. **CLOSED run 17. `/gtm-strategy` — 9 queries, 22 impressions, zero clicks. Below the threshold for any intervention, and both earlier diagnoses were wrong.** The transferable rule: **never diagnose a page's intent from its own content — pull its query set.** Do not reopen.
  - **The transferable rule, and today's second instance of it: never diagnose a page's intent from its own content. Pull its query set.** A confident wrong prescription sat at the top of the backlog for a full run because nobody spent four browser clicks.

4. **CLOSED 2026-08-27. `/insights/what-226-founder-reviews-reveal` shipped** — original research from 226 public GrowthMentor reviews; analysis and caveats in `RESEARCH.md`. **Verdict 2026-09-24. The mechanism under test is citation, not ranking** — panel prompts 6 and 8 and the Generative AI report first, classic clicks last and possibly never. A null result is a real finding. Rule earned: **before recording something as blocked on a person, check whether it is already public.**
  - **Generalisable, and it happened twice in one day:** *before recording something as blocked on a person, check whether it is already public.* The other instance was concluding the GSC AI report was unreachable after testing exactly one tool.
  - **Granola changed the article before it was written.** The private record (~350 meetings, ~1/3 founder-facing) corroborated three findings and **contradicted the fourth**: the draft headline was going to be that founders don't think in stage terms, which is false — they volunteer stage readily; it just never returns as an explanation of what is wrong. **Use the private record to make interpretations honest, never as a second published dataset.** Nothing from Granola was published.

1. **CLOSED. Migration recovery complete** (run 13). **Verdict 2026-09-23.** It worked because Daniel had the GTM guide body, not because a run was clever — writing 4,000 words of unsourced advice produces filler.

2. **CLOSED. All four designated pillars written to depth** (runs 9 and 13) — `what-is-a-fractional-cmo`, `startup-growth-bottlenecks`, `ai-native-gtm`, `b2b-saas-gtm-strategy`. **Verdicts 2026-09-16 and 2026-09-23; baseline to beat is 1 and 6 impressions.** Do not rewrite before the verdict date. **If they are still at zero on 2026-09-23 the pillar thesis is authority-bound too, and that changes the backlog.**

5. **CLOSED (run 13). `/insights` hub rewritten for provider intent** (was 1,032 impr / 0 clicks at 28.9 serving a link list). **Verdict 2026-09-23. Watch clicks, not impressions.** Earned its first AI citation in panel reading #3. One informational query queued against it for after that date — backlog 10.

9. **Housekeeping — done run 13.** `llms.txt` is a real index; oversized team PNGs go through `scripts/optimise-raster-images.mjs`, which **preserves alpha** — check `getchannel("A").getextrema()` before touching any PNG. **Deliberately not done: the three 166–173 char meta descriptions** (declined three times — do not raise again). Still open, config not content: HTML is never edge-cached (`cf-cache-status: DYNAMIC`) on a fully static build.

**Closed:** `cal…/auth/login` (already correctly noindexed via Traefik, run 3). The `www.` legacy host (run 4). **Legacy `/portfolio/*` URLs** — re-checked live in run 5: `www…/portfolio/equoo/` → apex → `/case-studies/equoo`, both 301, chain clean. The GSC rows showing `www…/portfolio/equoo/` as a landing page are historical. **Extending the run-4 pattern into the fractional-CMO cluster** — done in run 5 on the one page that ranks for it, and in run 9 on the one page that had nothing to rank with; the remainder of that cluster is at position 45+ and is authority-bound, so do not repeat it there.

## What worked / what didn't

- **run 1 — FAILED, twice read.** Query-family re-anchor of `/fractional-cmo-vs-agency`: 740 named-query impressions, weighted position 46.2, **zero clicks**, against the 44.1 that motivated it. **Content is necessary but not sufficient at position 45** — not "content doesn't matter"; runs 4 and 5 act where content *is* sufficient. Do not rewrite this page again.
- **run 2** — bug fixes, generated lastmod, Offer schema, CI guard. Verdict **2026-09-04**.
- **run 3** — fractional-CMO consolidation. Verdict **2026-10-03**; 301 spot-check **2026-09-19**.
- **run 4** — provider-answer passage + FAQs on the sprint page; three legacy 404s redirected. Verdict **2026-09-01**. Provider intent only — do not confuse with run 15's informational verdict on the same page.
- **run 5** — three-way decision answer, comparison table, four FAQs on `/insights/when-to-hire-fractional-cmo`. Verdict **2026-09-08**. Now at 71 impressions / pos 9.3 / zero clicks; the Series A three-way question is 43 impressions at pos ~7.9 across two punctuation variants.
- **run 9** — `/insights/what-is-a-fractional-cmo` to pillar depth (2,335 words), owning definition/scope only, five-way comparison table. Verdict **2026-09-16**. Baseline to beat is literally zero impressions.
- **runs 10–13** — one long interactive session with Daniel: sitewide audit, entity/schema repair, off-site profiles, three pillars, `/insights` hub, four orphans. Verdicts **2026-09-23**. ~4x an unattended run's scope, only because Daniel supplied the GTM guide and made the positioning calls. **Do not reproduce this scope unattended.**
- **An orphan page is not one diagnosis.** Run 13 found four pages with zero inbound internal links and gave them three different answers: link it (three of them, from pillars that actually have traffic), or noindex it (`/wss-calendar`, whose own headings are "What WSS Calendar does" and "Privacy and terms"). The useful question is not "how do I link this" but "why did nobody link it". Sometimes the honest answer is that it should never have been in the index.
- **Indexed and invisible is a content verdict, not a technical one.** Run 9's target returned URL-inspection verdict PASS, "Submitted and indexed", clean canonical, robots ALLOWED — and zero impressions in 90 days, last crawled 2026-07-24. Before diagnosing a zero-impression page as a crawl, canonical or sitemap problem, inspect it: if Google already has it, the problem is that there is nothing on it worth ranking.
- **Run 2 broke `main` for ~20 min** pushing a file before its dependency. **Run 9 left CI red for ~7 min** trusting a false claim in this file instead of checking `src/lastmod.ts` — caught only by re-running `check:lastmod` against the pushed tree. See Trap 5.
- **Run 4 chased a false lead** — brand sitelink stacking read as "position 3–8, zero clicks". The tell: same query, same position, seven pages. **Run 18 nearly chased a second one** (`scale startup`); the tell there was the SERP, not the data.
- **The hygiene layer is clean and re-auditing it is waste.** Run 5's sitewide `dist` audit: zero missing `alt`, all internal links resolve, all 596 JSON-LD blocks parse, all legacy 404 families redirect. **Spend the time on query data instead.**
- **Verdicts due, in order: run 4 2026-09-01 · run 2 2026-09-04 · run 5 2026-09-08 · run 9 2026-09-16 · run 15 2026-09-17 · run 3 2026-10-03 · runs 10–13 2026-09-23 · original research 2026-09-24.** Seven windows close inside a month. **The next several runs are mostly judging, not shipping — plan for that rather than hunting for something to change.**
- **2026-08-27 run 15** — new `/insights/how-to-run-a-90-day-growth-sprint`, plus a quick-win lastmod two-step cleaning up after run 14. Pending, due **2026-09-17**. Baseline is four queries, 110 impressions, positions 11.3–15.0, **zero clicks**, all landing on the £8k–£12k service page. Read **clicks**, and whether the new URL takes the informational queries off `/services/90-day-growth-sprint`. A position drop on that service page for those four queries is the system working, not a regression — do not confuse it with run 4's provider-query verdict on 2026-09-01, which is a different intent on the same page.
- **Positioning rule, added run 15** — put the direct answer in the **first 30% of the page**, not the conclusion. Current GEO research puts roughly 55% of AI Overview citations in the opening third of the cited page. Run 15's definition sits at 7% of rendered page depth by measurement, not by eye — measure it in `dist/*.html` rather than assuming.
- **THE GSC GENERATIVE-AI REPORT IS READABLE VIA THE UI.** The MCP connector cannot reach it (`searchAppearance` returns an empty array), and run 16 wrongly concluded from that alone that it was unavailable. **The UI can, and the bot can drive the UI.** *Never conclude a data source is unavailable without trying the browser.*
  - **Access note:** Chrome defaults to `admindjohnson@gmail.com`, which does **not** have the property. Switch to `daniel@wescalestartups.com` in the account picker, or append `/u/1/`. Report lives at Performance → **Generative AI** (Beta), or `search.google.com/u/1/search-console/performance/search-analytics/ai?resource_id=sc-domain:wescalestartups.com`.
  - **Impressions only. There is no clicks metric and no QUERIES tab** — only Pages, Countries, Devices, Days. So it tells you *which pages* get surfaced in AI features, never which prompt surfaced them. The AI panel in `AI-VISIBILITY.md` remains the only instrument for prompt-level visibility; the two are complements, not substitutes.
  - **BASELINE 2026-08-27: 653 AI impressions over 3 months, 217 over 28 days** (~7.2/day vs ~7.75/day). 50 pages over 3 months, 25 over 28 days. **Run 18 (2–29 Aug): 228 across 27 pages, ~8.1/day. Flat. Do not call it growth.**
  - **The one real mover is `/facts/we-scale-startups`: 7 → 17 AI impressions, and the panel cites it on prompt 5.** Two instruments, one conclusion — keep that page fresh and factual. `/fractional-cmo-vs-agency` held flat at 18.
  - **Top pages, 28 days:** `/services/90-day-growth-sprint` **37** · `/fractional-cmo-vs-agency` **18** · `/about` 16 · `/resources` 15 · `/services/fractional-cmo` 11 · `/gtm-strategy` 10 · `/services/growth-diagnosis` 9 · `/facts/we-scale-startups` **7** · `/about/daniel` 6, with the homepage above them. Over 3 months the homepage takes 256 and the sprint page 151 — **62% of all AI impressions between two URLs.**
  - **Two instruments now agree independently.** The AI panel found Google AI Mode citing `/services/90-day-growth-sprint` and `/facts/we-scale-startups`; this report ranks them 2nd and 8th by AI impressions. That is corroboration from a completely different measurement path, and it is the strongest evidence yet that the extraction-oriented page design works.
- **90-day reference: 149 clicks / 17.1k impressions / 0.9% / position 25.7** (UI, Web, run 18; 148 at run 17, 144 at run 16 — the drift is trailing-edge settling). An earlier "42 clicks / 9,500" line was wrong. **Where the API and UI disagree, check the UI before writing a number here.**
- **SETTLED: the top AI-cited pages are already built for extraction** — `/fractional-cmo-vs-agency` answers at ~2% depth, `/services/*` name price, duration and fit inside 90 words, `/facts/we-scale-startups` is a fact table. **Stop re-auditing this layer.** `/fractional-cmo-vs-agency` needs no third rewrite: right content, 574 classic impressions at 44.2, zero clicks, and the 2nd-highest non-homepage AI-cited page (18, flat across two readings). **Authority, definitively — and judge it on AI impressions, not rank.**
- **2026-08-31 run 18 — OFF-SITE. Nothing shipped, and three things were learned that change the next several runs.**
  - **The panel's own instrument was defective.** Perplexity runs on Daniel's signed-in account; reading #3 labelled the WSS mention "Given your work with We Scale Startups". Reading #2's recorded "first Perplexity citation" is almost certainly the same artefact. **A recorded win was downgraded to zero. Do that when the evidence says so.**
  - **`scale startup`, 860 impressions at position 10.3 with zero clicks, looked like the best unclaimed opportunity on the site and is a Scale AI brand collision.** The live SERP is scale.com, Wikipedia "Scale AI", WSS third, LinkedIn "Scale AI", YC "Scale AI". **One SERP fetch turned the run's most attractive lead into a prohibition** — which is exactly what that fixed rule is for. It also means this window's impression growth is an artefact and site CTR was flat (1.27% ex-collision), not down.
  - **The constraint named by the evidence was off-site and unusually specific** (backlog 13). No page this bot can write moves it.
  - **And one asymmetry worth watching:** run 15's insight was cited by Google AI Mode **four days** after publication, while classic search reallocated **zero** impressions to it in the same period — `how to run a 90 day growth sprint` still lands 100% on the £8k–£12k service page. Do not read that as a run-15 failure before 2026-09-17. It argues the two channels need different review horizons.
- **2026-08-27 run 17 — REPORT.** Nothing shipped: Search Console's window had not advanced since run 16, so no new evidence existed. Two panel readings on one calendar day is noise — **reading #3 must fall on a different day**, and did (run 18). Recorded: Google returns **zero external citations on panel prompts 2, 6 and 10**, structurally unwinnable in current form. The GSC MCP connector died mid-run (`invalid_grant`). **Run 18 fixed it, and the run-17 diagnosis was wrong: auth was fine.** Daniel installed the replacement extension (`search-console-mcp` v2.1.2) an hour after run 17, and it had **shipped without its `node_modules`** — it crashed on `import dotenv` at every launch, which the app surfaces as "Server disconnected". `npm install --omit=dev` in the extension directory fixed it; `sites_list` and `analytics_query` both return real data with no credential change. **`page,query` is available again.** Detail in `changelog/2026-08-27-run17.md`.
- **2026-08-27 run 16 — the first run to ship nothing on purpose, and the first to measure what the mandate is about.** Every page with demand was inside a verdict window or freshly committed; the backlog lead swept small; and the first panel reading said the constraint on eight of ten buyer prompts is that WSS is not in the retrieval set. **A run that ships nothing and records why is a successful run.** Fifteen consecutive shipping runs produced zero named non-brand clicks; the alternative to shipping is measuring, not manufacturing work. Detail in `changelog/2026-08-27-run16.md`.
- **The neutral-roundup class is nearly empty, and that is a finding.** Run 16 verified every "best growth agency / fractional CMO UK 2026" page it could find: essentially all are competitor-owned content marketing with no submission route — inBeat, Data-Mania, StrategicPete, Digital Hunch, Blazon, Ryesing, VXTX, One Umbrella, and Growth Division, which openly discloses it is not neutral. **One exception**, GrowthMentor's own agency roundup, which states it takes no referral fees and where Daniel is already a mentor — a warm route. **Do not spend another run hunting neutral roundups; the directories in `OUTREACH.md` are where the reachable supply is.**
- **Workstream C's thesis is evidenced, and reproduced.** Google surfaced the same third-party listicle containing "WeScaleStartups" in its sources on prompt 9 in **both** panel readings — a stable retrieval path, not a one-off. The engines read provider listicles and directories, so the target is getting onto those pages. **Do not pay for placement, and do not ask a competitor to add WSS to their own listicle.** Detail in `OUTREACH.md`.

## Standing decisions

- **Every commit boundary must be a state that builds.** If a change spans files, push the files together.
- **Verify the build against the tree you pushed, not the tree you have locally.** Since run 5, do this in the sandbox: clone, `npm ci`, apply the change, `npm run build` + `astro check`, and inspect the rendered `dist` HTML — *before* the commit exists.
- **Then verify again AFTER the push.** Fetch `origin/main`, rebuild from it, and re-run `check:lastmod`. Some checks — `check:lastmod` above all — are structurally incapable of failing before the commit exists (Trap 5). A run is not finished until the pushed tree has been built and checked.
- **Noindexing a static page is a two-file change.** `src/pages/sitemap.xml.ts` keeps a hand-maintained static-path list, so `metaRobots="noindex"` alone leaves a sitemap advertising a noindex URL. Change both, then check the arithmetic: **pages built − noindexed = sitemap `<loc>` count.**
- **Sitemap lastmod is generated, not hand-edited.** Two-step: commit the content, then `npm run refresh:lastmod` and commit the result. Applies to any route listed in `src/lastmod.ts` — static pages, service/case content, and the four `/insights/*` pillars. `grep` the route rather than assuming.
- Prefer content-only commits; avoid layout/CSS the bot can't visually verify. Reusing an existing component on an existing code path is fine; adding a page to a template's special-case list is not worth it if the same text already renders elsewhere in the DOM.
- Always fetch the live URL post-deploy before declaring success, and re-test that new redirect rules did not shadow existing specific ones.
- Never state unsourced market statistics. Anchor cost claims on WSS's published ranges and reuse the bands on `/insights/fractional-cmo-cost-uk` rather than minting new ones.
- **Keep the "point them somewhere better" voice.** Every comparison should name the case where a competitor or a different model is the right buy. It is the brand voice *and* what makes a passage worth citing.
- **When a change is defensible under two competing readings of the data, prefer it** over one needing the ambiguity resolved first.
- **Sweep the connectors before writing anything that implies WSS expertise or client work.** The VC guide was written assuming fund-side experience; Granola, Notion, Gmail and Drive established there is none — every named fund is a cold prospect. The real position is accelerator-side (Google for Startups, DeepMind, Techstars, GrowthMentor, several hundred founder sessions). **That check stopped an unsupportable claim reaching a public page.** Do it every time.
- **A hub page must not restate its spokes.** Run 3 spent a whole run undoing a self-competing cluster; do not rebuild one.
- **`src/data/growthTools.ts` is the primary source for what the free tools do — not any page that describes them.** Runs 13 and 14 both got this wrong. The Growth Bottleneck Scorecard is **12 questions, ~4 minutes**, scoring **founder dependency** — a different question from the five-layer framework on `/diagnose`. Two instruments, two questions; never describe either in terms of the other.
- **Push large files through a subagent that reads and pushes them itself.** Run 14 moved the 49 KB `src/pages/services/[slug].astro` this way: the subagent runs `git hash-object` on the sandbox file, pushes it, then checks the returned blob SHA matches. The bytes never enter the parent's context and corruption becomes detectable rather than silent. This is the practical answer to Trap 4 for anything large.
- **Judge a page from its rendered HTML, never from source-file size or a source grep.** Run 13 nearly noindexed `/when-growth-plateaus` after grepping its `.astro` file and finding no headings — it uses `StrategicPage`, which supplies them. On a component-driven site the source file is not the page. Measure `dist/*.html` or the live URL.
- **Verify claims about the site against the site, not against this file.** Three false facts have now been found here: an invented robots.txt reversal repeated across four runs, a "Bing reports zero inbound links" line left in place three bullets below the correction that disproved it, and the `/insights/*` lastmod exemption that put CI red in run 9. **The pattern in all three is a half-truth that was never re-checked against the primary source.** Before acting on any factual claim in this file, `grep` the repo or fetch the live URL — and when you correct something, **delete the wrong line** rather than adding a corrected one next to it. **Fourth instance, run 18: this file recorded the research insight as `/insights/what-226-founder-sessions-show`. The live slug is `/insights/what-226-founder-reviews-reveal`** — it was renamed behind a 301 in run 16 addendum 5 and the playbook kept the pre-rename name. Found by reading the deployed sitemap, not by reading this file.
- Mark confidence explicitly (🟢 verified / 🟡 inferred / 🔴 vendor-sourced). Most GEO research is published by companies selling GEO tools.
