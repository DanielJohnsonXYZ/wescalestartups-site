# 2026-08-26 (run 9, backlog 2) — wrote the pillar that was publishing a to-do list

**Shipped** (`dd0b6e7`). `/insights/what-is-a-fractional-cmo`, 1,345 bytes → 14,004 (2,335 words).

## What it was

A page designated a pillar in `insightPillarIds` whose entire body read "This is the hub for our fractional CMO notes. Linked notes *will* expand on…", followed by a **"Planned spokes"** list of four articles that do not exist. An internal content plan, published. **Zero impressions in 90 days.**

## Why this page, and not backlog 1

Backlog 1 (the GTM guide, 10,037 lost impressions) is bigger by volume, but the playbook flags it as needing Daniel's reconstruction and this was an unattended run. Writing 4,000 words of GTM advice from scratch, with no source document and no WSS-specific evidence, produces exactly the undifferentiated filler the VC run identified as valueless. Backlog 2 needed nobody. That reasoning is now written into backlog 1 so a future unattended run does not have to rediscover it.

## The evidence

GSC 90d (2026-05-28 → 2026-08-24), queries containing "fractional cmo":

**115 rows, 2,145 impressions, 0 clicks — 22.5% of every logged impression on this site.** The largest non-brand pool by a wide margin.

The head terms are authority-bound and stay unfought, per the standing rule:

| pos | imp | query |
|---|---|---|
| 51.7 | 244 | `b2b fractional cmo` |
| 43.8 | 215 | `saas fractional cmo` |
| 47.4 | 159 | `fractional cmo for b2b saas` |
| 50.4 | 131 | `startup fractional cmo` |
| 44.1 | 121 | `fractional cmo for saas` |

But the run-4/5 thesis says find the *question-shaped* query, see which page Google already chose, and make that page answer it. `what is a fractional cmo` is the canonical question-shaped query in this cluster, and the page for it had nothing to rank with. **This is the one page in the cluster where the constraint is not authority. It is absence.**

## Confirmed by URL inspection, and worth recording

Verdict **PASS**, "Submitted and indexed", canonical clean, robots ALLOWED — **last crawled 2026-07-24**. The page was never a crawl or index problem. Google had it, fetched it, and found 1.3KB with nothing to rank for. **Zero impressions on an indexed page is a content verdict, not a technical one.** Added to the playbook, because the instinct is to reach for sitemap and canonical diagnostics first.

## What the rewrite deliberately owns

Definition and scope only. Timing lives in `when-to-hire`, cost in `fractional-cmo-cost-uk`, the agency comparison in `fractional-cmo-vs-agency`; this page summarises and links rather than restating, so the hub stops competing with its own spokes. Run 3 spent a whole run undoing a cluster that competed with itself.

The differentiated section is the **five-way distinction** — fractional vs consultant vs interim vs advisor vs part-time hire, as a table, with the two distinctions that carry the weight stated plainly: consultant-vs-fractional is *accountability*, interim-vs-fractional is *whether the seat already exists*. Nobody in this market writes that down clearly, and it is the part worth citing.

The weekly operating rhythm is a table taken verbatim from the `process` array in `src/content/services/fractional-cmo.json` — Monday pipeline review, Wednesday experiment ship, Friday scale/stop/fix, and the CAC < LTV/3 decision rules. Named, specific, extractable.

## No new figures

£5k–£8k/mo and £7.5k–£10k/mo from `pricingTiers`; the £6k–£20k/mo agency band and £120k–£180k full-time band from `/insights/fractional-cmo-cost-uk`, which the playbook names canonical. No market statistics introduced, no client work implied, nothing repeated from a search summary.

## Voice

The page says an advisor is dramatically cheaper if you only want a sounding board, says buy execution if the channel is already chosen, and closes with three screening questions — one of which is to ask a fractional CMO to name the case where you should hire someone else.

## Verified

**In the sandbox, before the commit existed:** 117 pages build clean, `BUILD_EXIT=0`, `astro check` 0 errors 0 warnings, all **603** JSON-LD blocks parse sitewide, FAQPage carries 7 questions, Article `datePublished` 2026-05-04 / `dateModified` 2026-08-26, canonical clean, in sitemap (107 URLs), `check:lastmod` green (expected — `/insights/*` is not in `staticPathLastModified`), all six internal link targets resolve in `dist`, TL;DR renders as the first content block in the DOM.

**Post-deploy, live with cache-buster:** 200, title/description/canonical correct, "Planned spokes" gone, 6 JSON-LD blocks including FAQPage×7, present in the live sitemap. The four sibling cluster pages (`when-to-hire`, `cost-uk`, `vs-agency`, `services/fractional-cmo`) all still 200.

Title tightened to 40 chars (60 with brand suffix), description to 154 — both inside the house limits the 2026-08-25 audit set.

Bing: 1 URL submitted (quota 100/day, 600/month, otherwise untouched).

## Expected movement

This page had *zero* impressions, so any impressions at all are the signal. Watch for `what is a fractional cmo` and the definitional family appearing, and for the interim/consultant comparison queries the site has never ranked for. **Clicks are not the near-term read** — the whole cluster is zero-click and the head terms remain authority-bound. Secondary read: whether AI answers describing the fractional model start citing this URL, which is what the five-way table is built for and which will not appear in GSC at all.

**Review date:** 2026-09-16 (the page's observed crawl cadence is roughly monthly; last crawl 2026-07-24).

## Also this run

**No quick wins shipped.** Followed run 5's finding that the hygiene layer is clean and re-auditing it every run is waste; spot-checked only what this change touched.

**Fresh trend, recorded in the playbook.** Three consecutive 14-day windows: 14–27 Jul 18 clicks / 2,398 imp / 0.75% CTR / pos 27.9; 28 Jul–10 Aug 36 / 2,903 / 1.24% / 28.4; 11–24 Aug 35 / 2,560 / 1.37% / 25.0. Clicks roughly doubled and held, CTR rose in both steps, position improved 2.9 places. Impressions flat-to-down. The site is slowly improving and the audit narrative — entirely about the May cliff — had no read on direction.

**A method note that may outlast the ship.** `dimensions: "page"` and `dimensions: "page,query"` disagree, and the gap is the finding. `/insights/b2b-saas-gtm-strategy` shows 65 impressions at position 8.6 under one and returns **no rows at all** under the other, because 100% of its impressions are anonymised. Any page whose named-query rows do not reconcile to its page-level total is being found by long conversational prompts. Diff the two dimensions deliberately.

**Verdicts due today: none.** Run 1's window closed 2026-08-28 and its read is unchanged from run 5's second negative. Runs 2, 3, 4 and 5 are all still inside their windows (2026-09-04, 2026-10-03, 2026-09-01, 2026-09-08). Nothing marked working or failing on data that is not in yet.

## Housekeeping: the changelog is now one file per run

`CHANGELOG.md` reached 41,777 bytes after eight runs. There are no git credentials in the sandbox, so every write goes through the GitHub connector, which replaces whole files — a one-entry prepend requires re-emitting all 41KB verbatim from context, which is not a safe operation on a durable log and cannot be verified before it lands.

From run 9, each run writes `seo-bot/changelog/YYYY-MM-DD-runN.md`. **`CHANGELOG.md` is frozen exactly as it stands and still holds runs 1–8** — nothing was moved, rewritten or lost. `PLAYBOOK.md` points here.
