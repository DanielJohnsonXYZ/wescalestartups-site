---
import OfferRoutingCards from "../components/OfferRoutingCards.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import SectionIntro from "../components/SectionIntro.astro";
import { buildBreadcrumbSchema, buildFaqSchema, buildOfferCatalogSchema } from "../lib/schema";
import { pricingRelatedReading, pricingTrustBullets, pricingTiers, siteConfig } from "../site";

const pricingFaqs = [
  {
    question: "Why publish starting prices?",
    answer: "The ranges help founders self-qualify before a call. Final scope still depends on team size, channel mix, urgency, and execution depth."
  },
  {
    question: "Which engagement do most founders start with?",
    answer: "Most start with Growth Diagnosis when the bottleneck is unclear. If the constraint is already visible, a sprint, system build, or fractional CMO scope may be cleaner."
  },
  {
    question: "Are media spend and tools included?",
    answer: "No. WSS fees cover strategy, system design, execution support, and reporting. Media spend, tools, and third-party vendor costs are separate."
  }
];

// Offer markup for the four engagements. Slugs key into servicePriceRanges;
// names and descriptions come from pricingTiers so the markup cannot drift
// from the copy rendered below.
const offerCatalog = buildOfferCatalogSchema(
  pricingTiers.map((tier) => ({
    slug: tier.href.replace("/services/", ""),
    name: tier.name,
    description: tier.description,
    path: tier.href
  }))
);
---

<BaseLayout
  path="/pricing"
  breadcrumbItems={[
    { name: "Home", path: "/" },
    { name: "Pricing", path: "/pricing" }
  ]}
  title="Startup Growth Pricing | We Scale Startups"
  description="Engagement ranges for Growth Diagnosis, 90-Day Sprint, Acquisition System Build, and Fractional CMO. Honest ranges so you can self-qualify."
  keywords="fractional CMO pricing, growth diagnosis cost, 90-day growth sprint price, acquisition system build, post-PMF startup consulting UK"
  schema={[buildBreadcrumbSchema([
    { name: "Home", path: "/" },
    { name: "Pricing", path: "/pricing" }
  ]), buildFaqSchema(pricingFaqs), offerCatalog]}
>
  <section class="page-hero">
    <div class="shell">
      <p class="eyebrow">Pricing</p>
      <h1>Honest UK pricing. No "request a quote" gating.</h1>
      <div class="hero-support">
        <p class="lede">
          All prices in GBP, set for the UK market. Every tier is a range: where you land depends on team complexity, scope, and timeline.
          If the ranges aren't workable for you, an agency or in-house hire will probably be a better fit.
        </p>
        <div class="hero-actions">
          <a
            class="button button-brand"
            href={siteConfig.bookingUrl}
            data-cta="pricing-hero-book"
            title={siteConfig.bookingCallReassurance}
          >
            {siteConfig.bookingLabel} <span aria-hidden="true">→</span>
          </a>
          <a class="link-arrow" href={siteConfig.scorecardUrl} data-cta="pricing-hero-scorecard">
            Or, {siteConfig.scorecardLabel} →
          </a>
        </div>
        <p class="hero-microcopy">{siteConfig.bookingSubcopy}</p>
      </div>
      {/* The page's whole promise is published ranges, so the rail answers the
          headline in the hero instead of making the reader scroll for it. Same
          data as the tier cards below (pricingTiers), no second source. */}
      <div class="hero-rail hero-rail-stack">
        <p class="hero-rail-label">Engagement ranges</p>
        <div class="hero-ledger">
          {pricingTiers.map((tier, index) => (
            <div class="hero-ledger-cell">
              <span class="hero-ledger-index">{String(index + 1).padStart(2, "0")}</span>
              <span class="hero-ledger-value">{tier.priceFrom}</span>
              <span class="hero-ledger-label">{tier.name} · {tier.duration}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="shell">
      <SectionIntro eyebrow="Engagement tiers" title="Pick the smallest engagement that answers your question." />
      <div class="pricing-grid">
        {pricingTiers.map((tier, idx) => (
          <article
            class:list={[
              "pricing-card",
              { "is-recommended": idx === 0 },
              { "is-most-popular": "mostPopular" in tier && tier.mostPopular }
            ]}
          >
            {idx === 0 && <span class="recommended-badge">Most founders start here</span>}
            {"tierBadge" in tier && tier.tierBadge && <span class="popular-badge">{tier.tierBadge}</span>}
            <h3>{tier.name}</h3>
            <div class="pricing-meta">
              <span class="pricing-price">{tier.priceFrom}</span>
              <span class="pricing-duration">{tier.duration}</span>
            </div>
            <p>{tier.description}</p>
            {"popularRationale" in tier && tier.popularRationale && (
              <p class="pricing-popular-rationale">{tier.popularRationale}</p>
            )}
            <a class="card-link" href={tier.href}>Learn more →</a>
          </article>
        ))}
      </div>

      <div class="pricing-disclaimer">
        Final fee depends on team size, channel mix, and the depth of execution required.
        Every engagement starts with a {siteConfig.bookingCallShort} so we agree on scope before any contract.
        Limited number of clients each quarter.
      </div>

      <ul class="trust-list pricing-trust-list" aria-label="How engagements work">
        {pricingTrustBullets.map((line) => (
          <li>{line}</li>
        ))}
      </ul>
    </div>
  </section>

  <OfferRoutingCards />

  <section class="section">
    <div class="shell two-column">
      <div>
        <p class="eyebrow">What's not in the price</p>
        <h2>Media spend. Tools. Vendors.</h2>
        <p class="lead-copy">Our fee covers strategy, system design, execution, and reporting. Paid media spend, tooling subscriptions, and any third-party vendor fees are separate and pass through at cost.</p>
      </div>
      {/* Four identical cards read as four features until something marks them
          as exclusions. The × is the site's own glyph for "not included" and
          costs nothing — no icon set enters the build. */}
      <div class="grid grid-2 exclusion-grid">
        <article class="feature-card compact-card exclusion-card"><p>Paid media spend (Google, Meta, LinkedIn, etc.)</p></article>
        <article class="feature-card compact-card exclusion-card"><p>Tools and subscriptions (analytics, CRM, automation)</p></article>
        <article class="feature-card compact-card exclusion-card"><p>Third-party vendors (linkbuilding, video, design)</p></article>
        <article class="feature-card compact-card exclusion-card"><p>Travel for events or trade shows (if applicable)</p></article>
      </div>
    </div>
  </section>

  <section class="section section-alt">
    <div class="shell">
      <SectionIntro eyebrow="Related reading" title="Context for the numbers." />
      <ul class="related-insights-list">
        {pricingRelatedReading.map((item) => (
          <li>
            <a class="card-link" href={item.href}>
              {item.label} →
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section class="section">
    <div class="shell">
      <SectionIntro eyebrow="Pricing FAQ" title="Common questions before you book." />
      <div class="grid-3">
        {pricingFaqs.map((faq) => (
          <article class="feature-card">
            <h3>{faq.question}</h3>
            <p>{faq.answer}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="cta-band">
    <div class="shell cta-inner">
      <div>
        <p class="eyebrow">Not sure which tier fits?</p>
        <h2>Find out on the call.</h2>
        <p>
          {siteConfig.bookingCallDurationPhrase}. We'll tell you which engagement (if any) actually solves your bottleneck, and which would be wasted spend.
        </p>
      </div>
      <div class="hero-actions">
        <a
          class="button button-brand button-with-subcopy"
          href={siteConfig.bookingUrl}
          data-cta="pricing-cta"
          title={siteConfig.bookingCallReassurance}
        >
          <span class="button-label">{siteConfig.bookingLabel}</span>
          <span class="button-subcopy">{siteConfig.bookingSubcopy}</span>
        </a>
        <a class="link-arrow" href={siteConfig.scorecardUrl} data-cta="pricing-scorecard">
          Or, {siteConfig.scorecardLabel} →
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Shared surface lives in .trust-list (components.css); only the top
     margin is page-specific here, this list follows the disclaimer above. */
  .pricing-trust-list {
    margin: 1.75rem 0 0;
  }

  /* Exclusions, not features. Border/radius/background still come from the
     shared card contract in site-v9.css; this only adds the marker. */
  .exclusion-card {
    position: relative;
    padding-left: 3.1rem;
  }

  .exclusion-card::before {
    content: "×";
    position: absolute;
    left: 1.1rem;
    top: 50%;
    display: grid;
    place-items: center;
    width: 1.5rem;
    height: 1.5rem;
    transform: translateY(-50%);
    border: 1px solid var(--line-strong);
    border-radius: 50%;
    color: var(--muted);
    font-family: var(--v9-mono);
    font-size: 0.85rem;
    line-height: 1;
  }
</style>
