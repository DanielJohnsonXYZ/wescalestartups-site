# SEO/GEO/AEO bot — playbook

Updated each run. **Read this, then the changelog, at run start.** The changelog is in two places since run 9: `seo-bot/CHANGELOG.md` holds runs 1–8 and is frozen; every run from 9 onward writes its own file in `seo-bot/changelog/` named `YYYY-MM-DD-runN.md`. Read the most recent two or three of those plus the top of `CHANGELOG.md`. See Trap 4 in the connector notes for why. **Runs 10-14 were one long interactive session with Daniel** — 10 is the sitewide audit, 11 the entity/schema work, 12 the off-site profiles, 13 the orphan pages, 14 the diagnostic-tool corrections. Read 14 first; it carries the corrections, including two to run 13's own conclusions.

## Current understanding (2026-08-26)

- Site: **117 pages built, 11 noindexed, 106 in the sitemap** (2026-08-26), Astro static, push to main = deploy (Cloudflare Pages). Deploys land in **under 2 minutes**.
- **SETTLED — the May 2026 cliff and its recovery.** The site lost 60% of its organic impressions on 6 May 2026 (the WordPress-to-Astro rebuild) and no run noticed for seven. 5 URLs were 80% of the loss; nine legacy URLs were still 404ing on 2026-08-25, including one at 7,520 impressions / position 7.4. All redirected (`f814831`), the VC guide restored (`6ddf57f`), the GTM guide restored from Daniel's original. **Recovery is complete — do not re-diagnose it.** Full detail in `changelog/2026-08-26-run10-audit.md` and `-run13-orphans.md`.
- **The average position "decline" from 14.1 to 27.7 is composition, not decay.** Queries present in both windows *improved* (median -2.25 places). High-position legacy pages left the index and were replaced by new pages ranking at positions 24-64. Do not read the site-wide position number as a ranking signal.
- **Brand queries grew across the rebuild (+206 impressions); 100% of the named-query loss was commercial and long-tail informational** (-3,503). The rebuild traded a working long-form content footprint for a better-structured brand site.
- **Method rule that follows from all this: never analyse a single 90-day snapshot in isolation.** Every run from 1 to 7 did exactly that, which is why a 60% collapse was invisible while the playbook confidently attributed flat performance to "authority". Always compare the window to the preceding one, and pull `dimensions: "date"` when anything looks structural.
- **GSC 90d baseline (2026-05-25 → 2026-08-22): 42 clicks / 9,500 impressions / 0.44% CTR.** Use 90d windows. 819 query×page rows total, so the full dataset is small enough to pull whole.
- **THE "ZERO NON-BRAND CLICKS" HEADLINE IS A STATEMENT ABOUT THE NAMED-QUERY TABLE, NOT ABOUT THE SITE. Established run 16.** Two matched 28-day windows, **site totals**: 1–28 Jul = 36 clicks / 4,732 imp / 0.76% / pos 28.3; 29 Jul–25 Aug = **68 clicks / 5,248 imp / 1.30% / pos 26.8**. Clicks +89%, CTR +71%. But only **three queries site-wide received any click** — `we scale startups` 16, `we scale` 3, `wescalestartups` 3 — so **46 of the 68 clicks (68%) came from queries Google anonymised.** Ten non-homepage URLs took 21 clicks between them and **not one appears in the named-query click data**: `/about/daniel` 8, `/resources` 4, `/facts/we-scale-startups` 2, then `/book`, `/growth-operating-system`, `/industries/saas-growth`, `/insights/what-is-a-growth-operating-system`, `/pricing`, `/services/90-day-growth-sprint`, `/team` at 1 each. Prior window: 21 anonymised, 12 non-homepage. **Anonymised clicks 21 → 46 (+119%); non-homepage clicks 12 → 21 (+75%).** Three landed on money pages (`/book`, `/pricing`, the sprint page) against two prior.
  - **Two numbers every run must now read, neither of which is in the stated baseline: total clicks minus named clicks, and non-homepage clicks.** Both roughly doubled this window while the named table went backwards on non-brand. That contradiction is the single most important thing in the current data.
  - **Caveat, stated plainly: anonymised proves *rare*, not *non-brand*.** A long brand-adjacent query is anonymised too. But brand queries are high-volume by construction, and `/about/daniel` taking 8 clicks at position 3.1 with zero named brand queries behind it is not a navigational pattern. Strong evidence, not proof. The honest instrument is conversion data, not GSC.
- **AI PANEL BASELINE, run 16 — Perplexity 0/10, Google AI Mode 2/10, zero accuracy defects.** The first time in sixteen runs anyone checked whether WSS is actually cited. Google cited `/services/90-day-growth-sprint` (prompt 3) and `/facts/we-scale-startups` + `/about` + `/press` (prompt 5), quoting the £8k–£12k band, "6–8 experiments" and "12 weeks" correctly and framing WSS as a consultancy rather than an agency. **Both wins came from pages built to be extracted — the first external evidence that the site's deliberate GEO work does anything.** Perplexity's zero is uniform, not marginal: WSS is absent from its source graph on all ten prompts. The eight misses are an authority result, not a content one. Full table and the competitive set in `AI-VISIBILITY.md`. **One reading is a baseline. Do not read it as a trend.**
- **Trend as of 2026-08-26 — the site is slowly improving and it is not noise.** Three consecutive 14-day windows: 14–27 Jul **18 clicks / 2,398 imp / 0.75% CTR / pos 27.9**; 28 Jul–10 Aug **36 / 2,903 / 1.24% / 28.4**; 11–24 Aug **35 / 2,560 / 1.37% / 25.0**. Clicks roughly doubled and held, CTR rose in both steps, position improved 2.9 places in the latest window. Impressions are flat-to-down (−11.8% in the last window), which is consistent with the post-cliff footprint rather than further decay. **Read clicks and CTR here, not impressions.**
- **`page` and `page,query` will disagree, and the gap is the finding.** A page whose named-query rows do not reconcile to its page-level total is being found by long, rare, conversational prompts — the AI-visibility signature. `/insights/b2b-saas-gtm-strategy` was at 100%: 65 impressions under `page`, zero rows under `page,query`. **Pull both and diff them deliberately.**
- **Bing: 29 referring domains / 41 referring pages / 16 anchor texts** (UI figure, 2026-08-25). The "Bing reports zero inbound links" claim in runs 2–5 was a broken endpoint mistaken for a finding — never repeat it. 29 domains is low for these head terms; the authority thesis stands.
- **The entity is contested, and that is evidenced.** Bing's second-largest query is `wescale login` (106 imp) — the wescale.com e-procurement platform; login-intent variants are ~25% of apparent brand traffic. Two further WeScale businesses and **seven distinct Daniel Johnson LinkedIn profiles** also compete. Off-site entity work is an evidenced priority, not best practice.
- **Agents, not just humans, are doing vendor research here, and the site ranks position 1 for it.** Bing shows verbatim operator strings and a truncated system prompt among its queries; ~23% of Bing impressions are conversational or machine-issued. What they extract is **fit criteria, price band and contact** — keep those stated plainly on `/facts/*` and every service page.
- **Bing is a diagnostic mirror, not a channel.** 3,310 impressions / 30 clicks over 16 months; two genuine non-brand commercial clicks in that period. Do not spend a run optimising for it. Do read it when Google's data is ambiguous — it exposed both findings above.
- **The constraint is authority — but a large part of it was self-inflicted.** Technical foundation is good; commercial head terms sit at position 40–72 on 29 referring domains. Do not repeat "nothing points at this site" as the whole explanation without accounting for the May 2026 cliff.
- **Where this site ranks, it ranks for questions, not keywords.** This is the run-4 and run-5 thesis merged, and it is the single most useful idea in this file. Two independent confirmations:
  - `/services/90-day-growth-sprint` holds **position 1.0–5.6** on provider-selection prompts ("who offers / top providers of / which agencies provide / get a quote for"). Eight of the ten site-wide non-brand queries at position ≤5 land there.
  - `/insights/when-to-hire-fractional-cmo` holds **position 8.0–8.3** on three-way hiring-decision prompts ("should a series a startup hire a fractional cmo a growth agency or wait to hire a full-time vp of growth", 38 imp across two variants). The same decision shape recurs across the whole cluster at every position from 8.5 to 94.3.
  - Meanwhile `/services/fractional-cmo` sits at **position 51.8 across 1,624 impressions** of head terms (`b2b fractional cmo` 244 imp @ 51.7, `saas fractional cmo` 215 @ 43.8). Head terms are authority-bound. Question-shaped queries are not.
  - **Working rule:** find the queries phrased as questions or decisions, check which page Google already chose for them, and make that page answer the question in one extractable passage. Do not fight for the head term.
  - **Corollary found in run 9:** the rule assumes a page exists to carry the answer. Where the question-shaped query has *no* page behind it, the constraint is absence, not authority, and writing the page is the cheapest win available.
- **The rule "don't optimise titles/meta at position 45+" holds — and implies its inverse.** At position 1–10 the snippet and the first extractable passage are the whole lever. Runs 4 and 5 both acted on that.
- **Profile-page impressions are largely brand sitelink artefacts. Do not chase them.** `/proof`, `/press`, `/about/daniel`, `/start-here`, `/podcast`, `/speaking` are mostly the single query `we scale startups` at position 1.0 with zero clicks — Google logs one impression per sitelink URL at the parent's position and the homepage absorbs the clicks. The tell is the same query at the same position across many pages. **Check the query mix before believing a page-level position.** Caveat added run 16: `/about/daniel` took 8 real clicks this window from anonymised queries, so "sitelink artefact" is not a licence to ignore a page's clicks.
- **A second chunk of homepage impressions is brand-name collision**, not demand: `we scale`, `start and scale`, `wescale`, `we scale ab` (a Swedish company), `calconnect login`, `google for startups in shoreditch` (39 imp at pos 3.1 — someone looking for Google's campus, not WSS). ~800+ impressions of people looking for someone else. Discount them from every CTR calculation.
- **The site has AI visibility it does not have classic search visibility.** Hundreds of impressions of long, conversational, personally-phrased prompts, all zero-click. Optimise for passage extraction — a direct-answer paragraph under a heading, named facts, prices, durations, comparison tables — not for titles the user may never see.
- Entity graph is rooted on danieljohnson.xyz for Person, wescalestartups.com for Organization. `/facts/*` pages carry `lastUpdated` — keep them monthly-fresh.
- The repo emits six `ai:*` meta tags on all pages, five hardcoded and identical sitewide. No crawler consumes those names. Harmless, inert, **do not expand**. Note `meta-ai:summary` is derived from the page description, so improving a description improves it for free.
- `site.ts` is the single source of truth for pricing/proof/FAQ copy; page copy MUST match `pricingTiers`. The **numeric** ranges for structured data live in `src/lastmod.ts` (`servicePriceRanges`). `/insights/fractional-cmo-cost-uk` is the canonical place for the agency (£6k–£20k/mo) and full-time (£120k–£180k base) comparison bands — reuse those, do not invent new ones.
- **robots.txt says `ai-train=yes`, and always has since run 2. CORRECTED 2026-08-25 (audit).** The previous version of this bullet claimed Daniel had reverted run 2's change to `no` and told future runs not to re-flip it. That is false. The live file says `ai-train=yes`; `git log src/pages/robots.txt.ts` shows its last commit is run 2's `423d8ea`, "Resolve the robots.txt contradiction in favour of being trained on"; **Daniel never reverted anything.** Run 4 invented the reversal and runs 5-7 repeated it without checking. The current state is coherent (GPTBot/ClaudeBot are `Allow`ed and the signal says `yes`) and should be left alone — but leave it alone because it is correct, not because of a decision that never happened.
- Daniel works in the repo directly, and so does this bot several times a day. Check `list_commits` before choosing a file; do not touch a template another run edited hours ago.

## Connector and environment notes

- **The sandbox can clone, install and build the repo.** `git clone` + `npm ci` (~22s) + `npm run build` (~3s after OG generation) all work; the GitHub connector pushes. **Verify every change this way before committing** — build, `astro check`, JSON-LD parsing, rendered HTML and internal-link targets, all before the commit exists.
  - Trap 1: `git clone` writes ~10,000 lines of output and will blow the tool's token limit. Redirect everything to a log file and `tail -c` it.
  - Trap 2: **a shallow clone breaks `refresh:lastmod`** — with `--depth 1` every file resolves to the clone date and the script rewrites all 78 routes. Use `git fetch --unshallow` first. (Same reason CI needs `fetch-depth: 0`.)
  - Trap 3: cloned files are read-only to `rm -rf`; use a fresh directory name rather than deleting.
  - Trap 4: **no git credentials in the sandbox.** Every write goes through the GitHub connector, which replaces whole files, so a large append-only file cannot be safely appended to — re-emitting 40KB from context risks silent corruption. **This is why the changelog is split per run. Keep it that way.**
  - Trap 5: **`check:lastmod` cannot be verified before the content commit exists, and a green result pre-commit means nothing.** The script resolves each route to the last *git commit* touching its backing file. Run against a sandbox tree where the file is modified but uncommitted, a stale route still resolves to its old date and the check passes. It only goes red once the content lands. **Always re-run `check:lastmod` against the pushed tree**, and expect to need the second half of the two-step.
  - Trap 6: **the GitHub connector rejects an abbreviated or reconstructed blob SHA.** Expanding `e54b449` into a plausible 40-character string fails as stale, which is the system working. Always fetch the real SHA with `get_file_contents` (or `git rev-parse HEAD:path` against a freshly fetched tree) immediately before writing. Never reconstruct one.
  - Trap 7: **`refresh-lastmod.mjs` writes dates in the runner's local timezone** — a one-day discrepancy is harmless, do not debug it. On **shared dynamic routes it cannot tell which slug changed**: editing one service bumps all four `/services/*`. Harmless, but never read a `/services/*` lastmod as evidence that page changed.
  - Trap 8: **`web_fetch` can return an empty body for a live, working page on this site.** Run 15 got three empty responses for a freshly deployed URL while `web_fetch` of `/sitemap.xml` on the same host worked normally. The page was fine — the Chrome connector returned the full article. Never treat an empty `web_fetch` as evidence a page is broken, and never revert a commit on that basis. Confirm through the Chrome connector or the deployed `sitemap.xml` first.
- **`staticPathLastModified` covers 82 routes including SOME `/insights/*`** — the four pillars (`what-is-a-fractional-cmo`, `b2b-saas-gtm-strategy`, `ai-native-gtm`, `startup-growth-bottlenecks`) plus `/insights` and `/insights/glossary`. Every other insight falls back to `siteConfig.siteLastModified`. **So the sitemap two-step DOES apply to the pillars**, and to static pages and service/case content. Runs 5–8 recorded the opposite and run 9 left CI red trusting it. **Do not guess: `grep` the route in `src/lastmod.ts`.**
- **GSC MCP `row_limit` is yours to set and the default (100) silently truncates.** Rows come back clicks-desc first, then **zero-click rows sorted alphabetically by (page, query)** — a low limit guillotines everything after roughly `/f…`. Always pass `row_limit: 25000`. `dimensions: "page,query"` at that limit returns the whole 819-row dataset in one call.
- Large GSC pulls exceed the tool output limit. Run them in a **subagent** with an explicit list of the figures you want back, so the dump stays out of main context.
- 60–89% of a page's impressions — and **68% of the site's clicks** — are **anonymised** by Google (rare, personally-phrased queries). Some pages are at 100%. That gap is itself a signal and it skews heavily conversational.
- GSC MCP is read/inspect only — no request-indexing endpoint. Recrawl comes via the sitemap.
- **Bing MCP date endpoints were fixed 2026-08-25** — `parse_timestamp_from_api` returned a naive datetime, failing FastMCP's RFC 3339 `date-time` output validation. Patched to `tz=timezone.utc` in the vendored lib. `get_query_stats`, `get_page_stats`, `get_rank_and_traffic_stats`, `get_url_info` all work now. **A reinstall of the extension wipes this patch** — if those endpoints start erroring again, that is why. Backup: `utils.py.bak`.
- **Bing quirks:** query stats are weekly buckets dated to Fridays covering ~36% of impressions — never compare to GSC dailies. Backlinks must be read from the UI (`GetLinkCounts` returns empty regardless). Tools need the `self="bing"` arg. Quota 100/day, 600/month; `submit_url` returns `null` on success. Crawl-issue data is **historical** and still lists URLs fixed weeks ago — re-check live before acting.
- Cloudflare MCP has no Pages-deployment tools; verify deploys by fetching the live URL with a cache-busting query string.
- **`mcp__workspace__bash` curl works fine against the site** for status codes and redirect chains, despite the SKILL note about bot challenges. `web_fetch` sometimes returns a stale cached body — **trust curl status codes over web_fetch bodies when checking redirects.**

## Ranked backlog

1. **CLOSED. Migration recovery complete** (run 13). Nine 404s redirected, VC guide restored, GTM guide replaced with Daniel's own 4,300-word original behind the existing 301. **Verdict 2026-09-23.** The lesson worth keeping: it worked because Daniel had the body, not because a run was clever — writing 4,000 words of unsourced GTM advice produces the undifferentiated filler the VC run identified as valueless.

2. **CLOSED. All four designated pillars written to depth** (runs 9 and 13) — `what-is-a-fractional-cmo`, `startup-growth-bottlenecks`, `ai-native-gtm`, `b2b-saas-gtm-strategy`. **Verdicts 2026-09-16 and 2026-09-23; baseline to beat is 1 and 6 impressions.** Do not rewrite before the verdict date. **If they are still at zero on 2026-09-23 the pillar thesis is authority-bound too, and that changes the backlog.**
3. **Off-site authority — WORKSTREAM C, now lives in `seo-bot/OUTREACH.md`. Read that file, not this entry.** Settled by runs 11–13: Person schema fixed (the three institutions moved from `sameAs` to `affiliation`), Organization `sameAs` 7 → 11, founding year unified to **2016** everywhere, canonical email `daniel@`, name standardised as **We Scale Startups** (three words), **Ltd** removed from Crunchbase. **Standing decisions from Daniel: no Companies House registration; display name stays "Daniel Johnson" ("Cameron" lives in schema `additionalName`).** Open and blocked on Daniel: 5+ Clutch reviews (copy drafted in run-12 outputs), the GrowthMentor profile's website field, Instagram's Linktree, Facebook's `hello@` and its Dec-2022 placeholder post, and the Wikidata Q137046365 decision — **read the run-11 log first; the evidence that answer engines use it at inference is thin to nonexistent and the item is a small liability rather than an asset.**
4. **Original research from the 479+ founder sessions.** The highest-leverage single asset available and the only one a competitor cannot copy. Original data is one of the few documented drivers of AI citation. Feeds items 1 and 2 simultaneously. Requires real data from Daniel — never fabricate figures.
5. **CLOSED (run 13). `/insights` hub rewritten for provider intent** — it was pulling 1,032 impressions / 0 clicks at 28.9 on post-PMF provider queries while serving a link list. **Verdict 2026-09-23. Watch clicks, not impressions.** One informational query is queued against this page for after that date — see backlog 10.
6. **Homepage non-brand category cluster — considered in run 5 and deliberately deferred.** ~630 impressions at positions 7–13 with one click: `scale startup` (309 @ 12.6), `marketing startups` (126 @ 13.2), `startup development agency` (82 @ 12.4), `companies that help startups scale` (36 @ 12.0), `growth marketing agency` (31 @ 9.1), `growth marketing agency for startups` (25 @ 9.4). Tempting because it is the largest non-brand pool on page 1–2. Deferred because the fix would mean re-anchoring the homepage on "agency", which contradicts WSS's own positioning (the site sells *against* the agency model and has a page saying so), and because the homepage title/description is brand-critical and sits on top of Daniel's design work. Revisit only if a non-homepage page starts picking these up, or if Daniel wants a dedicated category page.
7. **Finish the abandoned growth-system consolidation.** `/growth-engine`, `/ai-driven-growth`, `/how-we-work` are treated as consolidated in `sitemapCanonical.ts` and have `_redirects` rules, yet still collect a handful of impressions. Low priority.
8. **`scalable land acquisition teams` (60 imp) is hitting `/services/acquisition-system-build`** — real-estate intent, wrong market. Harmless, but if the page ever needs a rewrite, tighten "acquisition" to "customer acquisition" in the visible copy.
9. **Housekeeping — done run 13.** `llms.txt` is a real index now, not a 96% copy of `llms-full.txt`; oversized team PNGs go through `scripts/optimise-raster-images.mjs`, which **preserves alpha** — check `getchannel("A").getextrema()` before touching any PNG. **Deliberately not done: the three 166–173 char meta descriptions** (assessed and declined three times — do not raise again). Still open, config not content: HTML is never edge-cached (`cf-cache-status: DYNAMIC`) on a fully static build.

**Closed:** `cal…/auth/login` (already correctly noindexed via Traefik, run 3). The `www.` legacy host (run 4). **Legacy `/portfolio/*` URLs** — re-checked live in run 5: `www…/portfolio/equoo/` → apex → `/case-studies/equoo`, both 301, chain clean. The GSC rows showing `www…/portfolio/equoo/` as a landing page are historical. **Extending the run-4 pattern into the fractional-CMO cluster** — done in run 5 on the one page that ranks for it, and in run 9 on the one page that had nothing to rank with; the remainder of that cluster is at position 45+ and is authority-bound, so do not repeat it there.

10. **Sweep for commercial pages ranking on informational queries — DEMOTED run 16 after a full sweep returned a small residue.** The pattern is real and run 15 took the only instance worth having. Everything left, current window: `whats the best fractional cmo… for a venture-backed startup` 19 imp @ **44.6** and two more on `/services/fractional-cmo` at 94.3 and 12.0 (prohibition 12 applies at 45+ and the authority verdict there is already recorded twice); `how to scale b2b saas acquisition` 10 imp @ 35.5 on `/services/acquisition-system-build` — clean but too small to justify a page. **37 impressions in total against run 15's 110.** `/pricing`, `/contact`, `/book`, `/services` and `/services/growth-diagnosis` returned no informational rows at all. **One live hit is queued, not dead:** `how does a post-pmf plg saas company scale acquisition without breaking unit economics`, 16 imp @ 36.1, landing on the bare `/insights` index — genuine, and blocked until run 13's verdict window closes **2026-09-23**. **Keep the sweep — it costs one pull. Do not rely on it to produce a shippable change.**

**Do not do:** Optimise any page for the site's own framework vocabulary — *layer*, *five-layer*, *bottleneck*, *scorecard*, *constraint*, *framework*. Run 14 pulled 90 days at `page,query` and the whole cluster is **14 queries, 56 impressions, zero clicks**, with no query containing "layer" as a growth term existing at all; `/diagnose` has 2 impressions and `/resources/growth-dependency` has zero. Fix those pages for buyers and for passage extraction, never for rank. Do not re-assess the three 166–173 character meta descriptions (see backlog 9 — assessed three times, declined three times). Register at Companies House, or describe WSS as an Ltd anywhere — **it is not a limited company** and Daniel has declined to register. Expand `llms.txt` or the `ai:*` meta tags. Add FAQPage schema *expecting rich results* (deprecated May–Aug 2026) — FAQ content is still worth writing for passage extraction, which is a different rationale. Build the `/alternatives/*` page programme. Ship `Review`/`AggregateRating`. Optimise titles/meta for queries at position 45+. Chase CTR on brand-sitelink impressions. Re-flip the robots.txt `ai-train` directive. Re-anchor the homepage on "agency" (see backlog 6). Merge the per-run changelog files back into one file (see Trap 4).

## What worked / what didn't

- **run 1 — FAILED, twice read.** Query-family re-anchor of `/fractional-cmo-vs-agency`: 740 named-query impressions, weighted position 46.2, **zero clicks**, against the 44.1 that motivated it. **Content is necessary but not sufficient at position 45** — not "content doesn't matter"; runs 4 and 5 act where content *is* sufficient. Do not rewrite this page again.
- **run 2** — bug fixes, generated lastmod, Offer schema, CI guard. Verdict **2026-09-04**.
- **run 3** — fractional-CMO consolidation. Verdict **2026-10-03**; 301 spot-check **2026-09-19**.
- **run 4** — provider-answer passage + FAQs on the sprint page; three legacy 404s redirected. Verdict **2026-09-01**. Provider intent only — do not confuse with run 15's informational verdict on the same page.
- **run 5** — three-way decision answer, comparison table, four FAQs on `/insights/when-to-hire-fractional-cmo`. Verdict **2026-09-08**. Now at 71 impressions / pos 9.3 / zero clicks; the Series A three-way question is 43 impressions at pos ~7.9 across two punctuation variants.
- **run 9** — `/insights/what-is-a-fractional-cmo` to pillar depth (2,335 words), owning definition/scope only, five-way comparison table. Verdict **2026-09-16**. Baseline to beat is literally zero impressions.
- **runs 10–13** — one long interactive session with Daniel: sitewide audit, entity/schema repair, off-site profiles, three pillars, `/insights` hub, four orphans. Verdicts **2026-09-23**. ~4x an unattended run's scope, possible only because Daniel supplied the GTM guide and made the positioning calls. **Do not try to reproduce this scope unattended.**
- **An orphan page is not one diagnosis.** Run 13 found four pages with zero inbound internal links and gave them three different answers: link it (three of them, from pillars that actually have traffic), or noindex it (`/wss-calendar`, whose own headings are "What WSS Calendar does" and "Privacy and terms"). The useful question is not "how do I link this" but "why did nobody link it". Sometimes the honest answer is that it should never have been in the index.
- **Indexed and invisible is a content verdict, not a technical one.** Run 9's target returned URL-inspection verdict PASS, "Submitted and indexed", clean canonical, robots ALLOWED — and zero impressions in 90 days, last crawled 2026-07-24. Before diagnosing a zero-impression page as a crawl, canonical or sitemap problem, inspect it: if Google already has it, the problem is that there is nothing on it worth ranking.
- **Run 2 broke `main` for ~20 minutes** by pushing `schema.ts` before the module it imported from. See standing decisions.
- **Run 9 left CI red for ~7 minutes** by trusting a false claim in this file instead of checking `src/lastmod.ts`, and skipping the second half of the two-step. Caught by re-running `check:lastmod` against the pushed tree in the final verification pass — which is the only reason it was caught at all. See Trap 5.
- **Run 4 spent significant effort on a false lead** — pages showing "position 3–8, hundreds of impressions, zero clicks" that turned out to be brand sitelink stacking. The tell was the *same* query at the *same* position 1.0 across seven pages.
- **Run 5 found no quick wins, and that is worth recording.** Sitewide audit of `dist`: zero images missing `alt`, all internal link targets resolve, all 596 JSON-LD blocks parse, the legacy 404 families from run 4 all redirect correctly. The hygiene layer is genuinely clean — stop re-auditing it every run and spend the time on the query data instead. Run 9 followed this and did not re-audit.
- **2026-08-27 run 15** — new `/insights/how-to-run-a-90-day-growth-sprint`, plus a quick-win lastmod two-step cleaning up after run 14. Pending, due **2026-09-17**. Baseline is four queries, 110 impressions, positions 11.3–15.0, **zero clicks**, all landing on the £8k–£12k service page. Read **clicks**, and whether the new URL takes the informational queries off `/services/90-day-growth-sprint`. A position drop on that service page for those four queries is the system working, not a regression — do not confuse it with run 4's provider-query verdict on 2026-09-01, which is a different intent on the same page.
- **Positioning rule, added run 15** — put the direct answer in the **first 30% of the page**, not the conclusion. Current GEO research puts roughly 55% of AI Overview citations in the opening third of the cited page. Run 15's definition sits at 7% of rendered page depth by measurement, not by eye — measure it in `dist/*.html` rather than assuming.
- **THE GSC GENERATIVE-AI REPORT IS READABLE, AND RUN 16 READ IT.** Flagged by run 15, wrongly closed as unreachable earlier in run 16, then opened in the Search Console UI via the Chrome connector the same day. **The MCP connector genuinely cannot reach it** — `gsc_query` exposes `query, page, country, device, date` only, and `dimensions: "searchAppearance"` returns an empty array rather than an error. **But the UI can, and the bot can drive the UI.** The wrong conclusion was drawn by testing the connector and stopping there. *Do not conclude a data source is unavailable without trying the browser.*
  - **Access note:** Chrome defaults to `admindjohnson@gmail.com`, which does **not** have the property. Switch to `daniel@wescalestartups.com` in the account picker, or append `/u/1/`. Report lives at Performance → **Generative AI** (Beta), or `search.google.com/u/1/search-console/performance/search-analytics/ai?resource_id=sc-domain:wescalestartups.com`.
  - **Impressions only. There is no clicks metric and no QUERIES tab** — only Pages, Countries, Devices, Days. So it tells you *which pages* get surfaced in AI features, never which prompt surfaced them. The AI panel in `AI-VISIBILITY.md` remains the only instrument for prompt-level visibility; the two are complements, not substitutes.
  - **BASELINE 2026-08-27: 653 AI impressions over 3 months, 217 over 28 days** (~7.2/day vs ~7.75/day — growing, but only ~8%, so do not call it a trend yet). 50 pages surfaced over 3 months, 25 over 28 days.
  - **Top pages, 28 days:** `/services/90-day-growth-sprint` **37** · `/fractional-cmo-vs-agency` **18** · `/about` 16 · `/resources` 15 · `/services/fractional-cmo` 11 · `/gtm-strategy` 10 · `/services/growth-diagnosis` 9 · `/facts/we-scale-startups` **7** · `/about/daniel` 6, with the homepage above them. Over 3 months the homepage takes 256 and the sprint page 151 — **62% of all AI impressions between two URLs.**
  - **Two instruments now agree independently.** The AI panel found Google AI Mode citing `/services/90-day-growth-sprint` and `/facts/we-scale-startups`; this report ranks them 2nd and 8th by AI impressions. That is corroboration from a completely different measurement path, and it is the strongest evidence yet that the extraction-oriented page design works.
  - **`/fractional-cmo-vs-agency` is not dead.** It carries 574 classic impressions at position 44.2 with zero clicks and two failed verdicts — and it is the **2nd-highest non-homepage AI-cited page at 18 impressions.** Classic position is the wrong lens on it. This does **not** license another rewrite (run 1 failed twice); it means the page is being consumed where rank does not apply, and its value should be judged on AI impressions from here.
- **CORRECTION, run 16: the "90d baseline 42 clicks / 9,500 impressions" line was wrong.** The Search Console UI shows **144 clicks / 16.3k impressions / 0.9% CTR / position 25.9** for the trailing 3 months (Search type: Web). The API's own 28-day pulls (36 then 68 clicks) reconcile with the UI figure and not with the old baseline. **Use 144 / 16.3k as the 90-day reference.** Where the API and the UI disagree, check the UI before writing a number into this file.
- **2026-08-27 run 16 — the first run to ship nothing on purpose, and the first to measure the thing the mandate is actually about.** Every page with demand was inside a verdict window or committed in the previous 24 hours; the one unattended backlog lead swept small (see backlog 10); and the first AI-panel reading said the constraint on eight of ten buyer prompts is that WSS is not in the retrieval set. **A run that ships nothing and records why is a successful run.** Fifteen consecutive shipping runs produced zero named non-brand clicks; the discipline being established here is that the alternative to shipping is measuring, not manufacturing work.
- **Workstream C now has an evidenced thesis, not a generic one.** On the panel's prompt 9, Google AI Mode surfaced a third-party listicle — "Growth Division, GrowthCurve, Kurve, Rise Marketing, and **WeScaleStartups**" — in its sources but not its answer. The engines retrieve **provider listicles and directories**, and WSS is already inside one. That makes the target class specific and reachable: get onto the pages the engines already read for these questions. Method: for every prompt where WSS is not named, look at what *was* cited, and keep only the directories and open roundups. Ten named competitors are the target list. **Do not pay for placement and do not ask a competitor to add WSS to their own listicle.** Queued in `OUTREACH.md`.

## Standing decisions

- **Every commit boundary must be a state that builds.** If a change spans files, push the files together.
- **Verify the build against the tree you pushed, not the tree you have locally.** Since run 5, do this in the sandbox: clone, `npm ci`, apply the change, `npm run build` + `astro check`, and inspect the rendered `dist` HTML — *before* the commit exists.
- **Then verify again AFTER the push.** Fetch `origin/main`, rebuild from it, and re-run `check:lastmod`. Some checks — `check:lastmod` above all — are structurally incapable of failing before the commit exists (Trap 5). A run is not finished until the pushed tree has been built and checked.
- **Noindexing a static page is a two-file change.** `src/pages/sitemap.xml.ts` keeps a hand-maintained list of static paths, so setting `metaRobots="noindex"` on the page does not remove it from the sitemap, and a sitemap advertising a noindex URL is a contradictory signal. Change both, then check the arithmetic: pages built minus noindexed must equal the sitemap `<loc>` count. It did after run 13 — 117 built, 11 noindexed, 106 in the sitemap.
- **Sitemap lastmod is generated, not hand-edited.** Two-step: commit the content, then `npm run refresh:lastmod` and commit the result. Applies to any route listed in `src/lastmod.ts` — static pages, service/case content, and the four `/insights/*` pillars. `grep` the route rather than assuming.
- Prefer content-only commits; avoid layout/CSS the bot can't visually verify. Reusing an existing component on an existing code path is fine; adding a page to a template's special-case list is not worth it if the same text already renders elsewhere in the DOM.
- Always fetch the live URL post-deploy before declaring success, and re-test that new redirect rules did not shadow existing specific ones.
- Never state market statistics that aren't sourced; anchor cost claims on WSS's own published ranges, and reuse the bands already published on `/insights/fractional-cmo-cost-uk` rather than minting new ones.
- **Keep the "point them somewhere better" voice.** Every comparison the site publishes should name the case where a competitor or a different model is the right buy. It is the brand voice and it is also what makes a passage worth citing.
- **When a change is defensible under two competing readings of the data, prefer it** over one that requires the ambiguity to be resolved first.
- **Sweep the connectors before writing anything that implies WSS expertise or client work.** The VC guide was written assuming fund-side experience; Granola, Notion, Gmail and Drive established there is none — every named fund is a cold prospect. The real position is accelerator-side (Google for Startups, DeepMind, Techstars, GrowthMentor, several hundred founder sessions). **That check stopped an unsupportable claim reaching a public page.** Do it every time.
- **A hub page must not restate its spokes.** Run 9's pillar owns definition and scope only, and summarises-and-links on timing, cost and the agency comparison. Run 3 spent a whole run undoing a cluster that competed with itself; do not rebuild one.
- **`src/data/growthTools.ts` is the primary source for what the free tools do — not any page that describes them.** Runs 13 and 14 both got this wrong. The Growth Bottleneck Scorecard is **12 questions, ~4 minutes**, scoring **founder dependency** — a different question from the five-layer framework on `/diagnose`. Two instruments, two questions; never describe either in terms of the other.
- **Push large files through a subagent that reads and pushes them itself.** Run 14 moved the 49 KB `src/pages/services/[slug].astro` this way: the subagent runs `git hash-object` on the sandbox file, pushes it, then checks the returned blob SHA matches. The bytes never enter the parent's context and corruption becomes detectable rather than silent. This is the practical answer to Trap 4 for anything large.
- **Judge a page from its rendered HTML, never from source-file size or a source grep.** Run 13 nearly noindexed `/when-growth-plateaus` after grepping its `.astro` file and finding no headings — it uses `StrategicPage`, which supplies them. On a component-driven site the source file is not the page. Measure `dist/*.html` or the live URL.
- **Verify claims about the site against the site, not against this file.** Three false facts have now been found here: an invented robots.txt reversal repeated across four runs, a "Bing reports zero inbound links" line left in place three bullets below the correction that disproved it, and the `/insights/*` lastmod exemption that put CI red in run 9. **The pattern in all three is a half-truth that was never re-checked against the primary source.** Before acting on any factual claim in this file, `grep` the repo or fetch the live URL — and when you correct something, **delete the wrong line** rather than adding a corrected one next to it.
- Mark confidence explicitly (🟢 verified / 🟡 inferred / 🔴 vendor-sourced). Most GEO research is published by companies selling GEO tools.
