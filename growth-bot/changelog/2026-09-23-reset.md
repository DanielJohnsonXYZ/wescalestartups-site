# 2026-09-23: programme reset (live session with Daniel)

Daniel reviewed runs 1–9 and said they weren't useful. What changed:

- **Mission:** own growth across all channels (LinkedIn, outreach, podcasts, referrals, AI referrals, search), not just website CRO on qualified bookings. Count every booking; ICP fit is a lens, not a gate.
- **Cadence:** Monday (full run) and Thursday (follow-through). It was daily.
- **Reporting:** final chat message only. No Slack, email or push. The same change was applied to the acquisition run and the weekly social check-in.
- **Rules:** the bot may now fix real defects (including `public/_headers`) and build pages when the evidence supports it. Cross-bot verdict locks are replaced by a 48-hour same-file check.
- **State:** the old `PLAYBOOK.md` (30KB) and `FINDINGS.md` (26KB) moved to `archive/`. The new `PLAYBOOK.md` holds only durable facts and starting candidates.

## Shipped today

- `ebf5643`: added `https://challenges.cloudflare.com` to CSP `script-src` and `frame-src`. Verified live: `window.turnstile` loads on `/contact` at mobile and desktop widths. The only remaining CSP refusal is Intercom (pre-existing). The form layout is unchanged because the widget is invisible. Tokens stayed empty in headless Chrome, which is expected bot-detection behaviour. `TURNSTILE_ENFORCE` is still unset.

## Data pulled for the reset (GA4 259840282 and GSC, 90d to 2026-09-22)

See `PLAYBOOK.md` → "What we know".
