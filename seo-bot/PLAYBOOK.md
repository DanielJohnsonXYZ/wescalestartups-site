# SEO/GEO/AEO bot — playbook

**Read this, then the last three files in `changelog/`, at run start.** `seo-bot/CHANGELOG.md` holds runs 1–8 and is **frozen**; every run from 9 writes `seo-bot/changelog/YYYY-MM-DD-runN.md`. Runs 10–14 were one interactive session; **read 14 first, it corrects 13.**

**The verdict record and run history moved to `seo-bot/VERDICTS.md` at run 26** (this file had hit 40,874 bytes). Read it for what worked, what failed, and what is due when. **This file stays small enough to re-emit safely every run — that is a hard requirement, not tidiness.**

## Current understanding (2026-09-11, run 26)

- Site: **121 pages built, 11 noindexed, 110 in the sitemap** (run 26). Astro static, push to main = deploy (Cloudflare Pages), live in **under 2 minutes**. **Assert `built − noindexed == sitemap <loc>` after every structural change, and as a standing check — run 26 caught a missing page that way.**
- **THE SITEMAP STATIC-PATH LIST IS HAND-MAINTAINED.** `src/pages/sitemap.xml.ts` keeps top-level routes in a literal array. **A new page Daniel adds will not add itself** — this is how `/mentoring` shipped indexable, nav-linked and absent from the sitemap for a day (run 26), and how `/resources/category-reframe` did before it. **Check the arithmetic whenever `git log` shows a new `src/pages/*.astro`.**
- **SETTLED — the May 2026 cliff. Recovery complete, do not re-diagnose** (`changelog/2026-08-26-run10-audit.md`). **Two leftovers: third-party pages researched around 7 May 2026 describe the OLD site, and the stale `www.` GSC property.**
- **The site-wide average position "decline" is composition, not decay.** Queries present in both windows *improved*; high-position legacy pages left the index and were replaced by new pages at 24–64. **Do not read the site-wide position number as a ranking signal.**
- **"ZERO NON-BRAND CLICKS" IS ABOUT THE NAMED-QUERY TABLE, NOT THE SITE (run 16).** **Four** queries site-wide have ever taken a click — `we scale startups`, `we scale`, `wescalestartups`, and `we scale ab`, **which is another company**. State it precisely: **zero clicks from any named query that is neither WSS's brand nor someone else's.** Most clicks come from anonymised queries whose URLs appear nowhere in the named data. **Read every run: total minus named clicks, and non-homepage clicks.** **Anonymised proves *rare*, not *non-brand*.** Detail: `changelog/2026-08-27-run16.md`.
  - **Clicks / anonymised / non-homepage: 36/21/12 → 72/50/21 → 67/40/20 → 61/37/20 → 61/36/18 → 60/35/18 → 60/34/17.** **MONEY-PAGE CLICKS 2 → 3 → 3 → 5 → 5 → 5 → 5** (run 26, terminal 2026-09-08: `/pricing` 3, `/contact` 1, sprint 1, `/book` 0).
  - **THE LAST FOUR "HELD AT 5" READINGS SHARE 24–27 OF 28 DAYS. THEY ARE ONE OBSERVATION REPORTED FOUR TIMES. Do not stack them into a trend.** **First genuinely independent window: 2026-09-29.**
  - **CHECK THE TERMINAL DATE AND HOW MANY DAYS THE TWO WINDOWS SHARE, BEFORE COMPARING ANYTHING.** Frozen at 2026-09-01 for runs 22 and 23; 09-05 run 24; 09-06 run 25; **09-08 run 26 — a settled ~3-day lag.** Two 28-day windows three days apart are ~90% the same data.
  - **Position improving while clicks stay flat: 72 → 67 → 61 → 61 → 60 → 60, position 26.7 → 24.6 → 24.1 → 22.6 → 22.1.** Windows overlap heavily; a direction, not independent readings.
  - Run 19 measured the anonymised gap at **2,321 of 6,552 = 35.4%**, highest on entity pages (`/about` 51%, `/about/daniel` 48%), lowest on `/fractional-cmo-vs-agency` at 21%.
- **AI PANEL — ten readings. Google AI Mode 2, 2, ~~2~~, ~~1~~, 3, 2, 2, 3, 3, 2/10.** **It is a RANGE (2–3), not a level — run 24 called 3/10 "the current level" on two consecutive readings and it lasted one.** **Reading #10: mention 2/10, own-domain citation 3/10, cited URLs 6, accuracy 2/2, surface 9/10.** Prompts 3 (ranked #2) and 5 (leads, 4 URLs) carry it; prompt 6 lost the body naming but kept the `/` citation.
  - **RUN 26 TOOK NO READING — Claude in Chrome was not connected. NULL, not zero. The series is still ten readings. Do not interpolate.**
  - **Prompt breadth and URL count are separate metrics. Report both.**
  - **PERPLEXITY IS CONTAMINATED AND NO ACCOUNT SETTING FIXES IT. STOP LOOKING FOR ONE.** Run 25 turned "Use search history" OFF, emptied `perplexity.ai/computer/memory`, confirmed Personalisation empty — **all verified, and prompt 6 was still personalised after each.** The residue is most likely visible thread history, which no setting governs. **The instrument is SELF-POISONING: the panel's own repeated WSS queries feed it.**
    - **Asymmetry that keeps half the data usable: contamination manufactures false POSITIVES, not false negatives. ABSENCES stay valid; NAMINGS do not.**
    - **THE TELL IS SEMANTIC AND CANNOT BE REGEX-MATCHED.** It mutated four times in one session. **The invariant: WSS appearing in the second person, as the reader's own organisation, rather than as a vendor recommended to a buyer. READ every Perplexity naming; never pattern-match it.** Never re-add `based on your` — it false-positives.
    - **AWAITING DANIEL: take all Perplexity readings SIGNED OUT** (proposed run 25, broadened run 26 to the whole panel since the built-in browser pane is signed out on every engine). **Changes reading conditions mid-series, so it needs sign-off and a marked series break.** **Until agreed: absences valid, every naming unscoreable and quoted in full. ZERO verified organic Perplexity namings in the whole series.**
  - **COMPLETION TELLS ARE MANDATORY.** Google AI Mode: the literal string `AI Mode response is ready` in `document.body.innerText` — **it does NOT cover the sources card, which renders only after you SCROLL. Scroll to the bottom, THEN read.** Perplexity: a `<button>` whose label starts with `Finished` (`Finished · N steps`). **Every Perplexity reading before run 25 was taken on a fixed wait with no completion check** — run 25 read one prompt at 5,418 chars and again at 8,956.
  - **PROMPT 6 USES AN EM DASH AND MUST BE URL-ENCODED `%E2%80%94` ON EVERY ENGINE.**
  - **GOOGLE BUSINESS PROFILE IS A RETRIEVAL SURFACE** — a GBP card renders inside AI Mode answers. Category corrected to "Business management consultant", confirmed live run 24. **Closed. Treat GBP as part of Workstream A.**
  - **Prompt 6 calls WSS an "agency"** when it names it. No site-side lever exists that does not violate prohibition 15. Recorded, not actionable.
  - **PROMPT 5 STATES THE STAGE CORRECTLY; PROMPT 9 STATES IT WRONGLY, FROM GROWTH DIVISION — same engine, same morning, two sources.** The competitor page is the defect, and correcting it is CLOSED (backlog 13).
  - **Prompt 10's replacement works — no M&A split in #9 or #10. WSS not named in either; that is the baseline. Do not trend across the #8/#9 break.**
  - **GROUNDEDNESS IS VOLATILE PER READING, NOT A PROPERTY OF THE PROMPT.** Prompt 2 cited run 5's page in #9 and was wholly ungrounded in #10, three days later.
  - **A single reading of one prompt is a sample. Report a share across ten, never a result on one.** **Readings #3 and #4 are floors (premature reads); do not draw a trend through them.** **A reading that could not be taken is NULL — never log it as a low share.** **Always state the winnable surface alongside the share.**
  - Panel tables: `AI-VISIBILITY.md` for #1–#9 (FROZEN), then `seo-bot/ai-visibility/YYYY-MM-DD-readingN.md`.
- **`page` and `page,query` will disagree, and the gap is the finding** — a page whose named rows do not reconcile to its total is found by long, rare, conversational prompts: the AI-visibility signature. Sitewide ~35%; **`/resources` 77%.**
- **Bing: 29 referring domains / 41 referring pages / 16 anchor texts** (UI, 2026-08-25). Low for these head terms; **the authority thesis stands.**
- **The entity is contested, and that is evidenced.** The `wescale.com` e-procurement platform, two further WeScale businesses, seven Daniel Johnson LinkedIn profiles, and **`scale startup` (855 impr at 10.3, zero clicks) is Scale AI.** Discount it from every impression and CTR figure. **It reaches page level (76% of `/resources`' named impressions) and it takes clicks — `we scale ab`.**
  - **RUN 26 GENERALISES THIS: ordinary English service nouns collide too.** `/services/acquisition-system-build` is **71% `scalable land acquisition teams`** (35 of 49 named impressions, position 91 — property development). Its recorded "94 impr at 41.4" is mostly that one query dragging the mean; **real topical demand is 13 impressions at 27.0.** **Before reading any page's average position, sum its named set and ask what share is a collision.** Bad nouns: `acquisition`, `scale`, `sprint`, `pod`, `resources`.
- **Agents, not just humans, do vendor research here.** ~23% of Bing impressions are conversational or machine-issued and extract **fit criteria, price band and contact** — state those plainly on `/facts/*` and every service page. **But they do not click. Serve them for citation, never traffic, and NEVER let a fanout ranking justify a content change.**
- **Bing is a diagnostic mirror, not a channel** (3,310 impr / 30 clicks over 16 months). **Never spend a run optimising for it;** skipping it is deliberate, not a gap.
- **The constraint is authority.** Technical foundation is good; commercial head terms sit at 40–72 on **29 referring domains**. **The listicle route into that authority is closed (backlog 16) — what remains is directories, and original research.**
- **Where this site ranks, it ranks for questions, not keywords** — **qualified: it matters whether a HUMAN asked the question.** See the run 1 / 4 / 5 set in `VERDICTS.md`, which is the most load-bearing thing this bot has established. **Head terms are authority-bound; question-shaped queries are not — but only the human ones are worth winning.**
  - **Working rule:** find the queries phrased as questions or decisions, check which page Google chose, and make that page answer it in one extractable passage. Do not fight for the head term.
  - **Corollary (run 9):** where the question has *no* page behind it, the constraint is **absence**, not authority, and writing the page is the cheapest win available.
- **"Don't optimise titles/meta at 45+" implies its inverse:** at 1–10 the snippet and first extractable passage are the whole lever — **but check the query shape first: a 1–10 position on a fanout string is not a human's position.**
- **`/resources` IS NOT A CONVERSION ASSET; BACKLOG 17 CLOSED.** Its 2.7% CTR was an artefact of the `wescale.com` collision; its named set converts at 0%.
- **Profile-page impressions are largely brand sitelink artefacts. Do not chase them** (`/proof`, `/press`, `/start-here`, `/podcast`, `/speaking` — mostly `we scale startups` at 1.0, zero clicks). **Tell: the same query at the same position across many pages.** Not a licence to ignore real clicks: `/about/daniel` 4 clicks / 403 impr at 3.4; `/facts/we-scale-startups` is NOT in this class.
- **The site has AI visibility it does not have classic search visibility**, measured not inferred. Optimise for passage extraction: direct-answer paragraph under a heading, named facts, prices, durations, tables. Entity graph roots on danieljohnson.xyz for Person, wescalestartups.com for Organization.
- **PROMPT 4 IS SETTLED AND THE ANSWER IS NO (run 22). Do not reopen.** Soft SERP, no demand: the cost/pricing cluster is 7 queries / 8 impressions at 31–92, zero clicks; absent from the panel for six readings. **A soft SERP is a statement about competitors, not about demand — do the cheap demand check first.**
- **THE PERSON ENTITY IS REAL TO GOOGLE AND THE SITE IS NOT YET ITS SOURCE (run 21).** Reading #6 named Daniel Johnson as his own entry beside WSS on prompt 5, but cited **LinkedIn, not `/facts/daniel-johnson`**.
- **Both `/facts/*` pages derive their visible freshness date from the generated `staticPathLastModified` map, so rendered date and sitemap `lastmod` cannot drift. Nothing to refresh monthly.** Never switch either to `siteConfig.lastUpdated` (stale). **Founding year 2016 lives in `src/lib/schema.ts:81`, not `src/site.ts`.**
- `site.ts` is the single source of truth for pricing/proof/FAQ copy; page copy MUST match `pricingTiers`. **Numeric** ranges for structured data live in `src/lastmod.ts` (`servicePriceRanges`). `/insights/fractional-cmo-cost-uk` is canonical for the agency (£6k–£20k/mo) and full-time (£120k–£180k) bands — reuse, do not mint new ones.
- **robots.txt `ai-train=yes` is CORRECT and coherent** (GPTBot/ClaudeBot are `Allow`ed). Run 4 invented a "Daniel reverted it" story that runs 5–7 repeated. **Leave it because it is right, not because of a decision that never happened.**
- **THE RUN CADENCE STILL EXCEEDS THE EVIDENCE CADENCE EVEN AT MON/WED/FRI.** **A run that ships nothing and records why is a successful run** — but see run 21 and run 26: **build-verifiable work survives an evidence blackout, so a blacked-out run is not automatically a REPORT run.**
- Daniel works in the repo directly, and so does this bot. **Check `list_commits` before choosing a file**; never touch a page committed in the last 24 hours.

## Connector and environment notes

- **`analytics_query` WORKS AGAIN AS OF RUN 26. The old "IT IS UNUSABLE FOR MEASUREMENT, EVERY DIMENSION ANSWERS ABOUT THE STALE `www.` PROPERTY" instruction was deleted, not softened.** Verified four-for-four against run 25's hand-read UI table on the same window; `["page"]`, `["query"]`, `["date"]` and **`filters`** all correct. **Cause unknown and deliberately not chased** (the server offers 2.1.2 → 2.1.3, unverified as the fix).
  - **RE-RUN THE DISCRIMINATOR EVERY RUN, because a fault that appeared without explanation can return the same way: pull `["page"]` and read row one. A `www.` URL means the wrong property and nothing page- or query-level is trustworthy.**
  - **`filters` is the cheapest instrument this bot has ever had.** An exact-match page filter returns a page's query set in one call — **this is the whole Workstream B method now.** Always pass an explicit `siteUrl`.
  - **The GSC UI-through-the-DOM method is DEMOTED TO FALLBACK, not deleted.** If the discriminator fails: `javascript_tool` on the **visible** table — `[...document.querySelectorAll('table')].find(t => t.offsetParent !== null && t.getBoundingClientRect().height > 0)`. **Necessary and not sufficient: after a tab change, `window.scrollTo(0,900)` to force layout, THEN select, THEN assert `rows[0].cells[0]` reads the heading you expect.** **Clicking a tab by coordinate or `[role="tab"]` often does not register — the tab strip is duplicated in the DOM. Prefer the URL: `&breakdown=date|page|query`, and `&page=!<url-encoded-url>&breakdown=query`.** **`document.body.innerText` on `search.google.com` is refused by the permission classifier — read the table nodes, never the body.**
- **`["searchAppearance"]` STILL RETURNS `[]`, re-confirmed run 26 after the rest of the API was repaired. These were always two separate faults.** The **Generative AI report remains UI-ONLY**, at `/performance/search-analytics/ai?resource_id=…`. Impressions only, no queries tab — it names surfaced *pages*, never the prompt. **Unreachable without a working browser.**
- **`genai_query_insights` is NOT an AI-citation report** — heuristic pattern-matching on ordinary query data, by its own caveat. **Never quote it as citation evidence.**
- **`inspection_inspect` is working-but-PARTIAL:** it returned a full result for `/pricing` and a false "you do not own this site" for `/resources` in the same call on the same property. **A success is trustworthy; a failure is not evidence about the URL.** `sitemaps_list` returns `[]` for a live submitted sitemap. Recrawl comes via the sitemap; `indexing_submit` untested (prohibition 18).
- **BROWSERS: there are two, and they are not interchangeable for the panel.** Claude in Chrome is the signed-in profile the whole ten-reading series was taken on. The built-in browser pane is a **separate, signed-out profile** — fine for reading the live site, competitor pages and `sitemap.xml` (run 26 used it for all three), **but taking a panel reading there is a series break needing Daniel's sign-off.** A "not connected" extension is an **outage**, not a permission refusal (Trap 12) — report it as an ask and take no reading.
- **The two phantom `legacy_*` accounts are RECREATED AT EVERY SERVER START from env vars** (`dist/common/auth/config.js`; blank optional fields arrive as the truthy literal `${user_config…}`, so "do not fill them in" names the cause as the cure). **Removing them is a per-run chore at the top of every run. Do not spend a run diagnosing it.** Root cause and clean fix: `changelog/2026-09-02-run21.md` §B.
- 60–89% of a page's impressions and ~68% of clicks are **anonymised** by Google. The gap is a signal and skews conversational.
- **The sandbox can clone, install and build the repo** (`git clone` + `npm ci` + `npm run build`); the GitHub connector pushes. **Verify every change this way.** The traps that cost real time: **2**, **5**, **8**.
- **`staticPathLastModified` covers 84 routes including SOME `/insights/*`** — the four pillars, `/insights` and `/insights/glossary`; every other insight falls back to `siteConfig.siteLastModified`. **So the sitemap two-step DOES apply to the pillars**, to static pages, and to service/case content. **Do not guess: `grep` the route.** **The script only refreshes routes already in the map — insert a placeholder key and let `refresh:lastmod` write the real date.** Run 26 used exactly this for `/mentoring`.
- **THE SANDBOX IS NOT PERSISTENT: run 25's clone was wiped between two tool calls.** Do clone → edit → build → push in one continuous stretch, and **keep repo edits as a replayable script against the pristine file rather than hand-made changes.**
- **Bing quirks:** weekly buckets dated to Fridays — never compare to GSC dailies. Backlinks from the UI (`GetLinkCounts` empty). Tools need `self="bing"`. Quota 100/day. Site URL `https://wescalestartups.com` — **a `sc-domain:` value returns `InvalidUrl`, which is a parameter error, not an outage.**
- Cloudflare MCP has no Pages-deploy tools; verify deploys by fetching the live URL with a cache-buster. **curl status codes beat `web_fetch` bodies for redirects.**

## Backlog

**Numbers are stable IDs, not priority; never reassign them.** Order within each list is the ranking.

### Open

15. **`how to scale b2b saas acquisition` on `/services/acquisition-system-build`** — a £15k–£25k sales page answering a how-to. **BLOCKED UNTIL 2026-09-17**, when run 15's verdict lands; shipping the second instance before judging the first confounds both. **RUN 26 DID THE SHAPE CHECK EARLY AND THE PREMISE IS WEAKER THAN THIS ENTRY SAID: the query is human-shaped (good), but it is 13 impressions at position 27, not 16 at 30.9, and 71% of the page's named set is a land-acquisition collision.** Judge it on 13 at 27. Detail: `query-map/2026-09-11-run26.md`.

10. **Sweep for commercial pages on informational queries — DEMOTED run 16.** One lead queued: `how does a post-pmf plg saas company scale acquisition…`, 18 impr @ 35.3 on bare `/insights` — blocked until **2026-09-23**, and **run 24 reclassified it FANOUT, so check the shape before acting.** **Now cheap: `filters` does a page's query set in one call.** Next pages to sweep: `/experimentation`, `/gtm-strategy`, `/insights`, `/growth-operating-system`.

12. **`agency brief template` — the page does not rank for its own name** (90 impr @ 34.2); its download is an 83-word skeleton. Soft SERP, real pre-hire buyer. If worked, thicken the deliverable; do **not** chase `agency collaboration template`, a closed decoy.

3. **Off-site authority — WORKSTREAM C lives in `seo-bot/OUTREACH.md` (frozen) plus `seo-bot/outreach/`.** Entity hygiene settled runs 11–13. **Daniel's standing decisions: no Companies House registration; display name stays "Daniel Johnson"; NO client review requests (any platform); NO cold outreach to competitors, which closes C-1.**
  **WORKSTREAM C IS A LIST OF ONE: be the source these pages cite — original research.** Every route that depended on a third party is closed. **Run 24 got the data (backlog 4); Wikidata Q137046365 closed.** **It was never a client-permission ask: the public corpus needed nobody's permission.**

6. **Homepage non-brand category cluster — deferred since run 5.** ~630 impr at 7–13, one click. The fix means re-anchoring on "agency" (prohibition 15). Revisit only if another page picks these up.

### Closed — kept for the reasoning

13. **CLOSED 2026-09-07 ON DANIEL'S STANDING — he has no relationship with Growth Division and will not cold-email a competitor's co-founder. DO NOT RE-PROPOSE, RE-DRAFT OR RE-RANK IT.** The factual case is unchanged, correct, and re-verified live 2026-09-09: the comparison row for "WeScaleStartups" gets the stage, pricing, Clutch status, channels and clients wrong, plus two unpublished performance figures and an unattributed testimonial. **Run 25 proved the causal chain: Perplexity renders WSS's pricing as "Custom (three tiers)" from this page.** **The counter-move is to be the better source, not to correct the worse one** — prompt 5 states the stage correctly every reading because Google builds it from WSS's pages.

16. **CLOSED — THE RETRIEVAL LAYER IS COMPETITOR-OWNED, ON BOTH ENGINES.** All three listicles the engines cite (Growth Division, K3C, dimartec) are competitor self-rankings, each headed by its own author, none including WSS. **Cross-engine, confirmed on Perplexity run 24. Closed because being excluded is the point of the page.** **METHOD RULE: citation proves retrievability, NOT reachability. Open the page and read the headings.**

17. **CLOSED run 23. `/resources` is a false lead** — named set converts at 0%, is 76% `wescale.com` collision, all clicks anonymised.

14. **CLOSED run 21. `/facts/daniel-johnson` freshness signal shipped.** No verdict on rank; shipped under the gate's exception.

11. **CLOSED run 17. `/gtm-strategy`** — below any intervention threshold. Rule earned: **never diagnose a page's intent from its own content — pull its query set.**

4. **CLOSED 2026-08-27. `/insights/what-226-founder-reviews-reveal` shipped** — original research from 226 PUBLIC GrowthMentor reviews; caveats in `RESEARCH.md`. **RUN 24 ADDED A SECOND, PRIVATE CORPUS — 28 advisory transcripts — AND IT INDEPENDENTLY REPRODUCES THE PUBLIC CORPUS'S FINDING 2: in 27 of 28 sessions the presenting problem was not the actual constraint.** Two corpora, opposite methods, same result — **the strongest evidential position WSS has.** **THE TRANSCRIPT CORPUS IS PRIVATE AND UNPUBLISHABLE; anonymisation is not consent. Publish from the PUBLIC corpus only.** **Verdict 2026-09-24, on CITATION not ranking.** **Prompt 8 is held by Lenny's and andrewchen with a founder-dependency diagnostic of exactly the shape WSS has data for.**

1, 2, 5, 9. **Closed — migration recovery, four pillars, `/insights` hub, housekeeping.** See `VERDICTS.md`. Still open, config not content: HTML is never edge-cached (`cf-cache-status: DYNAMIC`).

**Closed:** `cal…/auth/login` (noindexed). Legacy `/portfolio/*` (301 chains clean). Run 4's fractional-CMO pattern (remainder at 45+, authority-bound). **The `www.` legacy host — its GSC URL-prefix property still exists.**

### Do not do — playbook-specific, ON TOP OF the task file's FIXED RULES

*(The FIXED RULES already cover framework vocabulary, the meta descriptions, Companies House/Ltd, llms.txt and `ai:*`, FAQPage-for-rich-results, `/alternatives/*`, Review/AggregateRating, titles at 45+, brand-sitelink CTR, robots `ai-train`, homepage-as-agency, and merging the changelogs. Not restated.)*

- **Target `scale startup`** — a Scale AI collision, not demand (run 18, SERP-verified).
- **Score a personalised Perplexity mention as a win** (run 18, reinstated run 25).
- **Quote `genai_query_insights` as citation evidence** (run 18).
- **Re-optimise `/services/90-day-growth-sprint` for provider intent, or read the collapse of its provider queries as a regression** (run 19 verdict).
- **Call an `analytics_query` without an explicit `siteUrl`** (run 19).
- **Compare a raw panel share between readings without also stating the winnable surface** (run 19).
- **Classify an off-site target from the fact that an engine cited it** (run 23) — open the page and read the headings.
- **Compare two GSC windows without first checking the terminal date and how many days they share** (runs 23, 25).
- **Ship schema in order to obtain a rich result without checking the type is eligible in that context** (run 23).
- **Believe a page-level position, CTR or click figure without summing its named query set and checking for a collision** (run 23 on `/resources`, run 26 on `/services/acquisition-system-build`).
- **Log a panel reading taken in a different browser profile as part of the existing series** (run 26).

## Standing decisions

- **The verification protocol, the lastmod two-step and the noindex two-file rule are in the task file's FIXED RULES. Not restated.** The two that have cost time: `check:lastmod` cannot fail before the content commit exists (Trap 5), so only the post-push pass means anything; and **pages built − noindexed = sitemap `<loc>` count**.
- **Never state unsourced market statistics.** Anchor cost claims on WSS's published ranges; reuse `/insights/fractional-cmo-cost-uk`'s bands.
- **Keep the "point them somewhere better" voice.** Every comparison should name the case where a competitor or a different model is the right buy — brand voice *and* what makes a passage worth citing.
- **When a change is defensible under two competing readings of the data, prefer it** over one needing the ambiguity resolved first.
- **Sweep the connectors before writing anything implying WSS expertise or client work.** The real position is accelerator-side (Google for Startups, DeepMind, Techstars, GrowthMentor, several hundred founder sessions) — **a mentoring relationship, not client work.** Run 20 found a third party had collapsed that distinction off-site and run 21 found the site doing it on `/facts/daniel-johnson`. **Do this check every time.**
- **A hub page must not restate its spokes.** Run 3 spent a run undoing a self-competing cluster.
- **`src/data/growthTools.ts` is the primary source for what the free tools do** — not any page describing them. The Scorecard is **12 questions, ~4 min**, scoring **founder dependency** — not the five-layer framework on `/diagnose`. Never describe either in terms of the other.
- **KEEP FILES SMALL AND CHECK THE RETURNED BLOB SHA.** The connector's `content` is a string literal, so *someone* must regenerate every byte — a subagent only moves that risk. **What works: `git hash-object` the file on the sandbox, push it, and verify the SHA matches.** Run 26 pushed two source files and both matched. **The real defence is keeping each file small enough to emit in one call** — which is why `changelog/`, `ai-visibility/`, `outreach/`, `query-map/` and now `VERDICTS.md` are split out. **`AI-VISIBILITY.md` (64KB) and `OUTREACH.md` (52KB) are FROZEN. Do not append to either.** **`QUERY-MAP.md` is at 28KB and is next.**
- **Verify claims about the site against the site — and claims about WSS's OFF-SITE presence against the PLATFORM, not against whoever described it. FOURTEEN false facts have been found here, same shape every time.** The ledger and the three that still matter as warnings are in `VERDICTS.md`. **`grep` the repo, fetch the live URL, or open the page before acting on anything in here — and when you correct one, DELETE the wrong line.** **Run 26's addition: this applies to claims about the bot's own tooling. "`analytics_query` is unusable" survived five runs because nobody re-ran the one-call discriminator that would have falsified it.**
- Mark confidence explicitly (verified / inferred / vendor-sourced). Most GEO research is published by firms selling GEO tools.
