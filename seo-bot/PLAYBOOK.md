# SEO/GEO/AEO bot — playbook

Updated each run. **Read this, then the changelog, at run start.** `seo-bot/CHANGELOG.md` holds runs 1–8 and is **frozen**; every run from 9 writes `seo-bot/changelog/YYYY-MM-DD-runN.md`. Read the most recent two or three (Trap 4 explains the split). Runs 10–14 were one interactive session; **read 14 first**, it corrects run 13.

## Current understanding (2026-09-09, run 25)

- Site: **120 pages built, 11 noindexed, 109 in the sitemap** (verified run 19), Astro static, push to main = deploy (Cloudflare Pages), live in **under 2 minutes**. **Check that arithmetic after every structural change.**
- **SETTLED — the May 2026 cliff. Recovery complete, do not re-diagnose** (`changelog/2026-08-26-run10-audit.md`). **Two leftovers: third-party pages researched around 7 May 2026 describe the OLD site (the Growth Division listicle is dated 7 May 2026 exactly), and the stale `www.` GSC property.**
- **The site-wide average position "decline" is composition, not decay.** Queries present in both windows *improved*; high-position legacy pages left the index and were replaced by new pages at 24–64. **Do not read the site-wide position number as a ranking signal.**
- **THE "ZERO NON-BRAND CLICKS" HEADLINE IS ABOUT THE NAMED-QUERY TABLE, NOT THE SITE (run 16).** **Four** queries site-wide have ever taken a click — `we scale startups`, `we scale`, `wescalestartups`, and (run 24) **`we scale ab`, which is another company**. So state it precisely: **zero clicks from any named query that is neither WSS's brand nor someone else's.** Most clicks come from anonymised queries whose URLs appear nowhere in the named data. **Read every run: total minus named clicks, and non-homepage clicks.** **Anonymised proves *rare*, not *non-brand*.** Detail: `changelog/2026-08-27-run16.md`.
  - **Clicks / anonymised / non-homepage: 36/21/12 → 72/50/21 → 67/40/20 → 61/37/20 → 61/36/18 → 60/35/18.** **MONEY-PAGE CLICKS 2 → 3 → 3 → 5 → 5 → 5** (run 25, terminal 2026-09-06: `/pricing` 3, sprint 1, `/contact` 1, `/book` 0). **The last two readings share 27 of 28 days and the one before that 24 of 28. Three "held at 5" readings are ONE reading. Do not stack them into a trend.** **First genuinely independent window: 2026-09-29.**
  - **CHECK THE TERMINAL DATE IN THE DAYS TAB BEFORE COMPARING ANY TWO WINDOWS, AND CHECK HOW MANY DAYS THE WINDOWS SHARE.** Frozen at 2026-09-01 for runs 22 AND 23; **2026-09-05** run 24; **2026-09-06** run 25 — **one day in two calendar days, a widening lag.** **Twenty-eight-day windows three days apart are ~90% the same data: an advanced terminal date is NOT the same as an independent reading, and run 25 nearly treated it as one.**
  - **Position improving while clicks stay flat: 72 → 67 → 61 → 61, position 26.7 → 24.6 → 24.1 → 22.6, impressions 5.46k → 6.15k → 6.27k → 6.2k.** Windows overlap heavily; a direction, not independent readings.
  - Anonymised is strong evidence, not proof; the honest instrument is conversion data. **Total-minus-named and non-homepage clicks roughly doubled while the named table went backwards on non-brand. That contradiction is the most important thing in the current data.**
  - Run 19 measured the anonymised gap at **2,321 of 6,552 = 35.4%**, highest on entity pages (`/about` 51%, `/about/daniel` 48%), lowest on `/fractional-cmo-vs-agency` at 21%.
- **AI PANEL — ten readings. Google AI Mode 2, 2, ~~2~~, ~~1~~, 3, 2, 2, 3, 3, 2/10.** **3/10 DID NOT HOLD A THIRD TIME. It is a range, not a level — do not describe 3/10 as "the current level" again.** **Reading #10: mention 2/10, own-domain citation 3/10, cited URLs 6, accuracy 2/2, surface 9/10** (prompt 2 ungrounded). Prompts 3 (ranked #2) and 5 (leads, 4 URLs) carry it; **prompt 6 lost the body naming after two consecutive readings but KEPT the `/` citation** — the flip the file predicted.
  - **Prompt breadth and URL count are separate metrics and moved together in #10 (3/10, 6 URLs) after diverging in #9. Report both.**
  - **PERPLEXITY IS CONTAMINATED AGAIN AND RUN 23'S RESOLUTION IS FALSE (run 25, thirteenth false fact).** Prompt 6 returned *"Given you run We Scale Startups and manage multiple client brands…"*. **Settings → Memory settings shows "Use search history — use previous searches to answer future questions" is ON, on the FREE plan.** Free tier does have memory; run 23's *"Pro had memory, free does not"* was wrong. **The instrument is SELF-POISONING: the panel's own repeated WSS queries are what feed it.**
    - **Asymmetry that keeps half the data usable: contamination manufactures false POSITIVES, not false negatives. Perplexity ABSENCES stay valid; Perplexity NAMINGS do not.** Reading #9's 0/5 stands. Reading #10: prompts 1, 3, 5 absent (valid); **prompt 6 named and demonstrably personalised — NOT a win**; prompt 9 named from `growth-division` with its misspelling, **suspect, not scored**.
    - **The old tell regex is defective twice over (run 25): `based on your` is a FALSE-POSITIVE generator — it matched *"narrowing this down based on your company's stage"* — and it MISSED the real tell, `Given you run`.** Use `Given you run` / `Given your work with` / any reference to WSS as the reader's own company. **Never re-add `based on your` alone.**
    - **Blocked on Daniel: Perplexity → Settings → Memory settings → turn OFF "Use search history".** One toggle; it restores half the panel. Until then mark every Perplexity naming unreliable.
  - **PERPLEXITY COMPLETION TELL, FOUND RUN 25: a button whose label starts with `Finished` (`Finished · N steps`).** Poll for it before reading. Without it the panel was reading partial renders — run 25 took prompt 5 at 5,418 chars and again at 8,956. Same failure mode that corrupted readings #3–#4 on Google.
  - **PROMPT 6 USES AN EM DASH AND MUST BE URL-ENCODED `%E2%80%94` ON EVERY ENGINE.** Run 25 issued the hyphen variant to Perplexity, got a different query, and had to discard and re-take it.
  - **GOOGLE BUSINESS PROFILE IS A RETRIEVAL SURFACE** — a GBP card renders inside AI Mode answers. Category corrected to "Business management consultant" 2026-09-04, confirmed live run 24. **Closed. Treat GBP as part of Workstream A.**
  - **Prompt 6 names WSS and calls it an "agency"** when it names it at all (#9). The GBP defect relocated into the answer body, where there is no field to edit. **No site-side lever exists that does not violate prohibition 15. Recorded, not actionable.**
  - **PROMPT 5 STATES THE STAGE CORRECTLY; PROMPT 9 STATES IT WRONGLY, FROM GROWTH DIVISION — same engine, same morning, two sources. The competitor page is the defect, not the site, and correcting it is CLOSED (backlog 13).**
  - **Prompt 10's replacement works — no M&A split in #9 or #10. WSS not named in either; that is the baseline. Do not trend across the #8/#9 break.**
  - **DETERMINISTIC COMPLETION TELL for AI Mode: the literal string `AI Mode response is ready` in `document.body.innerText`.** It does NOT cover the sources card, which renders only after you SCROLL. Scroll to the bottom, THEN read.
  - **GROUNDEDNESS IS VOLATILE PER READING, NOT A PROPERTY OF THE PROMPT.** Prompt 2 cited run 5's page in #9 and was wholly ungrounded in #10, three days later. Never infer a prompt's winnability from one reading.
  - **A single reading of one prompt is a sample, not a measurement — report a share across ten, never a result on one.** **Readings #3 and #4 are floors (premature reads); do not draw a trend through them.** **A reading that could not be taken is null — never log it as a low share.**
  - Panel tables: `AI-VISIBILITY.md` for readings #1–#9 (FROZEN), then `seo-bot/ai-visibility/YYYY-MM-DD-readingN.md` from #10.
- **`page` and `page,query` will disagree, and the gap is the finding** — a page whose named rows do not reconcile to its total is found by long, rare, conversational prompts: the AI-visibility signature. Sitewide ~35%; **`/resources` 77%.**
- **Bing: 29 referring domains / 41 referring pages / 16 anchor texts** (UI, 2026-08-25). 29 domains is low for these head terms; the authority thesis stands.
- **The entity is contested, and that is evidenced.** The `wescale.com` e-procurement platform, two further WeScale businesses, seven Daniel Johnson LinkedIn profiles, and **`scale startup` (855 impr at 10.3, zero clicks) is Scale AI** — the largest single distortion in this data; discount it from every impression and CTR figure. **It reaches page level (76% of `/resources`' named impressions) and it now takes clicks — `we scale ab`, run 24.**
- **Agents, not just humans, do vendor research here.** ~23% of Bing impressions are conversational or machine-issued and extract **fit criteria, price band and contact** — state those plainly on `/facts/*` and every service page. **But they do not click. Serve them for citation, never traffic, and NEVER let a fanout ranking justify a content change (runs 4 and 5 both).**
- **Bing is a diagnostic mirror, not a channel** (3,310 impr / 30 clicks over 16 months). **Never spend a run optimising for it;** skipping it is deliberate, not a gap.
- **The constraint is authority.** Technical foundation is good; commercial head terms sit at 40–72 on **29 referring domains**. **Run 23: the listicle route into that authority is closed (backlog 16) — what remains is directories, C-1 and original research.**
- **Where this site ranks, it ranks for questions, not keywords** — the most useful idea in this file, **qualified by run 4: it matters whether a HUMAN asked the question.** The sprint page held 1.0–5.6 on provider-fanout prompts for zero clicks; `/insights/when-to-hire-fractional-cmo` holds ~8 on hiring-decision prompts that look human, which run 5's verdict tests. **Head terms are authority-bound; question-shaped queries are not — but only the human ones are worth winning.**
  - **Working rule:** find the queries phrased as questions or decisions, check which page Google chose, and make that page answer it in one extractable passage. Do not fight for the head term.
  - **Corollary (run 9):** the rule assumes a page exists to carry the answer. Where the question has *no* page behind it, the constraint is **absence**, not authority, and writing the page is the cheapest win available.
- **"Don't optimise titles/meta at 45+" implies its inverse:** at 1–10 the snippet and first extractable passage are the whole lever — **but check the query shape first (run 24): a 1–10 position on a fanout string is not a human's position.**
- **`/resources` IS NOT A CONVERSION ASSET; BACKLOG 17 CLOSED (run 23).** Its 2.7% CTR was an artefact of the `wescale.com` collision (76% of named impressions); its named set converts at 0%. **CHECK THE QUERY MIX BEFORE BELIEVING ANY PAGE-LEVEL FIGURE.**
- **Profile-page impressions are largely brand sitelink artefacts. Do not chase them** (`/proof`, `/press`, `/start-here`, `/podcast`, `/speaking` — mostly `we scale startups` at 1.0, zero clicks). **Tell: the same query at the same position across many pages. CHECK THE QUERY MIX BEFORE BELIEVING ANY PAGE-LEVEL NUMBER — position, CTR or clicks (run 23).** Not a licence to ignore real clicks: `/about/daniel` 5 clicks / 419 impr at 3.1; `/facts/we-scale-startups` is NOT in this class.
- **The site has AI visibility it does not have classic search visibility**, measured not inferred. Optimise for passage extraction: direct-answer paragraph under a heading, named facts, prices, durations, tables. Entity graph roots on danieljohnson.xyz for Person, wescalestartups.com for Organization.
- **PROMPT 4 IS SETTLED AND THE ANSWER IS NO (run 22). Do not reopen it.** Soft SERP, no demand: the whole cost/pricing cluster is 7 queries / 8 impressions at 31–92, zero clicks. Absent from the panel for five readings. **A soft SERP is a statement about competitors, not about demand — do the cheap demand check before the expensive SERP check.** Detail: `changelog/2026-09-03-run22.md` §5.
- **THE PERSON ENTITY IS REAL TO GOOGLE AND THE SITE IS NOT YET ITS SOURCE (run 21).** Reading #6 named Daniel Johnson as his own entry beside WSS on prompt 5, but cited **LinkedIn, not `/facts/daniel-johnson`**.
- **Both `/facts/*` pages derive their visible freshness date from the generated `staticPathLastModified` map, so rendered date and sitemap `lastmod` cannot drift. Nothing to refresh monthly.** Never switch either to `siteConfig.lastUpdated` (stale). **Founding year 2016 lives in `src/lib/schema.ts:81`, not `src/site.ts` — grep `schema.ts`.**
- `site.ts` is the single source of truth for pricing/proof/FAQ copy; page copy MUST match `pricingTiers`. **Numeric** ranges for structured data live in `src/lastmod.ts` (`servicePriceRanges`). `/insights/fractional-cmo-cost-uk` is canonical for the agency (£6k–£20k/mo) and full-time (£120k–£180k) bands — reuse, do not mint new ones.
- **robots.txt `ai-train=yes` is CORRECT and coherent** (GPTBot/ClaudeBot are `Allow`ed). Run 4 invented a "Daniel reverted it" story that runs 5–7 repeated. **Leave it because it is right, not because of a decision that never happened.**
- **THE RUN CADENCE STILL EXCEEDS THE EVIDENCE CADENCE EVEN AT MON/WED/FRI** — run 23 found GSC frozen at the terminal date run 22 read. **A run that ships nothing and records why is a successful run.**
- Daniel works in the repo directly, and so does this bot. **Check `list_commits` before choosing a file**; never touch a template edited hours ago.

## Connector and environment notes

- **The sandbox can clone, install and build the repo** (`git clone` + `npm ci` + `npm run build`); the GitHub connector pushes. **Verify every change this way before committing.** The traps that cost real time: **2**, **5**, **8**.
- **`staticPathLastModified` covers 83 routes including SOME `/insights/*`** — the four pillars, `/insights` and `/insights/glossary`; every other insight falls back to `siteConfig.siteLastModified`. **So the sitemap two-step DOES apply to the pillars**, to static pages, and to service/case content. **Do not guess: `grep` the route.** **The script only refreshes routes already in the map — insert a placeholder key and let `refresh:lastmod` write the real date.**
- **GSC MCP `analytics_query` IS UNUSABLE FOR MEASUREMENT. EVERY DIMENSION — INCLUDING `["date"]` — ANSWERS ABOUT THE STALE `www.` PROPERTY WITHOUT ERRORING (run 23).** A run planning around it gets zeros and may record them as a traffic collapse. **There is no working API route; the UI is not the preferred route, it is the only one.** Not the phantom accounts — run 22 cleared them first and the bug persisted. Discriminator: pull `["page"]`, read row one; a `www.` URL means the wrong site.
- **THE SEARCH CONSOLE UI IS THE ONLY WORKING PAGE- AND QUERY-LEVEL INSTRUMENT, READ VIA THE DOM.** `get_page_text` is USELESS here — it returns a stale orphaned container. Use `javascript_tool` and select the **visible** table: `[...document.querySelectorAll('table')].find(t => t.offsetParent !== null && t.getBoundingClientRect().height > 0)`. **Necessary and not sufficient: after clicking a tab, `window.scrollTo(0,900)` to force layout, THEN select, THEN assert `rows[0].cells[0]` reads the heading you expect.** **Run 25: clicking a tab by coordinate or by `[role="tab"]` often does not register — the tab strip is duplicated in the DOM (stale + live). Prefer the URL: `&breakdown=date|page|query`, and `&page=!<url-encoded-url>&breakdown=query` for an exact page filter.** Map `[...t.rows].map(r=>[...r.cells].map(c=>c.textContent))` and strip the `Copy URL to clipboard|Open in new tab|Inspect URL` cruft. Generative AI report: `/performance/search-analytics/ai?resource_id=…`. **Run 25: `document.body.innerText` on `search.google.com` is refused by the permission classifier — read the table, never the body.**
- 60–89% of a page's impressions and ~68% of clicks are **anonymised** by Google. The gap is a signal and skews conversational.
- **`genai_query_insights` is NOT an AI-citation report** — heuristic pattern-matching on ordinary query data, by its own caveat. **Never quote it as citation evidence.** Recrawl comes via the sitemap; `indexing_submit`/`indexing_status` untested (prohibition 18). **`inspection_inspect` is working-but-PARTIAL (run 23): it returned a full result for `/pricing` and a false "you do not own this site" for `/resources` in the same call on the same property. A success is trustworthy; a failure is not evidence about the URL.** `sitemaps_list` returns `[]` for a live submitted sitemap.
- **The two phantom `legacy_*` accounts break BOTH GSC and Bing with `Multiple google accounts found` / `InvalidApiKey`, and are RECREATED AT EVERY SERVER START from env vars** (`dist/common/auth/config.js`; blank optional fields arrive as the truthy literal `${user_config…}`, so "do not fill them in" names the cause as the cure). **Removing them is a per-run chore, not a fix: `accounts_manage remove legacy_google` then `legacy_bing`, at the top of every run. Present again run 22. Do not spend a run diagnosing it.** Root cause and the clean fix: `changelog/2026-09-02-run21.md` §B.
- **The task file's four instrument checks are not optional and the fourth is the newest: read the terminal date in the Days tab.** A frozen window makes a re-measure impossible and a false trend easy (runs 22–23); an advanced one changes what the run can be (run 24).
- **Trap 12 covers the fault/denial distinction. The third case it does NOT cover: a dataset that has not advanced is neither — defer the measurement and say so (run 23).** An "unavailable" connector is a symptom — read the server's stderr before writing down a cause (runs 17–18 blamed re-authorisation; the real fault was missing `node_modules`).
- **`AI-VISIBILITY.md` (64KB) AND `OUTREACH.md` (52KB) ARE FROZEN AS OF RUN 25. DO NOT APPEND TO EITHER.** Both exceed what `get_file_contents` returns in one call, and — the binding constraint — **the connector REPLACES whole files, so any edit to a 64KB file costs 64KB of regenerated output, which is not byte-safe.** This is Trap 4 arriving at two more files and the answer is the same one: **new readings go to `seo-bot/ai-visibility/YYYY-MM-DD-readingN.md`, new off-site work to `seo-bot/outreach/YYYY-MM-DD-runN.md`.** Read the frozen files from a sandbox clone for history; never rewrite them. **Corollary, and it is a planning rule: keep every state file small enough to re-emit safely, and treat "this file needs appending to" as a signal to split it, not to push 60KB.**
- **THE SANDBOX IS NOT PERSISTENT: run 25's clone was wiped between two tool calls and every edit had to be re-derived.** Do clone → edit → build → push in one continuous stretch, and **keep repo edits as a replayable script against the pristine file rather than hand-made changes** — that is the only reason run 25 recovered.
- **Bing quirks:** weekly buckets dated to Fridays — never compare to GSC dailies. Backlinks from the UI (`GetLinkCounts` empty). Tools need `self="bing"`. Quota 100/day. Site URL `https://wescalestartups.com`.
- Cloudflare MCP has no Pages-deploy tools; verify deploys by fetching the live URL with a cache-buster. **curl status codes beat `web_fetch` bodies for redirects.**


## Backlog

**Numbers are stable IDs, not priority; never reassign them.** Order within each list is the ranking.

### Open

13. **CLOSED 2026-09-07 ON DANIEL'S STANDING — he has no relationship with Growth Division and will not cold-email a competitor's co-founder. DO NOT RE-PROPOSE, RE-DRAFT OR RE-RANK IT.** The factual case is unchanged, correct, and **re-verified live 2026-09-09** (run 25 §5): the comparison-table row for "WeScaleStartups" on `growth-division.com/blog/best-startup-growth-agencies-in-the-uk` reads **stage "Pre-seed to Series A"** (wrong), **"Pricing: Not disclosed"** (wrong — four bands are published), **"Clutch: Not listed"** (false), channels "Meta Ads, Google Ads, SEO, GTM" (wrong), clients "Google, Newsflare, GrowthMentor" (mentoring as client work), plus two unpublished performance figures and an unattributed testimonial. **Run 25 also proved the causal chain: Perplexity renders WSS's pricing as "Custom (three tiers)" from this page's "Not disclosed" + "three tiers".** **Still no action. The counter-move is to be the better source, not to correct the worse one** — prompt 5 states the stage correctly every reading because Google builds it from WSS's pages. Log as observation.
16. **CLOSED — THE RETRIEVAL LAYER IS COMPETITOR-OWNED, ON BOTH ENGINES.** All three listicles the engines cite (Growth Division, K3C, dimartec) are competitor self-rankings, each headed by its own author, none including WSS; **run 24 found Perplexity cites the same three on prompt 9, so the class is CROSS-ENGINE. It is closed because being excluded is the point of the page.** Run 22 classified two of them as "neutral" **without opening them** — **METHOD RULE: citation proves retrievability, NOT reachability. Open the page and read the headings.** Do not audit the remaining unverified names expecting a different answer. Directories are DONE (backlog 3).

10. **Sweep for commercial pages on informational queries — DEMOTED run 16.** One lead queued: `how does a post-pmf plg saas company scale acquisition…`, 18 impr @ 35.3 on bare `/insights` — blocked until **2026-09-23**, and **run 24 reclassified it FANOUT, so check the shape before acting** (`QUERY-MAP.md`). Keep the sweep; do not rely on it.

15. **`how to scale b2b saas acquisition` — 16 impressions @ 30.9 on `/services/acquisition-system-build`** (run 22 remeasure; page is 94 impr at 41.4, zero clicks), a £15k–£25k sales page answering a how-to. The exact backlog-10 defect class. **BLOCKED UNTIL 2026-09-17, and run 22 declines it on those grounds:** the intervention would be "write the how-to page and pull the queries off the service page", which is precisely what run 15 shipped and precisely what its verdict tests. **Shipping the second instance before judging the first confounds both.**

**Do not do — playbook-specific, ON TOP OF the task file's FIXED RULES (which already cover framework vocabulary, the meta descriptions, Companies House/Ltd, llms.txt and `ai:*`, FAQPage-for-rich-results, `/alternatives/*`, Review/AggregateRating, titles at 45+, brand-sitelink CTR, robots `ai-train`, homepage-as-agency, and merging the changelogs — do not restate those here):**

- **Target `scale startup`** — a Scale AI collision, not demand (run 18, SERP-verified).
- **Score a personalised Perplexity mention as a win** (run 18) — **run 25 reinstates this as the live risk: the account's search-history memory is ON and produced exactly such a mention.**
- **Quote `genai_query_insights` as citation evidence** (run 18).
- **Re-optimise `/services/90-day-growth-sprint` for provider intent, or read the collapse of its provider queries as a regression** (run 19 verdict).
- **Call an `analytics_query` without an explicit `siteUrl`** (run 19).
- **Compare a raw panel share between readings without also stating the winnable surface** (run 19).
- **Classify an off-site target from the fact that an engine cited it** (run 23) — open the page and read the headings.
- **Compare two GSC windows without first checking the terminal date in the Days tab** (run 23) — **and without stating how many days they share** (run 25).
- **Treat a Perplexity naming as organic while the account's search-history memory is on** (run 25).
- **Ship schema in order to obtain a rich result without checking the type is eligible in that context** (run 23).

12. **`agency brief template` — the page does not rank for its own name** (90 impr @ 34.2); its download is an 83-word skeleton. Soft SERP, real pre-hire buyer. If worked, thicken the deliverable; do **not** chase `agency collaboration template`, a closed decoy.


3. **Off-site authority — WORKSTREAM C lives in `seo-bot/OUTREACH.md`. Read that, not this.** Entity hygiene settled runs 11–13. **Daniel's standing decisions: no Companies House registration; display name stays "Daniel Johnson"; NO client review requests (any platform); NO cold outreach to competitors, which closes C-1. See `OUTREACH.md`, 2026-09-07.**
  **WORKSTREAM C IS A LIST OF ONE: be the source these pages cite — original research. Every route that depended on a third party is closed. RUN 24 GOT THE DATA (backlog 4); WIKIDATA Q137046365 CLOSED — leave it, see `OUTREACH.md`.** **It was never a client-permission ask: the public corpus needed nobody's permission, and run 24 sourced the second corpus from Daniel's own meeting notes.**

6. **Homepage non-brand category cluster — deferred since run 5.** ~630 impr at 7–13, one click. The fix means re-anchoring on "agency" (prohibition 15). Revisit only if another page picks these up.


### Closed — kept for the reasoning, not the task

17. **CLOSED run 23. `/resources` is a false lead** — named set converts at 0%, is 76% `wescale.com` collision, all clicks anonymised. **Nothing to ship to it.**

14. **CLOSED run 21. `/facts/daniel-johnson` freshness signal shipped** — run 19's derived-date pattern. No verdict on rank; not measurable in clicks, shipped under the gate's exception.

11. **CLOSED run 17. `/gtm-strategy`** — below any intervention threshold. Rule earned: **never diagnose a page's intent from its own content — pull its query set** (run 23 applied this to `/resources` and closed backlog 17 on it). Do not reopen.


4. **CLOSED 2026-08-27. `/insights/what-226-founder-reviews-reveal` shipped** — original research from 226 PUBLIC GrowthMentor reviews; caveats in `RESEARCH.md`. **RUN 24 ADDED A SECOND, PRIVATE CORPUS — 28 advisory transcripts — AND IT INDEPENDENTLY REPRODUCES THE PUBLIC CORPUS'S FINDING 2: in 27 of 28 sessions the presenting problem was not the actual constraint.** Two corpora, opposite methods, same result — **the strongest evidential position WSS has.** **THE TRANSCRIPT CORPUS IS PRIVATE AND UNPUBLISHABLE; anonymisation is not consent. Publish from the PUBLIC corpus only. Read `RESEARCH.md`'s publication rule before writing a word.** **Verdict 2026-09-24. The mechanism under test is CITATION, not ranking** — panel prompts 6 and 8 and the Generative AI report first, classic clicks last and possibly never. A null result is a real finding. **Prompt 8 is held by Lenny's and andrewchen with a founder-dependency diagnostic of exactly the shape WSS has data for — demand and format confirmed, WSS not on that surface (six readings).**

1. **CLOSED. Migration recovery complete** (run 13). **Verdict 2026-09-23.** It worked because Daniel had the GTM guide body — 4,000 words of unsourced advice produces filler.

2. **CLOSED. All four designated pillars written to depth** (runs 9, 13). **Verdicts 2026-09-16 and 2026-09-23; baseline to beat is 1 and 6 impressions.** Do not rewrite before then. **If still at zero on 2026-09-23 the pillar thesis is authority-bound too, and that changes the backlog.**

5. **CLOSED (run 13). `/insights` hub rewritten for provider intent** (was 1,032 impr / 0 clicks at 28.9 serving a link list). **Verdict 2026-09-23. Watch clicks, not impressions.** **Cited by AI Mode in readings #3 and #6 — and in #6 it took prompt 5's citation off the facts page.** One informational query queued for after that date — backlog 10.

9. **Housekeeping — done run 13.** PNGs go through `scripts/optimise-raster-images.mjs`, which **preserves alpha**. Still open, config not content: HTML is never edge-cached (`cf-cache-status: DYNAMIC`).

**Closed:** `cal…/auth/login` (noindexed). Legacy `/portfolio/*` (301 chains clean). Run 4's fractional-CMO pattern (remainder at 45+, authority-bound). **The `www.` legacy host — its GSC URL-prefix property still exists and is the live trap behind every broken API dimension.**

## What worked / what didn't

- **run 1 — FAILED, twice read.** Query-family re-anchor of `/fractional-cmo-vs-agency`: 740 named impressions, weighted position 46.2, **zero clicks**. **Content is necessary but not sufficient at position 45.** Do not rewrite this page again.
- **run 2 — CLOSED 2026-09-04: SPLIT VERDICT.** **Recrawl PASSED** — Google recrawled `/pricing` ~2h after the commit; clearing the stale lastmod worked. **The Offer rich result FAILED and was never eligible** — `OfferCatalog` on a **Service** is not rich-result-eligible, Google detects Breadcrumbs only, Search Appearance empty at 28d and 90d. **RULE, PROHIBITION-STRENGTH: VALID ≠ ELIGIBLE. Check Google's rich-results gallery for the type in that context before shipping schema to obtain a SERP feature — otherwise ship it for entity understanding and passage extraction only, and say so.** Not reverting: the price bands ARE being extracted. Detail: `changelog/2026-09-04-run23.md` §3.
- **run 3** — fractional-CMO consolidation. Verdict **2026-10-03**; 301 check **2026-09-19**.
- **run 19** — `/facts/we-scale-startups` completed as a fact source. Verdict **2026-09-22**, on AI impressions and panel prompt 5, **not rank**.
- **run 4 — CLOSED 2026-09-01: FAILED.** Provider passage + FAQs on the sprint page. **VERIFIED: position 1 on provider-fanout queries produces no clicks** — 51 → 41 → 2 impressions at 1.0–9.0, zero throughout. **Do not re-optimise this page for provider intent; do not read the disappearance as a regression.** Run 5 completed the argument — see its entry.
- **run 5 — CLOSED 2026-09-07: FAILED, and RECLASSIFIED. The most load-bearing verdict in this file.** `/insights/when-to-hire-fractional-cmo`: 85 impr / pos 7.7 / **zero clicks**. **73 of its 77 named 90-day impressions (95%) are two variants of ONE seventeen-word fanout string; the human phrasings drew THREE impressions in ninety days, at positions 22.0 and 30.0.** The flattering 7.7 belongs entirely to the machine phrasing. **The verdict is not "content at position 8 does not convert" — it is "there was no human demand here, and the position that looked like 8 was never a human's position."** Do not rewrite. Judge on AI citation. Detail: `changelog/2026-09-07-run24.md` §1.
  - **THE RUN 1 / RUN 4 / RUN 5 SET IS COMPLETE AND SAYS ONE THING.** Run 1: position 46, real queries, zero. Run 4: position 1.0–9.0, provider fanout, zero. Run 5: 7.7 on fanout, 22–30 on the human phrasing, zero. **On this site, every examined case of "we rank well for question-shaped queries" has been a statement about machine-issued strings. Before writing or rewriting for a question, check that HUMANS ask it, IN THAT PHRASING, in measurable volume.**
- **run 9** — `/insights/what-is-a-fractional-cmo` to pillar depth, definition/scope only. Verdict **2026-09-16**. Baseline to beat is zero impressions.
- **runs 10–13** — one interactive session with Daniel (audit, entity/schema, off-site profiles, three pillars, `/insights` hub, four orphans). Verdicts **2026-09-23**. ~4x unattended scope, only because Daniel supplied the GTM guide. **Do not reproduce unattended.**

- **2026-09-09 run 25 — MEASURE, nothing shipped, and the run's whole yield is instruments.** GSC advanced ONE day; no independent measurement was possible and none was faked. Panel **#10 broke the 3/10 story: 2/10, prompt 6 flipped out of the body.** **Found that Perplexity's search-history memory is ON on the free plan and personalised a mention — run 23's resolution is the thirteenth false fact, and the panel's own queries are what feed the contamination.** Found the **Perplexity completion tell** (`Finished`) and proved the old personalisation regex both false-positives and misses. Re-verified Growth Division's page live and **proved the causal chain from its "Pricing: Not disclosed" cell to Perplexity's wrong pricing row** — logged as observation, no action, backlog 13 stays closed. **Generative AI impressions 258 → 337.**
- **2026-09-07 run 24 — VERDICT, nothing shipped, and the reason is the verdict itself.** Closed **run 5**. Re-measured **money pages at 5**. Panel **#9**. Confirmed the **GBP category live**. Found the **eighth and ninth false facts** (Clutch, `pricingTiers`) and deleted a live self-contradiction. **Both SHIP candidates evaporated on inspection, which is the correct outcome.**
- **Five false leads chased, five different tells** — brand sitelink stacking (4), `scale startup` (18), a subagent reading WordPress paths on an Astro site (19), Bing's `InvalidApiKey` (21), `/resources`' 2.7% CTR (23). **The data looked internally consistent every time. Check the thing the data is about.**
- **The hygiene layer is clean and re-auditing it is waste** (run 5: no missing `alt`, links resolve, JSON-LD parses, legacy 404s redirect).
- **Verdicts due, in order: run 9 2026-09-16 · run 15 2026-09-17 · run 19 2026-09-22 · runs 10–13 and backlog 10's lead 2026-09-23 · original research 2026-09-24 · the independent money-page re-measure 2026-09-29 · run 3 2026-10-03. NONE was due on 2026-09-09.** **The next several runs are mostly judging, not shipping — plan for that rather than hunting for something to change.**
- **2026-09-04 run 23 — VERDICT, nothing shipped.** Closed **run 2** and **backlog 17**. **Retracted run 22's two off-site targets** — both competitor self-rankings. Panel **#8**: 3/10, 8 URLs; surfaced the GBP "Marketing agency" category. Found `["date"]` broken and the UI's live table is the **visible** one.
- **2026-09-03 run 22 — MEASURE, nothing shipped; the yield was instruments.** Repaired page/query measurement via the GSC UI through the DOM; proved the API bug is **not** the phantom accounts; found the AI Mode completion tell; settled **prompt 4 as a NO**.
- **2026-08-27 run 15** — new `/insights/how-to-run-a-90-day-growth-sprint`. Due **2026-09-17**. Baseline: four queries, 110 impressions at 11.3–15.0, **zero clicks**, all on the £8k–£12k service page. Read **clicks**, and whether the new URL takes those queries off the service page. **Cited by AI Mode four days after publication while classic search reallocated nothing — the two channels need different review horizons.**
- **THE GSC GENERATIVE-AI REPORT IS UI-ONLY**, at `/performance/search-analytics/ai?resource_id=…` — the connector returns empty `searchAppearance`. **Impressions only, no QUERIES tab: it names surfaced PAGES, never the prompt.** **217 → 228 → 258 → 337 (28d).** Run 25's +31% is the largest single move in the current data and it is on the AI channel while classic clicks are flat. Movers: run 15's page 20 → **34** (verdict 09-17), `/facts/we-scale-startups` 7 → 17 → 19 → **23** (verdict 09-22), `/services/90-day-growth-sprint` **46**, homepage 77. **Judge each open verdict on the channel the change was made for.**
- **A page rendering canonical metrics AND hand-written prose can contradict itself, and the JSON-LD half is the half that gets extracted (run 21).** The canonical reconciliation is **50+ startups supported, 20+ paid client engagements** — the dropped qualifier is the mentoring-vs-client-work distinction. **Diff hand-written figures against `canonicalProofMetrics` before trusting either.**
- **SETTLED: the top AI-cited pages are already built for extraction. Stop re-auditing this layer** — but *do* check a fact page actually carries the facts (four defects, runs 19–21). **`/fractional-cmo-vs-agency` needs no third rewrite:** 549 impressions at 38.1, zero clicks, **uncited on prompt 7 — its exact query — in readings #5–#10.** Authority-bound; judge on AI impressions, not rank.
- **2026-09-01 run 19 — VERDICT, then SHIP.** Judged run 4 and shipped `/facts/we-scale-startups`. **Verdict 2026-09-22, on AI impressions and panel prompt 5 — not rank.** Four instruments agree it is in the retrieval set: AI impressions 7 → 17 → 19 → **23**, panel prompt 5 in readings #1–3, #5, #7–#10, and 561 classic impressions at 6.1.
- **2026-09-02 run 21 — SHIP under an instrument blackout, then an addendum that overturned half its own diagnosis.** Fixed a 2.5x self-contradiction on `/facts/daniel-johnson` — **build-verifiable work, the one class that survives an evidence blackout. A blacked-out run is not automatically a REPORT run.**
- **run 18 — OFF-SITE, nothing shipped.** Downgraded a recorded Perplexity win to zero on the personalisation tell; turned `scale startup` into a prohibition on **one SERP fetch**.
- **run 20 — MEASURE then OFF-SITE, nothing shipped.** Found the sixth false fact (the panel's reading method) and established that Google names WSS on a buying prompt **solely from a competitor's page that gets the stage wrong** (backlog 13, 16).

- **Workstream C: Growth Division has been in Google's prompt-9 sources in every reading, and in #5 was the ONLY source behind Google naming WSS. Do not pay for placement, and never ask a competitor to add WSS to their own listicle** — C-1 is a factual-accuracy correction, a different ask.

## Standing decisions

- **The verification protocol, the lastmod two-step and the noindex two-file rule are in the task file's FIXED RULES. Not restated here.** The two that have cost time: `check:lastmod` cannot fail before the content commit exists (Trap 5), so only the post-push pass means anything; and **pages built − noindexed = sitemap `<loc>` count**, checked after every structural change.
- **Never state unsourced market statistics.** Anchor cost claims on WSS's published ranges; reuse `/insights/fractional-cmo-cost-uk`'s bands.
- **Keep the "point them somewhere better" voice.** Every comparison should name the case where a competitor or a different model is the right buy — brand voice *and* what makes a passage worth citing.
- **When a change is defensible under two competing readings of the data, prefer it** over one needing the ambiguity resolved first.
- **Sweep the connectors before writing anything implying WSS expertise or client work.** The VC guide assumed fund-side experience; there is none. The real position is accelerator-side (Google for Startups, DeepMind, Techstars, GrowthMentor, several hundred founder sessions) — **a mentoring relationship, not client work. Run 20 found a third party had collapsed that distinction off-site and run 21 found the site doing it on `/facts/daniel-johnson`. Do this check every time.**
- **A hub page must not restate its spokes.** Run 3 spent a run undoing a self-competing cluster.
- **`src/data/growthTools.ts` is the primary source for what the free tools do** — not any page describing them (runs 13 and 14 both got this wrong). The Scorecard is **12 questions, ~4 min**, scoring **founder dependency** — not the five-layer framework on `/diagnose`. Never describe either in terms of the other.
- **KEEP FILES SMALL AND CHECK THE RETURNED BLOB SHA. Run 25 corrects the old "push large files through a subagent" rule, which was half wrong:** the connector's `content` is a string literal, so *someone* must regenerate every byte — a subagent only moves that risk, it does not remove it, and run 25's subagent correctly refused a 188KB push on exactly that ground. **What actually works: `git hash-object` the file on the sandbox, push it, and check the blob SHA in the response matches. Run 25 pushed four files and all four matched.** The real defence is keeping each file small enough to emit in one call — which is why the changelog, and now `ai-visibility/` and `outreach/`, are split per run.
- **Verify claims about the site against the site — and claims about WSS's OFF-SITE presence against the PLATFORM, not against whoever described it. THIRTEEN false facts have been found here, and the shape is identical every time: a claim recorded from a secondary source and never re-checked against the primary one.** The full ledger is in the changelogs; the three that still matter as warnings are **"WSS is not listed on Clutch"** (taken from a competitor's page, never opened — the profile is live, claimed, accurate, 5.0, page one), **"the LAST table is the live view"** (it is the VISIBLE one), and **run 25's: "Perplexity's free tier has no memory, so its zeros are real"** — written by run 23, falsified by opening the settings page. **`grep` the repo, fetch the live URL, or open the page before acting on anything in here — and when you correct one, DELETE the wrong line.**
- Mark confidence explicitly (verified / inferred / vendor-sourced). Most GEO research is published by firms selling GEO tools.
