# Run 5 — 2026-09-20 — MEASURE

Clarity came back. The first thing it did was add two booked calls nobody had recorded, and the second was to show that the `/book/thanks` URL carries the entire Cal.com booking form — company, challenge, attendee, time — which makes qualification a read rather than an investigation. The booking count for this window is **12, not 10. None of them qualify.**

## Classification

**MEASURE.** No verdict was due (`EXPERIMENTS.md` is still empty). Nothing shipped; the one candidate that needed no answer from Daniel was chased to its end and evaporated on inspection, which is the correct outcome.

**Window: 2026-08-23 → 2026-09-19**, against **2026-07-26 → 2026-08-22**. This shares **27 of 28 days** with run 4's window, so under the standing rule it is not a new measurement of traffic. **GA4 was deliberately not re-read.** Run 4's traffic numbers stand: sessions 1,012 (−26.5%, almost all Direct), money-page landing sessions 106 (+53.6%), organic key events 5 → 13. The run was spent on the primary metric and on instruments instead.

## The booking count was wrong again. 12, not 10.

Run 3 established that the Cal.com email feed under-counts and that the Clarity `/book/thanks` invitee list is the second ledger. With Clarity back, that reconciliation ran properly for the first time since.

**Two bookings existed in neither the email feed nor any prior run's count:**

| Invitee | Company | Call | Ledger that caught it | Qualified? |
| --- | --- | --- | --- | --- |
| **Lei Zhao** | ViiRaa (`lei.zhao@viiraa.com`) | Thu 24 Sept 17:20 | Clarity only — no Cal.com email, no calendar notice | **No** — B2C glucose/prediabetes coaching app. Her own form text: *"we reverse prediabetes by lifestyle change that hold… No drug, just routine. Trigger event is diagnosis, but not sure how to expand it."* |
| **Koa Browne** | Onfound (`koa@onfound.com`) | Tue 22 Sept 13:20 | Clarity only | **No** — AI founder-networking app, freemium $19/mo, ~3,100 founders, mobile app in Apple review. Not B2B SaaS at £1m–£10m ARR |

A third find: **Josefina Quiroga booked a second time** (call 29 Sept), also absent from the feed. Counted once by invitee, consistent with how Revathy's second booking was treated at run 3.

**Corrected window: 12 bookings, ZERO qualified.** Twelve for twelve; **fourteen consecutive unqualified bookings** across every window this programme has measured.

- **Website-sourced: still 3** — Revathy Sivasankaran, Anthony Pollock (09-02 Growth Audit), Bea Brampton. The denominator has now survived three separate attempts to move it.
- Non-website: 9 of 12 (75%), up from 7 of 10.

**The email feed's miss rate is worse than recorded: 5 of 12 (42%), not ~30%.** Missing: Tom McColllum, Thabi Pulikkotil, Scott McKim, Lei Zhao, Koa Browne.

### Koa Browne looked website-sourced and was not

Clarity shows the booking session as `/` → `/pricing` → `/book/thanks`, referrer `wescalestartups.com`, 84.67% scroll depth, 1.5 min active. On behaviour alone this is the website-sourced ICP booking the programme has been waiting for.

It is not. The Notion Sales CRM page for Onfound, written 09-15, records: **Origin "Direct email conversation / WSS Growth Audit"** — Daniel contacted Koa on 10 Sept about the Johannesburg founder ecosystem, Koa raised a growth/GTM engagement on 14 Sept, and **Daniel sent the booking link**. Daniel's own Next Action reads *"advance only if a genuine paid WSS fit is confirmed."*

**The standing rule held: a booking is not a website conversion until you know how the invitee got the link — and site behaviour is not how you find out.** This is the third invitee (after Josefina and Scott McKim) to look website-sourced on one instrument and not be.

## The instrument upgrade: `/book/thanks` carries the whole booking form

Clarity's Top pages card with **URL parameters ON**, filtered to `Visited URL contains isSuccessBookingPage=true`, returns each success URL in full. Cal.com's redirect carries:

`email` · `attendeeName` · `title` · **`description` (the invitee's free-text challenge)** · `startTime` / `endTime` · `uid` · `rescheduleUid` / `rescheduledBy` / `formerTime` · `isEmbed`

That is the complete qualification payload — the same fields the Cal.com notification email carries, on the ledger that does *not* drop 42% of bookings. **This replaces email-by-email qualification as the primary route.** Segment URL, reusable directly:

`clarity.microsoft.com/projects/view/wkannkoxst/dashboard?date=Last%2028%20days&URL=2%3B2%3BisSuccessBookingPage%3Dtrue`

Narrow to one invitee by putting a domain fragment in the same filter (`&URL=2%3B2%3Bonfound`), which returns that session's full path and referrer too.

**Caveat, measured this run: neither ledger is complete.** Jordy Hartendorp and Thabi Pulikkotil are in the email/calendar feed and absent from the `/book/thanks` list. **Reconcile both, every run; neither alone is the count.**

**Trap: the Path → URL autocomplete is a static recent-URL list capped at 10, not a search.** Typing into it does not filter it. Run 3 read that list and got 10 — the cap, not the answer. **Apply the filter and read the Top pages card instead.**

## Backlog 2 closed — the sitewide LCP is the homepage, and nothing permitted fixes it

Clarity's **Performance overview → URL performance** tab attributes CWV per page. First reading, 28d:

| Page | Score | LCP | INP | CLS |
| --- | --- | --- | --- | --- |
| `/book/thanks` | 94 | 0.9s | 120ms | 0 |
| `/resources/growth-dependency` | 94 | 0.7s | 140ms | 0 |
| `/case-studies` | 92 | 0.68s | 140ms | 0 |
| `/proof` | 92 | 1.6s | 180ms | 0 |
| `/book` | 86 | 2.3s | 180ms | 0.004 |
| `/ai-growth-audit` | 81 | 3.5s | 140ms | 0 |
| **`/`** | **78** | **3.1s** | **350ms** | **0.1** |
| `/podcast-guest-application` | 64 | 20s | 470ms | 0 |
| `/fractional-cmo-vs-agency` | 39 | 45s | 550ms | 0 |

**The sitewide 3.1s LCP is the homepage**, open since run 3. `/` is also the top entry page (205 Clarity pageviews, 425 GA4 landing sessions), so it is the slow page that matters most.

**And it is not shippable.** Every image on `/` already carries `loading="lazy" decoding="async"` and explicit `width`/`height`; there is no hero image and no eager image. The permitted intervention — image weight and lazy-loading — **is already in place**. What remains (a font- or CSS-driven LCP and the 0.1 CLS) lives in `BaseLayout`/global CSS, a shared component touching locked pages, which FIXED RULE 6 forbids. **No money page is slow: `/book` is 86/2.3s and `/book/thanks` is 94/0.9s.**

**`/pricing` has no CWV row at all** — 46 pageviews and not one CWV sample. Worth knowing before anyone reads its absence as a good score.

**The two extreme rows are sample artefacts, not defects.** `/fractional-cmo-vs-agency` at 45s LCP has **no images on the page at all** and answers in 0.36s; `/podcast-guest-application` at 20s answers in 0.44s. The whole sitewide CWV sample is 30 pageviews, so one stalled or backgrounded session owns a page's number. **Do not chase these.**

## Diagnosis

*What, today, most stops a qualified visitor already on this site from booking?*

Nothing on the site does. The answer is unchanged and now carries more weight behind it:

- **No friction, read a third time and materially identical.** Rage 0% (0), excessive scrolling 0% (0), dead clicks 3.88% (23 of 593), quick backs 3.20% (19); pages/session 1.25, scroll depth 31.59%, active time 45s. Against run 1 (0 / 0 / 5.25% / 3.78%) and run 3 (0 / 0 / 4.85% / 3.56%). **Zero JavaScript errors sitewide.**
- **No performance constraint on a money page** (above).
- **High-intent traffic is rising** — money-page landings +54%, organic key events +160% (run 4).
- **And the bookings are still, without exception, out of ICP** — on the invitees' own words: *"We aren't SaaS"*, a prediabetes app, a pet drinks brand, an incoming employee wanting personal guidance, an accountancy, an IT-services mentor match, a freemium founder-networking app.

**The site converts non-ICP visitors competently and says nothing about who it is for.** That has been the diagnosis since run 1; this run adds two more unqualified bookings and a third false website-sourced positive to it.

## Why nothing shipped

The standing candidate is unchanged: state the positive ICP (B2B SaaS and AI, Seed–Series B, £1m–£10m ARR) on `/book` above the calendar and at the top of `/pricing`.

- **Test 1 passes**, more strongly than at run 4.
- **Test 4 fails.** The metric is qualified share of website-sourced bookings; the denominator is still 3 per 28 days, now confirmed a fourth time.
- **Test 6 fails**, for run 4's reason, unchanged: money-page traffic volume and composition are both moving inside the window the SEO programme has under observation, with its first genuinely independent money-page read on **2026-09-29**. Shipping now spends the one clean pre/post read this programme has.

The performance candidate — the one that needed no answer from Daniel — was worked to its end this run and **fails test 1**: the evidenced constraint has no cause that any permitted change addresses.

Tests 2, 3, 5 and 7 pass. Nothing else on the permitted list has evidence behind it: no rendered defect exists (all money pages 200, 0.28–0.58s server response, zero JS errors), no CTA-placement problem is visible in behaviour, and money-page meta descriptions are locked until 09-29.

## Instruments

- **Clarity authenticated on the first attempt.** Run 4's "session has expired" was a transient Clarity-side session fault; the Google round-trip to `daniel@wescalestartups.com` completed and the dashboard loaded. **Not a standing block. Try it before reporting it dark.**
- **Chrome (`claude-in-chrome`) connected** and drove everything asked of it.
- **The Rankrat / `wss-search-analytics` connector is NOT in this session** — third consecutive absence. Treat it as gone, not as a bonus.
- **A repo clone route exists this session** (`git clone` over HTTPS). Run 4's blocker is resolved for this run, so `PLAYBOOK.md` and the `seo-bot` handoff were both maintained with scripted edits on disk. **It is session-dependent, not fixed.**
- Notion Sales CRM answered and was decisive on Koa Browne.
- Live money pages all 200: `/`, `/book`, `/pricing`, `/contact`, `/fractional-cmo-vs-agency`, `/podcast-guest-application`, `/ai-growth-audit`.

## Negative findings — do not re-derive

- **No friction constraint on this site.** Third identical read, 34 days apart. A single re-read of the top four numbers is the whole check.
- **No money page has a performance constraint.** `/book` 86, `/book/thanks` 94.
- **The homepage's 3.1s LCP has no permitted fix.** Images are already lazy with dimensions set.
- **`/fractional-cmo-vs-agency` 45s and `/podcast-guest-application` 20s LCP are thin-sample artefacts**, not defects.
- **No new booking arrived between 2026-09-16 and 2026-09-19.** The last Cal.com "new event" email is Josefina, 09-16 07:49.
- The Clarity autocomplete is not a search (above).

## Handoff to seo-bot — written to `seo-bot/PLAYBOOK.md`

Three items, including the two run 4 could not write:

1. **Organic Search key events 5 → 13 (+160%)** on flat sessions (116 → 120), session key-event rate 4.31% → 5.83%, while site-wide key events fell 31 → 28. Money-page landing sessions 69 → 106, `/pricing` 6 → 26. Leading indicator ahead of the 09-29 verdict, measured independently of GSC.
2. **`AI Assistant` is now a named default channel group in GA4 `259840282`** — 17 sessions, 64.71% engagement, 36s, 2 key events. Direct arrivals, not impressions.
3. **`wescalestartups-site.pages.dev` is live, returns 200, serves `robots.txt` with `Allow: /` and `<meta name="robots" content="index,follow">`.** Its canonical correctly points at `https://wescalestartups.com`, which is the mitigation — but the preview origin is crawlable on its own terms and drew **7 tracked pageviews** in 28 days, so it is also polluting Clarity slightly. `robots.txt` is no-touch for growth-bot (FIXED RULE 4). seo-bot's call.

## Assumptions

- Josefina's second booking is counted once by invitee, matching run 3's treatment of Revathy's second booking. Counted per booking event instead, the window holds 14.
- Onfound and ViiRaa were qualified from the invitees' own Cal.com form text, public company pages and (for Onfound) Daniel's CRM record. No funding data was available for either; ICP exclusion rests on business model and scale, and is **medium confidence** on ARR.
- Run 4's GA4 traffic figures are carried forward unre-read, because the window moved one day.

## State files

- `PLAYBOOK.md` updated on disk with scripted edits, including run 4's deferred entries.
- `seo-bot/PLAYBOOK.md` handoff written.
- **`FUNNEL.md` gets no row.** The last row was added at run 3 on 09-17, three days ago; the rule is one per week. The corrected count is in this file.
- **`EXPERIMENTS.md` unchanged.** Nothing shipped, so no row and no new lock.

## Locks observed

`/insights/what-is-a-fractional-cmo`, `/insights/how-to-run-a-90-day-growth-sprint`, `/services/90-day-growth-sprint` (entangled with the 09-17 row), `/facts/we-scale-startups`, the three pillars + `/insights` hub + orphans, `/insights/what-226-founder-reviews-reveal`, `/services/fractional-cmo`; and no title or meta-description change on any money page before 2026-09-29. `git log` for the last 24h shows one commit, `04830c7`, adding email-signature images under `public/images/` — no page. **Nothing was touched.**

## Blocked on Daniel

1. **Are non-ICP booked calls a problem to fix, or part of the model?** Unchanged since run 1, now **twelve for twelve** with 75% of volume from channels Daniel built by hand. Nothing about money-page filtering can ship until this is answered — and the 09-29 window is the moment it becomes actionable, so an answer before then is worth more than one after.
2. **Authorise the `daniel@wescalestartups.com` work calendar on the Google Calendar connector.** Now the highest-value access fix by some distance: the email feed misses **42%** of bookings and the `/book/thanks` ledger misses others, so the primary metric currently depends on reconciling two incomplete sources through a browser.
3. **Confirm the 2026-09-29 money-page interpretation** (body copy and proof placement available, no title/meta change). Unconfirmed since run 1 and load-bearing — it is the date this programme is now explicitly waiting for.
4. **Keep Chrome running with the Claude extension when the daily run fires.** Clarity is the only route to the second booking ledger, the device split and field CWV, and it is Chrome-only.
5. **Give the scheduled session a durable repo clone route.** It worked this run and did not at run 4; without it the programme's state cannot be maintained.
6. Configure `clarity` and `cloudflare` accounts on the search-analytics connector if that is possible.

## Tomorrow's single highest-value action

**Nothing, unless a new booking lands.** The window moves one day, the traffic read shares 27 of 28 days with this one, the behavioural baseline is closed, the performance question is answered, and the only change worth making is held until 09-29 by a lock this programme should not break. If a booking arrives, reconcile it across both ledgers and qualify it from the `/book/thanks` payload. Otherwise the next run with anything to do is **2026-09-29**, judging the money-page window with seo-bot.
