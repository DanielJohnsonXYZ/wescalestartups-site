# Run 8 — 2026-09-23

**Classification: REPORT.** Nothing shipped. Nothing passed all seven gates, and the run says why rather than manufacturing a change to fill itself.

## Window and funnel

Window **2026-08-26 → 2026-09-22** (last complete 28 days), against **2026-07-29 → 2026-08-25**. The window moved one day from run 7, so most numbers are not a new measurement; the bookings and the traffic comparison are.

| | Prior 28d | This 28d | Change |
| --- | --- | --- | --- |
| Sessions (GA4, all landing pages) | 1,326 | 1,003 | **−24.4%** |
| Key events (all) | 30.00 | 30.00 | flat |
| `/book` landing sessions | 18 | 25 | +39% |
| `/pricing` landing sessions | <13 (outside top 12) | 24 | up |
| `/services/growth-diagnosis` | 15 | 14 | flat |
| `/` landing sessions | 610 | 422 | −31% |
| Bookings | — | **14** | +2 on 09-22 |
| Qualified bookings | — | **0** | eighteen consecutive |
| Website-sourced bookings | — | **3** | unchanged through every correction |

Raw GA4 sessions stay ~80% direct/bot; 1,003 is not a conversion denominator.

## The primary metric moved — and neither booking touched the website

Two Cal.com bookings landed on 2026-09-22, the first since Josefina on 09-16.

- **Mahmod Mahamed**, `Growth Audit | Mahmod Mahamed × We Scale Startups`, Tue 22 Sept 17:15. **Unqualified, MentorCruise-sourced, proven on the thread.** MentorCruise inquiry 09-21 13:03; 09-22 08:14 "how to continue from here?"; 09-22 09:03 "Unfortunately I didn't get any link"; 09-22 11:04 **"Yeah, thanks. I scheduled a meeting"**. His own form answers: Company **M71**, Stage **pre-pmf**, constraint positioning, challenge "New clients". Personal Gmail, Asia/Jerusalem. Fifth MentorCruise booking on the record.
- **Joshua Herzig-Marx**, `Podcast Interview`, Tue 13 Oct. Non-website: the notice carries **"Additional notes: Via Lenny's"**. A podcast guest, never a qualified call.

**Mahmod left no `A new event has been scheduled` notice at all** — only his own Google Calendar acceptance, from his own address. Updated feed-miss figure: **6 of 14 (43%)**. This is the concrete instance that makes the work-calendar request worth acting on.

The two bookings also re-make the structural point without needing a new argument: **booking volume on this site comes from mentoring platforms and personal networks, and no permitted site change reaches either channel.**

## Diagnosis

What most stops a qualified visitor already on the site from booking? **On this window's evidence, nothing new does.** Money-page reach is *improving* while total traffic falls 24% and key events hold flat at 30 — the third independent confirmation of the run 6 shape, now on landing-page sessions rather than events. The traffic that left never reached a money page. Friction is closed four times over (`FINDINGS.md`); the CTA-under-served diagnosis was withdrawn inside run 6; backlog 1 rules out ICP-filtering copy.

## Why nothing shipped

- **Gate 1 fails.** No specific constraint with direct evidence. The one thing that moved this window moved in the right direction.
- **Gate 2 and gate 6 fail for anything CTA-shaped.** The open sticky-CTA row is judged on **sitewide** `book_call` ÷ `sticky_book_cta_shown` until **2026-10-19**, so adding, moving or relabelling a booking CTA anywhere confounds it.
- **Money-page titles and meta are held to 2026-09-29** by the seo-bot money-page click window.
- The seo-bot verdict windows lock the pillars, `/insights` hub, orphans, `/facts/we-scale-startups`, both insight pages, `/services/fractional-cmo` and — by the run 2 entanglement note — `/services/90-day-growth-sprint`.

**LOCKED PAGES this run:** `/insights/what-is-a-fractional-cmo`, `/insights/how-to-run-a-90-day-growth-sprint`, `/insights/what-226-founder-reviews-reveal`, `/insights` hub and orphans, the three pillars, `/facts/we-scale-startups`, `/services/fractional-cmo`, `/services/90-day-growth-sprint`, `StickyBookCta.astro` and every page it renders on. No site content was committed in the last 24h (the only commits since 2026-09-21 18:08 are `growth-bot:` state commits).

## Verdicts

**None due.** The single OPEN row reviews **2026-10-19**. Post-change data exists for roughly one day and was deliberately not read: the window is not shortened because early data is thin, and reading it now would invite a noise verdict.

## Negative findings

- **No new browser-side defect.** Headless Chromium on live `/`, `/pricing`, `/book`, `/contact`: the only console errors are the two already-recorded CSP refusals (Turnstile, Intercom). Zero JavaScript exceptions, no other failed request.
- **All eight money pages 200 in 0.39–1.27s.**
- **`/metrics/ag/g/c` `ERR_ABORTED` is not a defect** — it looks exactly like broken page_view collection. Direct probe: `GET` and `POST` both return **204**, and the property recorded 1,003 sessions. Closed on data, not hedged. Do not chase.

## Instruments

- **The WSS Search Analytics (Rankrat) connector was not in this session.** Checked the way Trap 13 requires and not the way run 7 got it wrong: `RefreshMcpTools` enumerated **30 servers, none of them it** (five unreachable servers *were* named `not_connected`), and a `select:` `ToolSearch` on its exact tool names returned no match. **Treated as a per-session provisioning difference, not an outage, and not a blocker** — GA4 answered through Chrome the same run. Not led with.
- **GA4's Chrome account index moved: it is now `authuser=2`.** `authuser=1` serves the provisioning page ("Welcome to Google Analytics", no properties) and `authuser=0` serves "Missing permissions". Both read as an outage and neither is one. Property name asserted in the header before any number was taken.
- **GA4's comparison parameters `_u.date10`/`_u.date11` silently did not apply**, and GA4 compared 28 days against **one day** (`22 Sept – 22 Sept`, 41 sessions), reporting **+2,346%** on sessions. The period label was the only tell. Discarded; two single-range reads used instead, which returned byte-identical totals for the same window.
- **Clarity not read this run.** The behavioural questions it answers are closed in `FINDINGS.md` and nothing in the diagnosis turned on it; reading it would have been activity, not evidence.

## Handoffs to seo-bot

None. No SEO-shaped constraint surfaced.

## Blocked on Daniel

1. **Authorise the `daniel@wescalestartups.com` work calendar on the Google Calendar connector.** Still the highest-value access fix, and run 8 sharpened it: **6 of 14 bookings have no Cal.com new-event notice**, and Mahmod Mahamed has no Cal.com notification of any kind. The primary metric is assembled by hand from two Gmail query shapes that each miss different bookings.
2. **Add `https://challenges.cloudflare.com` to `script-src` and `frame-src` in `public/_headers`, or retire Turnstile** — re-confirmed live this run, day 48. **Do not set `TURNSTILE_ENFORCE=1` before that**: it takes every form on the site down silently while the lead-capture monitor stays green.
3. **Is Intercom meant to be live?** Still CSP-blocked, still absent from the repo, so still a GTM-injected widget that has never rendered.

## Assumptions recorded

- Joshua Herzig-Marx's podcast-interview slot is counted as a Cal.com booking and as unqualified, consistent with how Anthony Pollock's podcast slot was treated in earlier windows.
- Money-page landing sessions are given for the three money pages inside the top 10; the rest are each under 13 and were not paginated for.

## Housekeeping

`PLAYBOOK.md` is at **29.7KB**, against the 30KB compaction threshold. Closed and withdrawn items were trimmed this run to stay under it. **The next run should compact on disk before analysis.**

## Tomorrow's single highest-value action

**Re-read the booking feed on both query shapes and qualify anything new — nothing else.** The window is inside the 09-29 and 10-19 lock walls, so the only series that can move is bookings, and run 8 showed it moves without warning and without a Cal.com notice.
