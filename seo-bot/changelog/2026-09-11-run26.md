# Run 26 — 2026-09-11

**Classification: SHIP.** One change, build-verifiable, live and confirmed. But the run's largest output is an instrument finding that overturns something this file has treated as settled for five runs.

**The single most important line: `analytics_query` WORKS. The playbook's bold, capitalised instruction — *"GSC MCP `analytics_query` IS UNUSABLE FOR MEASUREMENT. EVERY DIMENSION — INCLUDING `["date"]` — ANSWERS ABOUT THE STALE `www.` PROPERTY"* — is no longer true.** Verified four ways below, not assumed. **Fourteenth false fact in the ledger, and the second this bot wrote about itself.**

Instruments first, per the standing rule.

| Check | Result | Consequence |
| --- | --- | --- |
| `accounts_manage list` | **Both phantoms present** — removed. | Trap 11 as written. Not diagnosed. |
| `analytics_query` `["page"]` row one | **`https://wescalestartups.com/`, 43 clicks / 2,416 impr** — no `www.` | **CORRECT PROPERTY. The API is repaired.** See §1. |
| Browser | **Claude in Chrome NOT CONNECTED**, two retries | **Panel is NULL this run.** See §4. Built-in pane works and was used for the site checks. |
| **GSC terminal date** | **2026-09-08** — advanced two days in two calendar days | **26 of 28 days shared with run 25. Still not an independent window.** |

---

## 1. `analytics_query` IS REPAIRED. Verified against an independently-read figure, not assumed.

Runs 21–25 all recorded row one of `["page"]` as `https://www.wescalestartups.com/` at 1–2 impressions — the dead WordPress property. Run 23 escalated this to *"every dimension, including `["date"]`"* and concluded *"there is no working API route; the UI is not the preferred route, it is the only one."*

**Today every dimension answers about the correct property.** The check that makes this a finding rather than a hope is that run 25 read the named-query table **out of the Search Console UI by hand** on a 28-day window to 2026-09-06. The API was asked for the same window:

| Query | Run 25, read from the UI | Run 26, read from the API |
| --- | --- | --- |
| `we scale startups` | 18 / 227 / 1.1 | **18 / 227 / 1.06** |
| `wescalestartups` | 4 / 7 / 1.0 | **4 / 7 / 1.00** |
| `we scale` | 2 / 190 / 6.7 | **2 / 190 / 6.67** |
| `we scale ab` | 1 / 10 / 18.2 | **1 / 10 / 18.2** |

**Four for four, exact.** Two independent instruments, same window, same numbers.

**Three further repairs confirmed:**

- **`["date"]` works** — returned a clean daily series and gave the terminal date (2026-09-08) without opening a browser. This is the check run 23 declared broken.
- **`filters` works.** Trap 9 recorded *"every `filters` array returned `[]`"*. An exact-match page filter on `/services/acquisition-system-build` returned three real rows (§3). **This is the big one operationally** — page-filtered query sets were the thing the UI-through-the-DOM method existed to obtain, and they now cost one call instead of a scripted tab-click, a forced scroll and a header assertion.
- **`["searchAppearance"]` still returns `[]`.** **So the Generative AI report remains UI-only, and that part of the settled knowledge stands.** Worth stating plainly: the property bug and the `searchAppearance` gap were always two separate faults, and only one of them has cleared.

**Cause: not established, and deliberately not chased** (standing instruction). It is not the phantom accounts — run 22 cleared those first and the bug persisted; they were present and cleared again today. The server reports a newer version available (2.1.2 → 2.1.3), which is a plausible but unverified explanation.

**Instruction for run 27, and it is the cautious form: re-run the discriminator every run and do not assume the repair holds.** A fault that appeared without explanation can return the same way. The discriminator is unchanged and costs one call: pull `["page"]`, read row one, and a `www.` URL means the wrong property. **The UI method in the playbook is not deleted — it is demoted to the fallback it should always have been.**

## 2. SHIPPED — `/mentoring` was absent from the sitemap

**Daniel added a new top-level page yesterday** (`src/pages/mentoring.astro`, 2026-09-10 18:03 UTC, with five follow-up commits to 19:34 routing nav, footer and enquiries). It is a real, indexable page: `index,follow,max-image-preview:large`, self-canonical, 753 words, Person + Organization/ProfessionalService JSON-LD, linked from the main nav and the footer.

**It was not in the sitemap.** `src/pages/sitemap.xml.ts` keeps its static paths in a hand-maintained array, so a new top-level page does not get picked up. Confirmed both ways before touching anything:

- Live `sitemap.xml`: **109 `<loc>` entries, no `/mentoring`.**
- Local build of the pristine tree: **121 pages built − 11 noindexed = 110 expected, sitemap emitting 109.** **The standing arithmetic invariant was broken by exactly one, and the one was this page.**

**This is the same defect class as `/resources/category-reframe`, which carries a comment in that very file explaining that leaving it out "was a discovery gap, not a decision".** The comment did not prevent the recurrence, because the failure is structural: the list is manual and a new page does not announce itself.

**Change:** `/mentoring` added to `staticPaths`; a placeholder key inserted into `staticPathLastModified` and filled by `refresh:lastmod`, which wrote **2026-09-10**, matching the page file's git date. Falls to the default `monthly` / `0.75` — no change to `staticUrlMeta`, keeping the diff at two lines.

**Verification, full protocol:**

| Step | Result |
| --- | --- |
| `astro check` | **0 errors**, 0 warnings, 9 hints |
| `npm run build` pre-push | clean, sitemap **110**, `/mentoring` node correct |
| Pushed blob SHAs vs `git hash-object` | **both MATCH** — `f1cc334…` and `44e3a58…`, byte-exact |
| Fresh clone of pushed tree, rebuilt | clean; **121 built − 11 noindexed = 110 = sitemap `<loc>`** |
| `check:lastmod` **on the pushed tree** (Trap 5) | *"staticPathLastModified is current."* |
| Live `sitemap.xml` after deploy | **110 locs, `/mentoring` present** |
| Live `/mentoring` | **200** |

**On prohibition 19.** The mentoring *page* was committed inside the 24-hour window and **was not touched** — no edit to `mentoring.astro`, the nav, or the footer. The two files changed (`sitemap.xml.ts`, `lastmod.ts`) are not in Daniel's recent commits. **The judgement made, and it is worth recording so it can be overruled: a page shipped with `index,follow`, a self-canonical, a written meta description and links from the main nav is a page meant to be found, so the omission reads as the known manual-list gap rather than as work in progress.** If that was wrong, the revert is two lines.

**Honest sizing of the win.** `/mentoring` is in the sitewide nav, so Google would have found it anyway. What the sitemap adds is a `lastmod` signal and guaranteed discovery, and what the fix really restores is the **invariant** — a broken arithmetic check is a broken instrument, and leaving one broken over a weekend to preserve a technicality is the worse trade. **No verdict set: this is not a change whose effect on booked calls is measurable, and it is shipped under the gate's stated exception.**

## 3. BACKLOG 15 — the shape check run 24 asked for, done early. The premise is weaker than recorded.

Run 24's carry-forward #2: *"that item assumes `how to scale b2b saas acquisition` is worth a page, and 16 impressions have not been checked for fanout shape."* The repaired `filters` made this one call. **Backlog 15 is still blocked until 2026-09-17 and nothing was acted on** — this is preparation for that date.

**`/services/acquisition-system-build`, 28d to 2026-09-08. Three named queries, 49 impressions:**

| Query | Impr | Pos | Read |
| --- | --- | --- | --- |
| `scalable land acquisition teams` | **35** | 91 | **COLLISION** — property/land acquisition, not B2B SaaS |
| `how to scale b2b saas acquisition` | **13** | 27.0 | six words, **human shape** |
| `evaluation and development kits acquisitions` | 1 | 99 | collision (electronics) |

**Two findings, and they point opposite ways.**

**The good news for the premise: it is not fanout.** Six words, no punctuation-variant twin, no fifteen-word sentence. It passes run 5's filter, which is more than backlog 10's queued lead managed.

**The bad news is larger. 71% of that page's named impressions are a land-acquisition collision.** The playbook records the page as *"94 impr at 41.4"* — but **the page's real topical demand is 13 impressions in 28 days, and the 41.4 average position is mostly a position-91 real-estate query dragging the mean.** This is the `/resources` lesson repeating exactly: *check the query mix before believing any page-level figure.* Recorded there, not yet applied here.

**Consequence for 09-17: backlog 15 should be judged on 13 impressions at position 27, not on 94 at 41.4.** Thirteen impressions over 28 days is roughly 42 over 90 — above run 5's three, below anything that has ever justified a page here, and position 27 is authority-bound territory where run 1 already proved content alone does not move it. **Not declined today; the window is not open. But the case is thinner than the backlog entry states, and run 27 should not read "16 impressions" as the whole story.**

## 4. AI PANEL — NULL. The instrument is unavailable, and a substitute would break the series.

**Claude in Chrome is not connected. Two retries, same result.** This is an outage, not a permission refusal (Trap 12's distinction): the extension is unreachable, and the error names its own fix — install/sign in to the side panel.

**No reading was taken and none was manufactured.** The panel's own rule is explicit: *"a reading that could not be taken is null — leave the log alone rather than writing in a low share."* Series stays at ten readings, last value **2/10**, and remains a **2–3 range, not a level**.

**The substitute was considered and declined.** The built-in browser pane works — it read `sitemap.xml`, `/mentoring`'s rendered head and JSON-LD, and served every site check in §2. But it is a **different profile, signed out**, which is precisely the mid-series change of reading conditions that run 25 proposed for Perplexity and flagged as needing Daniel's sign-off. **Taking Google AI Mode there today would break comparability across the one metric the panel exists to produce.** Reported as an ask rather than done quietly.

**This also blocks the Generative AI report**, which §1 confirms is still UI-only. So **Generative AI impressions were not re-read this run** — run 25's carry-forward #5 asked for exactly that before treating the 258 → 337 rise as real, and it remains unconfirmed. **Do not describe that rise as established until it has been read a second time.**

## 5. Measurement — money pages at 5 for a fourth overlapping reading. Still not independent.

**28d to 2026-09-08: 60 clicks / 5,901 impressions / 1.0% / position 22.1.** Desktop 48 / 5,258; mobile 12 / 639.

| Money page | Clicks |
| --- | --- |
| `/pricing` | 3 |
| `/contact` | 1 |
| `/services/90-day-growth-sprint` | 1 |
| `/book` | 0 |
| **Total** | **5** |

**Sequence 2 → 3 → 3 → 5 → 5 → 5 → 5.** **This window shares 26 of 28 days with run 25's. It is a fourth report of substantially the same observation, and run 25's correction applies unchanged: stacking these reads as corroboration when it is repetition.** The first genuinely independent window still opens **2026-09-29**.

**Clicks / anonymised / non-homepage: … 61/36/18 → 60/35/18 → 60/34/17.**

**Named clicks 26 of 60**, from the same four queries as every previous run — `we scale startups` 18, `wescalestartups` 5, `we scale` 2, `we scale ab` 1. **Twenty-six runs, and still zero clicks from any named query that is neither WSS's brand nor another company's.**

Two page-level notes, neither actioned: `/facts/we-scale-startups` holds **559 impressions at 6.4 with zero clicks** (verdict 09-22, and it is judged on AI impressions, not this); `/fractional-cmo-vs-agency` **534 at 37.3, zero** — unchanged and authority-bound.

## 6. State files — PLAYBOOK.md split rather than compacted

PLAYBOOK.md reached **40,874 bytes**, over the task file's 40KB threshold, so compaction was due before anything else. **It was split instead**, which is the more durable move and the one the file's own rule points at: *"treat 'this file needs appending to' as a signal to split it, not to push 60KB."*

- **`seo-bot/VERDICTS.md` (new)** — the run-by-run verdict record and the "what worked / what didn't" history.
- **`seo-bot/PLAYBOOK.md`** — current understanding, connector notes, backlog, standing decisions. Now small enough to re-emit safely every run, which is the actual requirement.

**`QUERY-MAP.md` is at 28,298 bytes and is the next one to hit this.** Run 26's rows went to **`seo-bot/query-map/2026-09-11-run26.md`** rather than into the main file. Same pattern as `ai-visibility/` and `outreach/`.

## 7. What was deliberately not done

- **The mentoring page itself not touched** — prohibition 19. Only the sitemap path list and the generated lastmod map.
- **No page inside an open verdict window touched.** Run 9 (09-16), run 15 (09-17), run 19 (09-22), runs 10–13 and backlog 10's lead (09-23), original research (09-24), run 3 (10-03).
- **Backlog 15 not acted on** — blocked to 09-17. §3 is the shape check run 24 requested, not an intervention.
- **No AI panel reading logged**, and no substitute instrument used. §4.
- **The API repair not diagnosed** — standing instruction, and §1 states the cause as unestablished rather than guessing at it.
- **Bing not pulled.** Ninth run running; deliberate.
- **Nothing sent, posted, or created off-site.** Prohibition 17.
- **`/resources` not reopened** (backlog 17, closed), despite showing 5 clicks again — the named set is still the `wescale.com` collision.

## 8. Blocked on Daniel

1. **NEW, and it blocks half the mandate: Claude in Chrome is not connected.** Open the Claude side panel in Chrome and sign in with this account. Without it there is **no AI panel and no Generative AI report** — the two instruments that measure answer-engine visibility, which is the secondary objective in the task file.
2. **Sign-off on signed-out panel readings** (run 25's proposal, now broader). If the panel moved to the built-in browser pane permanently, it would be signed-out on every engine — which arguably measures a *buyer* better than a signed-in account does, and would fix the Perplexity contamination outright. **It breaks the ten-reading series and needs a deliberate decision, marked as a series break.**
3. **Original research from the 479+ founder sessions.** Unchanged, and still the only open Workstream C route. Publish from the **public** corpus only (`RESEARCH.md`).
4. **Facebook** — contact email still `hello@`, category "Consulting agency", 2022 cover image.
5. **Instagram** points at a Linktree rather than the site.
6. **Wikidata Q137046365** — closed as leave-and-reference; listed only so it is not silently dropped.
7. **The extension config that recreates the phantom accounts** (Trap 11). Both present again. Per-run chore until the manifest is fixed.
8. **Optional:** the search-console MCP reports 2.1.2 → 2.1.3 available. Given §1, **do not update mid-series without noting it** — if the version is what repaired the property bug, changing it again is a variable worth tracking rather than a routine upgrade.

## Carry forward

1. **RE-RUN THE `["page"]` DISCRIMINATOR EVERY RUN (§1).** The API is repaired, the cause is unknown, and an unexplained fix can un-fix itself. The UI method is demoted to fallback, not deleted.
2. **`filters` works — page-filtered query sets now cost one call (§1).** This is the cheapest instrument this bot has ever had for Workstream B. Use it on the backlog-10 sweep.
3. **Backlog 15's premise is 13 impressions, not 16, and 71% of that page is a land-acquisition collision (§3).** Judge it on that on 09-17.
4. **Generative AI impressions were NOT re-read (§4).** The 258 → 337 rise is still one reading. Do not treat it as established.
5. **The sitemap static-path list is manual, and a new top-level page will not add itself (§2).** **Standing check for every run: build the tree and assert `pages built − noindexed == sitemap <loc> count`.** It is one build and it caught this.
6. **Panel is null for reading #11. Series still ten readings, last 2/10, range 2–3.** Do not interpolate.
