# Run 11 — 2026-09-26

**Classification: MEASURE.** Nothing shipped.

## Reporting route

Daniel said in the run 10 live session that he wants reports in the session only, with no Slack. The stored task prompt still says Slack, because the prompt update was never applied. This run followed Daniel's latest direct instruction and did not post to Slack. It sent a routine notification.

## Funnel

GA4 `259840282` events, 2026-08-29 → 2026-09-25 (last 28d, GA4 default; the last two days may lag):

- 913 users, 998 `session_start`.
- `book_call` 31 events from 18 users. `sticky_book_cta_shown` 341 events from 276 users.
- `form_start` 9.

`book_call` ÷ `sticky_book_cta_shown` is 9.1% against the 8.4% baseline. **This is not a verdict.** 23 of those 28 days are from before the 2026-09-21 change, and the review date is 2026-10-19.

`FUNNEL.md` not appended: the last row is from 2026-09-23.

## Bookings since run 10 (searched `in:anywhere`, Trash included)

- **No new booking.**
- Revathy Sivasankaran declined the calendar invite for the old 29 Sept slot (25 Sept). That matches her move to 19 Oct. It is not a cancellation.
- All other notices (Josefina's cancellation, Conor, Isabelle, the Herzig-Marx podcast) are already on the ledger.

Qualified bookings in the window: still **0**.

## Measurement finding (the point of this run)

Run 10 made the `/pricing` Scorecard links a candidate, pending "click data by `data-cta`". **That data does not exist in GA4.** The property records 11 event names: `page_view`, `ga4event`, `session_start`, `first_visit`, `user_engagement`, `sticky_book_cta_shown`, `scroll`, `book_call`, `ai_referral`, `click`, `form_start`.

BaseLayout pushes `cta_click`, `scorecard_start`, `pricing_click`, `resource_click`, `booking_time_selected` and `booking_completed` to the dataLayer. None of them reach GA4 under its own name. `ga4event` (1,135 events, 800 users) is a generic GTM tag that fires for almost every user, which looks like `page_context`.

Two consequences:

1. The `/pricing` Scorecard-versus-book question can't be answered from GA4. The only route is Clarity click maps on `/pricing`. The next run should read those.
2. `booking_completed` is not a GA4 event, although the BaseLayout comment says it should be a key event. The GA4 "key events" figure is `book_call` clicks, not completed bookings. Gmail stays the only completed-booking source.

GTM is on the no-touch list, so this goes to Daniel.

## Diagnosis

No new evidence of friction on a money page. All five core money pages return 200 in 0.26–0.55s. Daniel is travelling until mid-October, and new calls are being pushed past 15 Oct.

## Why nothing shipped

- Gate 1 fails: no evidence-backed constraint.
- Any CTA change would confound the sitewide `StickyBookCta.astro` row (OPEN to 2026-10-19). The `book_call` event counts in-page and sticky clicks together.
- The SEO bot's money-page title/meta window runs to 2026-09-29.

**LOCKED PAGES:** unchanged from run 10. There have been no site content commits since `ebf5643` (2026-09-23).

## Verdicts

None due. Next: `StickyBookCta.astro` on 2026-10-19.

## Handoffs to seo-bot

None.

## Assumptions

- A "Declined" calendar reply for an already-moved slot is not a new cancellation.
- `ga4event` is read as the generic GTM tag. Its event-name parameter was not inspected because that needs an exploration or a custom dimension.

## Blocked on Daniel

1. **GTM:** forward `cta_click` (with the `data-cta` label as a parameter) and `booking_completed` to GA4, and register `cta` as a custom dimension. That unlocks per-CTA click data and a real on-site booking count.
2. Authorise the work calendar on the Google Calendar connector (carried over).
3. Apply the updated task prompt (in-session reporting, `in:anywhere`) so the stored prompt matches his direction.

## Next run

Read Clarity click and scroll data for `/pricing` (28d): how many clicks each of the two Scorecard links gets compared with the two book buttons.
